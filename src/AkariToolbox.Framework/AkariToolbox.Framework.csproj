<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>AkariToolbox.Framework</RootNamespace>
    <AssemblyName>AkariToolbox.Framework</AssemblyName>
    <Description>Reusable WinUI 3 MVVM framework: navigation, dialogs, services, converters, behaviors, collections and localization.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.WindowsAppSDK" />
    <PackageReference Include="Microsoft.Windows.SDK.BuildTools" />
    <PackageReference Include="CommunityToolkit.Mvvm" />
    <PackageReference Include="Microsoft.Xaml.Behaviors.WinUI.Managed" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <!-- Throwaway test fixture for IScriptRunner.RunEmbeddedScriptAsync (Plan 02-01, Task 2).
         Embedded here (not in AkariToolbox.Tests) because RunEmbeddedScriptAsync's production
         implementation searches typeof(ScriptRunner).Assembly — embedding the fixture in this
         project is what lets the test exercise the real assembly-resolution contract instead
         of a mocked one. -->
    <EmbeddedResource Include="Fixtures\exit7.ps1" />
  </ItemGroup>

</Project>
