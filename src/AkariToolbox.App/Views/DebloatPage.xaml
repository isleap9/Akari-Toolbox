<Page
    x:Class="AkariToolbox.App.Views.DebloatPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:AkariToolbox.App.Models">

    <ScrollViewer>
        <StackPanel Padding="32" Spacing="16">

            <TextBlock
                Text="Debloat"
                Style="{StaticResource TitleTextBlockStyle}" />

            <ItemsRepeater ItemsSource="{x:Bind ViewModel.CategoryGroups}">
                <ItemsRepeater.Layout>
                    <StackLayout Orientation="Vertical" Spacing="8" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="models:DebloatCategoryGroup">
                        <StackPanel Spacing="8" Margin="0,16,0,0">
                            <TextBlock
                                Text="{x:Bind Name}"
                                Style="{StaticResource SubtitleTextBlockStyle}" />

                            <ItemsRepeater ItemsSource="{x:Bind Actions}">
                                <ItemsRepeater.Layout>
                                    <StackLayout Orientation="Vertical" Spacing="8" />
                                </ItemsRepeater.Layout>
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="models:DebloatActionItem">
                                        <Border
                                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                            CornerRadius="8"
                                            Padding="16"
                                            MaxWidth="640"
                                            HorizontalAlignment="Stretch">
                                            <Grid ColumnSpacing="16">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" Spacing="4">
                                                    <TextBlock Text="{x:Bind Title}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                                    <TextBlock
                                                        Text="{x:Bind Description}"
                                                        TextWrapping="Wrap"
                                                        Opacity="0.8"
                                                        Style="{StaticResource CaptionTextBlockStyle}" />
                                                    <TextBlock
                                                        Text="Requires confirmation before running"
                                                        Visibility="{x:Bind RequiresConfirmation}"
                                                        TextWrapping="Wrap"
                                                        Foreground="{ThemeResource SystemFillColorCautionBrush}"
                                                        Style="{StaticResource CaptionTextBlockStyle}" />
                                                    <TextBlock
                                                        Text="Undo downloads a binary that is not signature-verified"
                                                        Visibility="{x:Bind UndoDownloadsUnverifiedBinary}"
                                                        TextWrapping="Wrap"
                                                        Foreground="{ThemeResource SystemFillColorCautionBrush}"
                                                        Style="{StaticResource CaptionTextBlockStyle}" />
                                                </StackPanel>

                                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                                    <ProgressRing
                                                        IsActive="{x:Bind IsRunning, Mode=OneWay}"
                                                        Visibility="{x:Bind IsRunning, Mode=OneWay}"
                                                        Width="20"
                                                        Height="20" />
                                                    <Button
                                                        Content="Run"
                                                        Command="{Binding ElementName=RootPage, Path=DataContext.RunActionCommand}"
                                                        CommandParameter="{x:Bind}" />
                                                    <Button
                                                        Content="Undo"
                                                        Visibility="{x:Bind HasUndo}"
                                                        Command="{Binding ElementName=RootPage, Path=DataContext.UndoActionCommand}"
                                                        CommandParameter="{x:Bind}" />
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </StackPanel>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>

        </StackPanel>
    </ScrollViewer>
</Page>
