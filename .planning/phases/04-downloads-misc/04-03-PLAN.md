---
phase: 04-downloads-misc
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/AkariToolbox.App/Services/AppCatalog.cs
  - src/AkariToolbox.App/Resources/DownloadsScripts/frameview-harden.ps1
  - src/AkariToolbox.App/Resources/DownloadsScripts/nvidiaapp-harden.ps1
  - src/AkariToolbox.App/Resources/DownloadsScripts/potplayer-harden.ps1
  - src/AkariToolbox.App/Resources/DownloadsScripts/rockstar-harden.ps1
  - src/AkariToolbox.App/Resources/DownloadsScripts/ubisoft-harden.ps1
  - src/AkariToolbox.App/Resources/DownloadsScripts/eft-install.ps1
  - src/AkariToolbox.App/AkariToolbox.App.csproj
  - src/AkariToolbox.Tests/DownloadsCatalogTests.cs
autonomous: false
requirements: [DOWNLOADS-02]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The Downloads catalog contains zero duplicate WingetId or Name entries across all 42 rows (29 existing + 13 new) — enforced by an automated test, preventing a user from installing the same app twice via two separate catalog rows (closes 04-RESEARCH.md Pitfall 4: Epic Games Launcher and GOG Galaxy are NOT re-added, since EpicGames.EpicGamesLauncher and GOG.Galaxy already exist in the 29-app base catalog)."
    - "Escape From Tarkov installs via a dedicated direct-CDN embedded script (D-03's documented exception), not a fabricated/guessed winget ID — because no winget package exists for it (confirmed live this session per 04-RESEARCH.md)."
    - "Nvidia App's msstore-sourced install path is manually verified to complete silently (via the blocking checkpoint) before being considered production-ready, since Microsoft Store-sourced winget installs may not honor --silent identically to a normal winget-source EXE/MSI installer."
    - "Every one of the 5 hardening scripts (Frame View, Nvidia App, PotPlayer, Rockstar Games, Ubisoft Connect) only touches OS-standard Start Menu shortcut folder paths (ProgramData/AppData), never the app's own Program Files install path — avoiding 04-RESEARCH.md Pitfall 1 (winget's install location may differ from the source script's hardcoded path)."
    - "eft-install.ps1 verifies the downloaded EFT launcher's Authenticode signature (Get-AuthenticodeSignature .Status -eq 'Valid') before executing it under elevation, and deletes the file + aborts without running it on any non-Valid status — closing CLAUDE.md's 'never execute unverified downloaded binary under elevation' rule for the one catalog entry that cannot use SHA256 pinning (D-08's manifest approach in Plan 04-02 only covers PostInstall's fixed-content assets)."
  artifacts:
    - src/AkariToolbox.App/Resources/DownloadsScripts/frameview-harden.ps1
    - src/AkariToolbox.App/Resources/DownloadsScripts/eft-install.ps1
  key_links:
    - "AppCatalog.Apps (13 new rows) -> AppInstallerService.InstallAsync -> IScriptRunner.RunEmbeddedScriptAsync(HardeningResourceSuffix | DirectInstallResourceSuffix)"
  prohibitions:
    - "Must not re-add Epic Games Launcher or GOG Galaxy as new catalog rows — both already exist verbatim in the 29-app base catalog with the same winget IDs (EpicGames.EpicGamesLauncher, GOG.Galaxy)."
    - "Must not guess or fabricate a winget ID for Escape From Tarkov — none exists; it must use the direct-CDN DirectInstallResourceSuffix exception."
    - "Must not execute eft-install.ps1's downloaded installer without first checking Get-AuthenticodeSignature returns Status 'Valid' — a corrupted or tampered launcher binary must never run under elevation (CLAUDE.md's single highest-severity failure mode for this app)."
    - supply-chain-class-risk: "Package-ID spoofing/typo risk (OWASP A08 Software/Data Integrity Failures) — mitigated by this session's requirement that every new WingetId matches 04-RESEARCH.md's live-verified Winget/Package Legitimacy Audit table exactly; no ID may be introduced that was not verified there."
---

<objective>
Expand the Downloads catalog (built end-to-end in Plan 04-01) with the 13 genuinely-new apps from D-02's source list (15 named minus 2 duplicates: Epic Games Launcher and GOG Galaxy already exist), each installed via winget per D-03, with D-04's real per-app post-install hardening where the source script actually provides privacy/perf value beyond a bare install (Start Menu shortcut cleanup for 5 apps), and D-05's category assignment (Gaming vs. Utilities).

Purpose: Deliver the full, corrected 42-app catalog DOWNLOADS-02 promises, without perpetuating CONTEXT.md's literal-but-inconsistent "15 new apps" framing (04-RESEARCH.md's live verification found 2 duplicates and 1 no-winget-package exception that the planner must resolve, not silently work around).
Output: 13 new `AppDefinition` catalog rows (42 total), 5 hardening scripts, 1 direct-CDN-install script, live-verified winget IDs, one human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-downloads-misc/04-CONTEXT.md
@.planning/phases/04-downloads-misc/04-RESEARCH.md
@.planning/phases/04-downloads-misc/04-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: One fully-featured new app end-to-end — Frame View (winget + hardening)</name>
  <files>src/AkariToolbox.App/Services/AppCatalog.cs, src/AkariToolbox.App/Resources/DownloadsScripts/frameview-harden.ps1, src/AkariToolbox.App/AkariToolbox.App.csproj</files>
  <read_first>
    src/AkariToolbox.App/Services/AppCatalog.cs (this plan's Plan-04-01-authored 29-entry catalog to append to, and its `&lt;remarks&gt;` doc comment to extend)
    C:/Users/isleap/Desktop/AkariOS Tweaks/4 Installers/1 Installers.ps1 lines 343-361 (Frame View's source branch: IWR download, silent install, then `Move-Item` the Start Menu shortcut from `$env:ProgramData\Microsoft\Windows\Start Menu\Programs\NVIDIA FrameView\FrameView.lnk` to the Programs root and `Remove-Item` the now-empty `NVIDIA FrameView` folder)
    src/AkariToolbox.App/Resources/GamingScripts/driverclean-auto.ps1 (any existing embedded `.ps1` for house style/header conventions — non-elevating, no menu scaffolding, since these are invoked directly by `IScriptRunner.RunEmbeddedScriptAsync`, not launched interactively)
  </read_first>
  <action>
    Add one new `AppDefinition` row to `AppCatalog.Apps`, category "Utilities": `Name="Frame View", Description="NVIDIA's lightweight FPS/performance overlay and benchmarking tool", Category="Utilities", WingetId="Nvidia.FrameView", HardeningResourceSuffix="frameview-harden.ps1"` (winget ID live-verified in 04-RESEARCH.md's Winget/Package Legitimacy Audit table).

    Create `Resources/DownloadsScripts/frameview-harden.ps1` containing only the shortcut-cleanup portion of the source branch (not the download/install — winget already does that): `Move-Item -Path "$env:ProgramData\Microsoft\Windows\Start Menu\Programs\NVIDIA FrameView\FrameView.lnk" -Destination "$env:ProgramData\Microsoft\Windows\Start Menu\Programs" -Force -ErrorAction SilentlyContinue`, then `Remove-Item "$env:ProgramData\Microsoft\Windows\Start Menu\Programs\NVIDIA FrameView" -Recurse -Force -ErrorAction SilentlyContinue`. This path is OS-standard (Start Menu Programs root), not dependent on where winget installs the actual FrameView binary — this is why it is safe to port even though D-03 changed the install mechanism (04-RESEARCH.md Pitfall 1 does not apply to Start-Menu-folder-only cleanup).

    In `AkariToolbox.App.csproj`, add `&lt;EmbeddedResource Include="Resources\DownloadsScripts\frameview-harden.ps1" /&gt;` to a new or existing `ItemGroup`, with a comment noting D-04 provenance (per-app post-install hardening, source: `1 Installers.ps1`).
  </action>
  <verify>
    <automated>dotnet build src/AkariToolbox.App/AkariToolbox.App.csproj -c Debug -p:Platform=x64</automated>
  </verify>
  <acceptance_criteria>
    - `AppCatalog.Apps` contains exactly one "Frame View" row with `WingetId == "Nvidia.FrameView"` and `HardeningResourceSuffix == "frameview-harden.ps1"`.
    - The embedded resource resolves via `typeof(App).Assembly.GetManifestResourceNames()` ending with `frameview-harden.ps1`.
  </acceptance_criteria>
  <done>Frame View is a real, installable, hardened catalog entry proving the full D-02/D-03/D-04 mechanism before the remaining 12 apps are added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remaining 12 new apps — 11 winget-only/hardened + Escape From Tarkov's direct-CDN exception</name>
  <files>src/AkariToolbox.App/Services/AppCatalog.cs, src/AkariToolbox.App/Resources/DownloadsScripts/nvidiaapp-harden.ps1, src/AkariToolbox.App/Resources/DownloadsScripts/potplayer-harden.ps1, src/AkariToolbox.App/Resources/DownloadsScripts/rockstar-harden.ps1, src/AkariToolbox.App/Resources/DownloadsScripts/ubisoft-harden.ps1, src/AkariToolbox.App/Resources/DownloadsScripts/eft-install.ps1, src/AkariToolbox.App/AkariToolbox.App.csproj</files>
  <read_first>
    C:/Users/isleap/Desktop/AkariOS Tweaks/4 Installers/1 Installers.ps1 lines 107-121 (Roblox), 149-178 (Battle.net), 219-235 (Electronic Arts), 272-298 (Escape From Tarkov — the full install to port, since no winget package exists), 419-433 (League of Legends), 537-550 (Nvidia App), 552-566 (OBS Studio), 567-596 (Onboard Memory Manager), 597-614 (PotPlayer), 616-634 (Rockstar Games), 686-702 (Ubisoft Connect), 703-717 (Valorant)
    04-RESEARCH.md's Winget/Package Legitimacy Audit table (every WingetId below must match it exactly)
  </read_first>
  <action>
    Append 11 new `AppDefinition` rows to `AppCatalog.Apps`:
    - Category "Gaming": `Roblox, "Cross-platform game creation and play platform", Gaming, "Roblox.Roblox"` (no hardening — source branch is a bare download+install with no extra step).
    - `Battle.net, "Blizzard's game launcher and updater", Gaming, "Blizzard.BattleNet"` (no hardening — the source script's shortcut-creation logic targets its own `--installpath=` value, which winget will not necessarily match; per Pitfall 1 this is NOT safe to port, unlike the OS-standard Start-Menu-cleanup scripts in Task 1).
    - `Electronic Arts, "EA's desktop app for EA game titles", Gaming, "ElectronicArts.EADesktop"` (no hardening — bare install only in the source).
    - `League of Legends (NA), "Riot Games' MOBA launcher, NA server", Gaming, "RiotGames.LeagueOfLegends.NA"` (D-05/Open-Question-1 resolution: no generic winget ID exists, only region-suffixed; defaults to NA per 04-RESEARCH.md's recommendation — no hardening, source has none beyond bare install).
    - `Rockstar Games, "Rockstar's game launcher", Gaming, "RockstarGames.Launcher", HardeningResourceSuffix="rockstar-harden.ps1"`.
    - `Ubisoft Connect, "Ubisoft's game launcher and social platform", Gaming, "Ubisoft.Connect", HardeningResourceSuffix="ubisoft-harden.ps1"`.
    - `Valorant (NA), "Riot Games' tactical shooter, NA server", Gaming, "RiotGames.Valorant.NA"` (same region-default rationale as League of Legends; no hardening).
    - Category "Utilities": `OBS Studio, "Free, open-source streaming and screen recording software", Utilities, "OBSProject.OBSStudio"` (no hardening).
    - `Onboard Memory Manager, "Logitech peripheral onboard-memory configuration utility", Utilities, "Logitech.OnboardMemoryManager"` (no hardening — the source script's shortcut-creation logic targets its own custom `Program Files (x86)\Onboard Memory Manager` install path, which is Pitfall-1-unsafe to port once winget controls the install location).
    - `PotPlayer, "Lightweight, feature-rich media player", Utilities, "Daum.PotPlayer", HardeningResourceSuffix="potplayer-harden.ps1"` (note the corrected ID — `PotPlayer.PotPlayer` does not exist per 04-RESEARCH.md's live verification).
    - `Nvidia App, "NVIDIA's unified GPU driver/control app (Microsoft Store-sourced)", Utilities, "XP8CLZL93F5Z4P", WingetSource="msstore", HardeningResourceSuffix="nvidiaapp-harden.ps1"`.

    For Escape From Tarkov (Gaming), add: `Name="Escape From Tarkov", Description="Battlestate Games' tactical extraction shooter (installed via direct download — no winget package exists for this title)", Category="Gaming", WingetId="", DirectInstallResourceSuffix="eft-install.ps1"` — `AppInstallerService.InstallAsync` (built in Plan 04-01) already dispatches to `DirectInstallResourceSuffix` when non-null, bypassing winget entirely.

    Create the 4 remaining Start-Menu-only hardening scripts (same OS-standard-path rationale as Task 1's Frame View script — none of these touch the app's own Program Files install path):
    - `nvidiaapp-harden.ps1`: Move `$env:ProgramData\Microsoft\Windows\Start Menu\Programs\NVIDIA Corporation\NVIDIA App.lnk` to the Programs root (`-Force -ErrorAction SilentlyContinue`), then remove the now-empty `NVIDIA Corporation` folder (`-Recurse -Force -ErrorAction SilentlyContinue`).
    - `potplayer-harden.ps1`: Move `$env:ProgramData\Microsoft\Windows\Start Menu\Programs\PotPlayer\PotPlayer 64 bit.lnk` to the Programs root, then remove the now-empty `PotPlayer` folder.
    - `rockstar-harden.ps1`: Move `$env:AppData\Microsoft\Windows\Start Menu\Programs\Rockstar Games\Rockstar Games Launcher.lnk` to `$env:ProgramData\Microsoft\Windows\Start Menu\Programs`, then remove the now-empty `$env:AppData\Microsoft\Windows\Start Menu\Programs\Rockstar Games` folder (note: source path is user-level AppData, matching the source script exactly).
    - `ubisoft-harden.ps1`: Move `$env:AppData\Microsoft\Windows\Start Menu\Programs\Ubisoft\Ubisoft Connect\Ubisoft Connect.lnk` to `$env:ProgramData\Microsoft\Windows\Start Menu\Programs`, then remove the now-empty `$env:AppData\Microsoft\Windows\Start Menu\Programs\Ubisoft` folder.

    Create `eft-install.ps1` as the full, self-contained direct-CDN install (D-03's documented single-app exception — this is the whole install mechanism, not a hardening add-on): download `https://prod.escapefromtarkov.com/launcher/download` to a temp `.exe` via `Invoke-WebRequest` (HTTPS-only — do not add `-SkipCertificateCheck` or otherwise relax TLS validation). Before running the downloaded binary, verify its Authenticode signature: `$sig = Get-AuthenticodeSignature -FilePath $tempExePath; if ($sig.Status -ne 'Valid') { Write-Output "[EFT-INSTALL] Authenticode signature verification FAILED (Status: $($sig.Status)) — deleting untrusted installer, aborting."; Remove-Item $tempExePath -Force -ErrorAction SilentlyContinue; exit 1 }`. This is the real, implementable mitigation for a launcher whose build changes over time and has no stable pinnable SHA256 (unlike PostInstall's fixed-content assets in Plan 04-02): a `Valid` Authenticode status proves the binary's bytes are unmodified since signing by a certificate chaining to a Windows-trusted root, catching a tampered or corrupted download before it ever runs under elevation, per CLAUDE.md's project-wide rule against executing unverified downloaded binaries under admin rights. Only when `$sig.Status -eq 'Valid'`, run the installer silently with `/VERYSILENT /NORESTART` and wait for exit, create a desktop shortcut targeting `$env:SystemDrive\Battlestate Games\BsgLauncher\BsgLauncher.exe` named "Battlestate Games Launcher.lnk", then move/clean the Start Menu shortcut the installer creates under `Battlestate Games\Battlestate Games Launcher.lnk` into the Programs root the same way as the other cleanup scripts.

    Add all 5 new `&lt;EmbeddedResource Include="Resources\DownloadsScripts\*.ps1" /&gt;` lines to `AkariToolbox.App.csproj`.
  </action>
  <reversibility rating="reversible">Region defaults for League of Legends/Valorant (.NA) — swapping to a different region ID later, or adding a region picker, is a localized catalog-row edit, not a mechanism-wide change.</reversibility>
  <verify>
    <automated>dotnet build src/AkariToolbox.App/AkariToolbox.App.csproj -c Debug -p:Platform=x64</automated>
  </verify>
  <acceptance_criteria>
    - `AppCatalog.Apps.Count == 42` (29 base + 13 new: Frame View from Task 1 + these 12).
    - No two rows share the same `Name` or the same non-empty `WingetId` — specifically, no row exists for "Epic Games Launcher"/"EpicGames.EpicGamesLauncher" or "GOG Galaxy"/"GOG.Galaxy" beyond the single pre-existing base-catalog row.
    - Escape From Tarkov's row has `WingetId == ""` and a non-null `DirectInstallResourceSuffix`.
    - All 5 new embedded `.ps1` resources resolve via `Assembly.GetManifestResourceNames()`.
    - `eft-install.ps1` calls `Get-AuthenticodeSignature` on the downloaded installer and exits (non-zero, without invoking the installer) when `.Status` is not `'Valid'` — no unconditional execution path exists.
  </acceptance_criteria>
  <done>All 13 new apps (12 winget-based here + Frame View from Task 1) are real, correctly-categorized, live-verified catalog entries; Escape From Tarkov's no-winget-package gap is closed via a documented, self-contained exception rather than a fabricated ID.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify Nvidia App's msstore-sourced silent install actually completes</name>
  <what-built>
    Nvidia App catalog entry (WingetId "XP8CLZL93F5Z4P", WingetSource "msstore") added in Task 2. This is the only entry in the 42-app catalog with no plain `winget`-source package — only a Microsoft Store listing, which 04-RESEARCH.md's Package Legitimacy Audit and Pitfall 3 flag as untested: msstore-sourced installs may not honor `--silent`/`--accept-package-agreements` identically to a normal EXE/MSI winget install, and may hang on a Store consent prompt `--silent` cannot suppress.
  </what-built>
  <how-to-verify>
    Run `winget install --id XP8CLZL93F5Z4P --source msstore --silent --accept-package-agreements --accept-source-agreements` on a real Windows machine (or via the app's own Downloads page, selecting only Nvidia App and clicking Install Selected) and confirm it completes without hanging on a consent prompt, with a zero exit code.
  </how-to-verify>
  <resume-signal>Type "approved" if it completes silently with exit code 0 (catalog entry stays as-is, proceed to Task 4); otherwise describe what happened (hang, consent prompt, non-zero exit) — do not silently ship a catalog entry known to hang, flag for a follow-up decision (e.g. drop the entry, or add a one-time "open Store page" fallback) before this plan is considered fully verified.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Full 42-app catalog regression tests</name>
  <files>src/AkariToolbox.Tests/DownloadsCatalogTests.cs</files>
  <read_first>src/AkariToolbox.Tests/DownloadsCatalogTests.cs (Plan 04-01's base tests to extend, not replace — see its own doc note about being extended by this plan)</read_first>
  <behavior>
    - Test: `AppCatalog.Apps.Count == 42`.
    - Test: `AppCatalog.Apps.Select(a => a.WingetId).Where(id => id.Length > 0)` has no duplicates.
    - Test: `AppCatalog.Apps.Select(a => a.Name)` has no duplicates.
    - Test: exactly one row has a non-null `DirectInstallResourceSuffix` (Escape From Tarkov) and its `WingetId` is empty.
    - Test: every row with a non-null `HardeningResourceSuffix` resolves to a real embedded resource via `Assembly.GetManifestResourceNames()`.
    - Test: the Nvidia App row has `WingetSource == "msstore"`.
  </behavior>
  <action>Add the theory/fact tests above to the existing `DownloadsCatalogTests` class (do not duplicate the Plan 04-01 tests already present).</action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj -c Debug -p:Platform=x64 --filter "FullyQualifiedName~DownloadsCatalogTests"</automated>
  </verify>
  <acceptance_criteria>
    - All `DownloadsCatalogTests` facts pass, old and new.
  </acceptance_criteria>
  <done>The full 42-app catalog's uniqueness, hardening-resource resolvability, and Escape From Tarkov/Nvidia App exception shapes are locked by automated regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App -> winget CLI (msstore source) | Nvidia App resolves through the Microsoft Store source rather than the default community winget source |
| App -> Battlestate Games CDN (direct download) | Escape From Tarkov's installer is fetched from a vendor CDN with no SHA256/signature check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Spoofing | `AppCatalog` new rows' `WingetId` values | high | mitigate | Every ID cross-checked against 04-RESEARCH.md's live `winget show --exact` audit table before being added — no guessed/training-data ID is used |
| T-04-09 | Tampering | `eft-install.ps1`'s direct CDN download (no SHA256 pinning possible — vendor build changes over time) | medium | mitigate | `Get-AuthenticodeSignature` requiring `.Status -eq 'Valid'` before execution — a valid signature chain proves the binary is unmodified since signing, catching a tampered/corrupted download without needing a pinned hash; installer is deleted and the script aborts (non-zero exit) on any non-Valid status, satisfying CLAUDE.md's "never execute unverified downloaded binary under elevation" rule via signature trust instead of hash pinning |
| T-04-10 | Denial of Service | Nvidia App's msstore install hanging on an unsuppressible consent prompt | medium | mitigate | Task 3's blocking human-verify checkpoint gates this specific entry before it is considered production-ready |
</threat_model>

<verification>
`dotnet build` succeeds; `dotnet test --filter DownloadsCatalogTests` passes with the full 42-app catalog; Task 3's checkpoint is explicitly confirmed before the plan is marked complete.
</verification>

<success_criteria>
Downloads catalog reaches its full 42-entry v1 scope with zero duplicate/fabricated package IDs, D-04 hardening applied only where OS-standard-path-safe, Escape From Tarkov's no-winget-package gap closed via a documented exception, and Nvidia App's msstore path human-verified.
</success_criteria>

<output>
Create `.planning/phases/04-downloads-misc/04-03-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's contribution)
- 13 new `AppDefinition` rows in `AppCatalog.Apps` (42 total)
- `frameview-harden.ps1`, `nvidiaapp-harden.ps1`, `potplayer-harden.ps1`, `rockstar-harden.ps1`, `ubisoft-harden.ps1` (embedded D-04 hardening scripts)
- `eft-install.ps1` (embedded D-03 direct-CDN-install exception script)
</output>
