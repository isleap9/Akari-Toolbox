---
phase: 04-downloads-misc
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/AkariToolbox.App/Models/MiscItem.cs
  - src/AkariToolbox.App/Services/IContextMenuService.cs
  - src/AkariToolbox.App/Services/ContextMenuService.cs
  - src/AkariToolbox.App/ViewModels/MiscViewModel.cs
  - src/AkariToolbox.App/Views/MiscPage.xaml
  - src/AkariToolbox.App/Views/MiscPage.xaml.cs
  - src/AkariToolbox.App/Services/TweakHandlerRegistration.cs
  - src/AkariToolbox.App/App.xaml.cs
  - src/AkariToolbox.App/MainWindow.xaml.cs
  - src/AkariToolbox.App/ViewModels/HomeViewModel.cs
  - src/AkariToolbox.Tests/MiscContextMenuTests.cs
autonomous: true
requirements: [MISC-01, MISC-02]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "User can navigate to the Misc page from Home/nav and see all 13 entries (12 direct-carry context-menu commands + the 13th classic-menu-restore toggle) in the predecessor's exact declared order, with the 13th listed last."
    - "Clicking Add on Take Ownership shows a confirmation dialog first (D-11) and does not write any registry keys if the user declines; the other 12 entries (including the 13th toggle) apply with zero friction, matching the predecessor's one-click behavior."
    - "Adding an entry that is already added is idempotent — IRegistryService.SetValue overwrites the same values without creating duplicate sibling keys or throwing; removing an entry that was never added is idempotent — DeleteSubKeyTree/DeleteValue never throw on a missing key."
    - "MiscViewModel.Items always contains exactly 13 compiled-in entries, never empty or null — there is no dynamic/runtime-variable input path for the Misc catalog."
    - "The 'Kill Not Responding' entry's Add targets RegistryHive.ClassesRoot while its Remove targets RegistryHive.LocalMachine + \"SOFTWARE\\Classes\\DesktopBackground\\Shell\" — this hive asymmetry is preserved exactly as the predecessor implemented it, not 'fixed' to a single hive."
    - statement: "AddAsync/RemoveAsync are CommunityToolkit.Mvvm [RelayCommand]-generated AsyncRelayCommands, which by default disable re-entrant execution while already running (no AllowConcurrentExecutions=true is set) — a rapid double-click on the same entry's Add button while its confirm dialog is open is a no-op, not a race."
      verification: backstop
  artifacts:
    - src/AkariToolbox.App/Models/MiscItem.cs
    - src/AkariToolbox.App/Services/ContextMenuService.cs
    - src/AkariToolbox.App/ViewModels/MiscViewModel.cs
    - src/AkariToolbox.App/Views/MiscPage.xaml
  key_links:
    - "MiscViewModel.AddAsync/RemoveAsync -> IContextMenuService.Add/Remove -> IRegistryService.SetValue/DeleteSubKeyTree/DeleteValue"
    - "MiscViewModel.AddAsync(take_own) -> IDialogService.ConfirmAsync (D-11 gate)"
    - "MainWindow.NavItems + HomeViewModel.Cards 'Misc' entries -> typeof(MiscPage), IsEnabled: true"
  prohibitions:
    - "Must not apply Take Ownership's icacls grant without an explicit confirmation dialog (D-11 — the one genuinely destructive entry)."
    - "Must not let the 13th entry's Remove/Default branch DeleteSubKeyTree an ancestor key shared by unrelated shell customizations (e.g. must scope deletes to exactly the named keys/values the Clean branch created, not a shared parent like HKCR\\Folder\\shell)."
    - injection-class-risk: "Command-injection risk (OWASP Injection) — not applicable: all 13 entries' registry command strings are compiled-in constants using %1/%V as OS-native placeholder tokens, never string-interpolated from user input (see 04-RESEARCH.md V5 Input Validation note)."
  flagged_assumptions:
    - "MISC-02 ('extra misc tools') is deferred per D-12 — no must-have truth is authored for it in this phase. REQUIREMENTS.md/ROADMAP.md have been updated to record the deferral (2026-09-02). A future milestone must define concrete tool candidates before this requirement can be planned or built."
---

<objective>
Ship the Misc page end-to-end: the predecessor's 12 direct-carry context-menu Add/Remove entries (MISC-01, D-09), plus a 13th classic-context-menu-restore toggle sourced from `4 Context Menu.ps1` (D-10), with Take Ownership gated behind a confirmation dialog (D-11). MISC-02 ("extra misc tools") is explicitly deferred per D-12 — no code is added for it in this plan; see REQUIREMENTS.md/ROADMAP.md for the formal deferral record made during this planning session.

Purpose: Every one of the 12 predecessor `MiscViewModel` entries is a pure `IRegistryService` read/write with zero process-spawn risk — port them as direct carries via the already-hardened, registry-squatting-safe primitive rather than raw `Microsoft.Win32.Registry` calls.
Output: Misc page with all 13 entries, enabled in nav + Home dashboard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-downloads-misc/04-CONTEXT.md
@.planning/phases/04-downloads-misc/04-RESEARCH.md
@.planning/phases/04-downloads-misc/04-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Misc architecture end-to-end — cmd_admin (zero-friction) + take_own (D-11 confirm-gated)</name>
  <files>src/AkariToolbox.App/Models/MiscItem.cs, src/AkariToolbox.App/Services/IContextMenuService.cs, src/AkariToolbox.App/Services/ContextMenuService.cs, src/AkariToolbox.App/ViewModels/MiscViewModel.cs, src/AkariToolbox.App/Views/MiscPage.xaml, src/AkariToolbox.App/Views/MiscPage.xaml.cs, src/AkariToolbox.App/Services/TweakHandlerRegistration.cs, src/AkariToolbox.App/App.xaml.cs, src/AkariToolbox.App/MainWindow.xaml.cs, src/AkariToolbox.App/ViewModels/HomeViewModel.cs</files>
  <read_first>
    C:/Users/isleap/Documents/GitHub/AkariOS-Companion/ViewModels/MiscViewModel.cs (the full 12-entry Items list at lines 12-26, and `AddCmdAdmin`/`RemoveCmdAdmin` lines 95-111/283-288, `AddTakeOwnership`/`RemoveTakeOwnership` lines 131-150/297-301, `RestartExplorer` lines 86-91 — port source for this task's 2 entries plus the shared dispatch/explorer-restart shape)
    src/AkariToolbox.Framework/Services/IRegistryService.cs (`SetValue(RegistryHive, subKeyPath, valueName, value, kind)` and `DeleteSubKeyTree(RegistryHive, subKeyPath)` — never throws on missing)
    src/AkariToolbox.App/ViewModels/DebloatViewModel.cs (the `action.RequiresConfirmation &amp;&amp; !isUndo` -> `IDialogService.ConfirmAsync` gate pattern, lines 79-91, to mirror for `take_own`)
    src/AkariToolbox.Framework/Services/IDialogService.cs (`ConfirmAsync(title, message, confirmText, cancelText)` signature)
    src/AkariToolbox.App/Views/DebloatPage.xaml and .xaml.cs (XAML/page-constructor idiom to copy)
    src/AkariToolbox.App/Services/TweakHandlerRegistration.cs, src/AkariToolbox.App/App.xaml.cs, src/AkariToolbox.App/MainWindow.xaml.cs, src/AkariToolbox.App/ViewModels/HomeViewModel.cs (current state after Plan 04-01's Downloads wiring — this task adds the "Misc" entries alongside, without disturbing the "Downloads" entries Plan 04-01 already enabled)
  </read_first>
  <action>
    Create `MiscItem.cs`: `ObservableObject` with init-only `Key`, `Title`, `Description`, `RequiresConfirmation` (bool, true only for `"take_own"`), and `[ObservableProperty] bool IsAdded` (drives Add/Remove button visibility — a simple 2-state flag, not a run-history flag, since Misc entries are not session-tracked; default `false` on page load matching the predecessor, which never read live registry state for these).

    Create `IContextMenuService` (`void Add(string key); void Remove(string key);`) and `ContextMenuService(IRegistryService registry)` implementing it with a `switch (key)` dispatch to private methods, exactly mirroring the predecessor's structure. Implement only `cmd_admin` and `take_own` in this task (the other 10 predecessor entries + the 13th toggle are Task 2's scope):
    - `AddCmdAdmin`: for each of `Directory\Shell\OpenElevatedCMD`, `Drive\Shell\OpenElevatedCMD`, `LibraryFolder\background\Shell\OpenElevatedCMD`, `Directory\Background\Shell\OpenElevatedCMD` — `registry.SetValue(RegistryHive.ClassesRoot, root, "", "Open CMD As Administrator", RegistryValueKind.String)`, `registry.SetValue(RegistryHive.ClassesRoot, root, "Icon", "imageres.dll,-5324", RegistryValueKind.String)`, `registry.SetValue(RegistryHive.ClassesRoot, $"{root}\Command", "", @"Powershell.exe -windowstyle hidden -Command ""Start-Process cmd.exe -ArgumentList '/s,/k,pushd,%V' -Verb RunAs""", RegistryValueKind.String)`.
    - `RemoveCmdAdmin`: `registry.DeleteSubKeyTree(RegistryHive.ClassesRoot, @"Directory\Background\Shell\OpenElevatedCMD")`, same for `Directory\Shell\OpenElevatedCMD` and `Drive\Shell\OpenElevatedCMD` (the predecessor's Remove omits the `LibraryFolder\background` root that Add creates — port this asymmetry exactly as-is, it matches the predecessor byte-for-byte).
    - `AddTakeOwnership`: write `*\shell\TakeOwnership` (default value "Take Ownership", `HasLUAShield`="", `NoWorkingDirectory`="", `NeverDefault`="", and under `...\command` both the default value and `IsolatedCommand` set to `powershell.exe -windowstyle hidden -command "Start-Process cmd -ArgumentList '/c takeown /f ""%1"" && icacls ""%1"" /grant *S-1-3-4:F /c /l & pause' -Verb runAs"`), and `Directory\shell\TakeOwnership` (same value shape, but the command strings use `takeown /f ""%1"" /r /d y /skipsl` and `icacls ""%1"" /grant *S-1-3-4:F /t /c /l` — the recursive/directory variant).
    - `RemoveTakeOwnership`: `DeleteSubKeyTree(RegistryHive.ClassesRoot, @"*\shell\TakeOwnership")` and `DeleteSubKeyTree(RegistryHive.ClassesRoot, @"Directory\shell\TakeOwnership")`.

    Create `MiscViewModel : ViewModelBase` constructor-injecting `IContextMenuService contextMenu, IDialogService dialogService, ILogConsoleService log`. Set `Title = "Misc"`. Build `Items` as an `IReadOnlyList&lt;MiscItem&gt;` with all 13 declared rows in the predecessor's exact order (this task only wires the dispatch behavior for the first 2 — `cmd_admin` and `take_own` — but the full 13-row `Items` list must exist now so Task 2/3 only add dispatch logic, not restructure the list): `cmd_admin` "Open CMD as Administrator", `ps_admin` "Open PowerShell as Admin", `take_own` "Take Ownership" (`RequiresConfirmation = true`), `ctrl_panel` "Control Panel", `file_hash` "File Hash", `kill_nr` "Kill Not Responding", `win_tools` "Windows Tools", `shutdown_menu` "Shut Down Menu", `pow_assoc` "Import Power Plan (.pow)", `run_priority` "Run with Priority", `change_res` "Change Resolution", `reboot_bios` "Reboot to BIOS", then `context_menu_classic` "Classic Context Menu (Windows 10 style)" last (the 13th, D-10). Use the predecessor's exact Description strings from `MiscViewModel.cs` lines 14-25 for the first 12; author a concise description for the 13th noting it restores the classic right-click menu and removes several built-in shell items.

    Add `[RelayCommand] async Task AddAsync(MiscItem item)`: if `item.RequiresConfirmation`, await `dialogService.ConfirmAsync(item.Title, "This grants broad Everyone:FullControl permissions recursively on the selected file/folder. Continue?")` and return without calling `contextMenu.Add` if declined; otherwise call `contextMenu.Add(item.Key)`, set `item.IsAdded = true`, log via `ILogConsoleService`, then call a private `RestartExplorer()` helper. Add `[RelayCommand] async Task RemoveAsync(MiscItem item)` mirroring this without the confirmation gate (D-11 gates only Add, matching the predecessor and Debloat's precedent), calling `contextMenu.Remove(item.Key)`, `item.IsAdded = false`, log, `RestartExplorer()`. Implement `RestartExplorer()` as a private static method: `foreach (var p in Process.GetProcessesByName("explorer")) p.Kill(); Process.Start("explorer.exe");` (direct port, matches the predecessor's `MiscViewModel.RestartExplorer` exactly — a UI-refresh side effect, not a registry primitive, per RESEARCH.md's Architectural Responsibility Map). Add a `&lt;remarks&gt;` doc comment on `MiscViewModel` noting MISC-02 ("extra misc tools") is deferred per D-12 — no UI or code for it exists in this phase; see REQUIREMENTS.md/ROADMAP.md for the formal deferral record.

    Create `MiscPage.xaml`/`.xaml.cs` matching `DebloatPage`'s shape: root `ScrollViewer` > `StackPanel Padding="32" Spacing="16"`, "Misc" title, a flat `ItemsRepeater` (no category grouping — the predecessor's Misc page is a flat list) bound to `Items`, each row a `Border` card with `Title`/`Description` text, an "Add" `Button` bound to `AddCommand`/`CommandParameter={x:Bind}` and a "Remove" `Button` bound to `RemoveCommand`, and a cautionary `TextBlock` visible only when `RequiresConfirmation` is true (mirroring `DebloatPage.xaml`'s `RequiresConfirmation` caption pattern).

    In `TweakHandlerServiceCollectionExtensions.AddTweakHandlers`, append `services.AddSingleton&lt;IContextMenuService, ContextMenuService&gt;();` with a rationale comment (thin wrapper over the already-registered `IRegistryService`, not an `ITweakHandler` — no `GetState`/`SetState`, matching MISC-01's flat Add/Remove shape).

    In `App.xaml.cs`'s `BuildHost()`, add `builder.Services.AddTransient&lt;MiscViewModel&gt;();`.

    In `MainWindow.xaml.cs`'s `NavItems`, change the `"Misc"` entry from `new("Misc", "\uE712", typeof(HomePage), IsEnabled: false)` to `new("Misc", "\uE712", typeof(MiscPage))`.

    In `HomeViewModel.cs`'s `Cards`, change the `"Misc"` `HomeCard`'s `Target = typeof(HomePage), IsEnabled = false` to `Target = typeof(MiscPage), IsEnabled = true`.
  </action>
  <reversibility rating="reversible">D-10's Add=Clean/Remove=Default mapping for the 13th entry — a semantic label choice on top of a fixed registry-op set, renamable later with zero migration cost.</reversibility>
  <verify>
    <automated>dotnet build src/AkariToolbox.App/AkariToolbox.App.csproj -c Debug -p:Platform=x64</automated>
  </verify>
  <acceptance_criteria>
    - `MiscViewModel.Items.Count == 13` in the exact declared order listed above.
    - `AddAsync` for `take_own` calls `IDialogService.ConfirmAsync` exactly once before any registry write, and does not call `IContextMenuService.Add` when the dialog is declined.
    - `AddAsync` for `cmd_admin` does NOT call `ConfirmAsync`.
    - `MainWindow.xaml.cs` NavItems and `HomeViewModel.cs` Cards both resolve "Misc" to `typeof(MiscPage)` with no `IsEnabled: false`.
  </acceptance_criteria>
  <done>Misc page is reachable from Home and nav, renders all 13 entries in order, and both the zero-friction path (cmd_admin) and the D-11 confirmation-gated path (take_own) work end-to-end against real registry writes via IRegistryService.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 10 direct-carry registry entries (ps_admin, ctrl_panel, file_hash, kill_nr, win_tools, shutdown_menu, pow_assoc, run_priority, change_res, reboot_bios)</name>
  <files>src/AkariToolbox.App/Services/ContextMenuService.cs</files>
  <read_first>
    C:/Users/isleap/Documents/GitHub/AkariOS-Companion/ViewModels/MiscViewModel.cs lines 113-346 (the 10 remaining Add/Remove pairs to port verbatim: `AddPsAdmin`/`RemovePsAdmin`, `AddControlPanel`/`RemoveControlPanel`, `AddFileHash`/`RemoveFileHash`, `AddKillNotResponding`/`RemoveKillNotResponding` — note the hive asymmetry, `AddWindowsTools`/`RemoveWindowsTools`, `AddShutDownMenu`/`RemoveShutDownMenu`, `AddPowerPlanAssoc`/`RemovePowerPlanAssoc`, `AddRunWithPriority`/`RemoveRunWithPriority`, `AddChangeResolution`/`RemoveChangeResolution`, `AddRebootToBios`/`RemoveRebootToBios`)
  </read_first>
  <action>
    Extend `ContextMenuService`'s `switch` dispatch (in `Add`/`Remove`) with these 10 predecessor entries, each a direct port from `MiscViewModel.cs` using `IRegistryService` instead of raw `Microsoft.Win32.Registry` calls (same `SetValue`/`DeleteSubKeyTree`/`DeleteValue` mapping already established in Task 1):
    - `ps_admin`: same 4-root shape as `cmd_admin` (`Directory\Shell\OpenElevatedPS`, `Drive\Shell\OpenElevatedPS`, `LibraryFolder\background\Shell\OpenElevatedPS`, `Directory\Background\Shell\OpenElevatedPS`), default value "Open Powershell As Administrator", Icon "powershell.exe", command `Powershell.exe -windowstyle hidden -Command "Start-Process cmd.exe -ArgumentList '/s,/c,pushd %V && powershell' -Verb RunAs"`. Remove deletes the same 3 roots as `cmd_admin`'s Remove (omitting `LibraryFolder\background`, matching predecessor asymmetry).
    - `ctrl_panel`: `DesktopBackground\shell\ControlPanel` with `MUIVerb`/`Icon`/`Position`/`SubCommands` plus 3 sub-command keys (`shell\1ControlPanelCmd` through `shell\3ControlPanelCmd`) each with its own `MUIVerb`/`Command` shell:::{GUID} target — port the exact GUIDs from the source (`{26EE0668-A00A-44D7-9371-BEB064C98683}`, `{21EC2020-3AEA-1069-A2DD-08002B30309D}`, `{ED7BA470-8E54-465E-825C-99712043E01C}`). Remove: `DeleteSubKeyTree(ClassesRoot, "DesktopBackground\shell\ControlPanel")`.
    - `file_hash`: `*\shell\Hash` with `MUIVerb`/`SubCommands`, plus 8 sub-menu keys (`shell\01Menu` through `shell\08Menu`) for SHA1/SHA256/SHA384/SHA512/MACTripleDES/MD5/RIPEMD160 and a "Show all" entry, each `Command` running `powershell -noexit get-filehash -literalpath '%1' -algorithm {ALGO} | format-list` (the "Show all" entry chains all 3 of SHA1/SHA256/MD5 with `;` separators, `CommandFlags`=0x32). Remove: `DeleteSubKeyTree(ClassesRoot, "*\shell\Hash")`.
    - `kill_nr`: Add targets `RegistryHive.ClassesRoot` path `DesktopBackground\shell\KillNotResponding` (`MUIVerb`="Kill not responding tasks", `Icon`=`%SystemRoot%\System32\imageres.dll,-98`, `Position`="Top", `Command`=`cmd.exe /K taskkill.exe /F /FI "status eq NOT RESPONDING"`) — Remove targets `RegistryHive.LocalMachine` path `SOFTWARE\Classes\DesktopBackground\Shell`, deleting the `KillNotResponding` subtree. Preserve this hive asymmetry exactly — do NOT "fix" Remove to also use ClassesRoot (both resolve to the same physical key under HKCR's merged-view semantics; changing it risks a silent behavior divergence per RESEARCH.md Pattern 5's explicit note).
    - `win_tools`: `DesktopBackground\Shell\WindowsTools` (`MUIVerb`="Windows Tools", `Icon`="imageres.dll,-114", `Position`="Bottom", `Command`=`explorer.exe shell:::{D20EA4E1-3957-11d2-A40B-0C5020524153}`). Remove: `DeleteSubKeyTree(ClassesRoot, "DesktopBackground\Shell\WindowsTools")`.
    - `shutdown_menu`: `DesktopBackground\shell\ShutDown` (`MUIVerb`="Shut Down", `Icon`="shell32.dll,-28", `Position`="Bottom", `SubCommands`=""), plus 4 sub-keys `shell\001ShutdownInstantly` (`shutdown -s -f -t 0`), `shell\002ShutdownWarning` (`shutdown -s`), `shell\003RestartInstantly` (`shutdown -r -f -t 0`, `CommandFlags`=32), `shell\004RestartWarning` (`shutdown -r`), each with its own `MUIVerb`/`Icon`. Remove: `DeleteSubKeyTree(ClassesRoot, "DesktopBackground\shell\ShutDown")`.
    - `pow_assoc`: `.pow` key, `.pow\DefaultIcon` (default value `%SystemRoot%\System32\powercfg.cpl,-202`), `.pow\Shell\open\command` (default value `powercfg /import %1`). Remove: `DeleteSubKeyTree(ClassesRoot, ".pow")`.
    - `run_priority`: `exefile\shell\Priority` (`MUIVerb`="Run with priority", `SubCommands`=""), sub-keys `shell\001flyout` (default "Realtime", command `cmd /c start "" /Realtime "%1"`) and `shell\002flyout` (default "High", command `cmd /c start "" /High "%1"`). Remove: `DeleteSubKeyTree(ClassesRoot, "exefile\shell\Priority")`.
    - `change_res`: `Directory\background\shell\Change Res` (`MUIVerb`="Change Resolution", command `C:\Windows\System32\rundll32.exe display.dll,ShowAdapterSettings 0`). Remove: `DeleteSubKeyTree(ClassesRoot, "Directory\background\shell\Change Res")`.
    - `reboot_bios`: `Directory\background\shell\reboot to fw` (`MUIVerb`="Reboot To BIOS", command `shutdown /r /fw /t 0`). Remove: `DeleteSubKeyTree(ClassesRoot, "Directory\background\shell\reboot to fw")`.
  </action>
  <verify>
    <automated>dotnet build src/AkariToolbox.App/AkariToolbox.App.csproj -c Debug -p:Platform=x64</automated>
  </verify>
  <acceptance_criteria>
    - `ContextMenuService.Add`/`Remove` handle these 10 keys without a default/unmatched-case fall-through.
    - `kill_nr`'s Add uses `RegistryHive.ClassesRoot` and its Remove uses `RegistryHive.LocalMachine` with path `SOFTWARE\Classes\DesktopBackground\Shell` (verified by Task 4's test).
  </acceptance_criteria>
  <done>All 10 remaining direct-carry Misc entries are fully wired against real IRegistryService calls, matching the predecessor's exact registry shape including the kill_nr hive asymmetry.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 13th entry — classic context menu restore toggle (D-10)</name>
  <files>src/AkariToolbox.App/Services/ContextMenuService.cs</files>
  <read_first>
    C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/4 Context Menu.ps1 (full file, both branches — the 13th entry's exact registry operations)
  </read_first>
  <action>
    Add the 13th entry, `context_menu_classic`, to `ContextMenuService`'s `switch` dispatch (in `Add`/`Remove`), mapping the source script's "1. Context Menu: Clean (Recommended)" branch to `Add` and "2. Context Menu: Default" to `Remove` (D-10):
    - Add (13 operations, direct `IRegistryService` calls, no `reg.exe`/`regedit.exe` process spawn): `SetValue(CurrentUser, "Software\Classes\CLSID\{86ca1aa0-34aa-4e8b-a509-50c905bae2a2}\InprocServer32", "", "", String)`; `SetValue(LocalMachine, "SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer", "NoCustomizeThisFolder", 1, DWord)`; `DeleteSubKeyTree(ClassesRoot, "Folder\shell\pintohome")`; `DeleteSubKeyTree(ClassesRoot, "*\shell\pintohomefile")`; `DeleteSubKeyTree(ClassesRoot, "exefile\shellex\ContextMenuHandlers\Compatibility")`; `SetValue(LocalMachine, "SOFTWARE\Microsoft\Windows\CurrentVersion\Shell Extensions\Blocked", "{9F156763-7844-4DC4-B2B1-901F640F5155}", "", String)`; `SetValue(LocalMachine, "SOFTWARE\Microsoft\Windows\CurrentVersion\Shell Extensions\Blocked", "{09A47860-11B0-4DA5-AFA5-26D86198A780}", "", String)`; `SetValue(LocalMachine, "SOFTWARE\Microsoft\Windows\CurrentVersion\Shell Extensions\Blocked", "{f81e9010-6ea4-11ce-a7ff-00aa003ca9f6}", "", String)`; `DeleteSubKeyTree(ClassesRoot, "Folder\ShellEx\ContextMenuHandlers\Library Location")`; `DeleteSubKeyTree(ClassesRoot, "AllFilesystemObjects\shellex\ContextMenuHandlers\ModernSharing")`; `SetValue(LocalMachine, "SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer", "NoPreviousVersionsPage", 1, DWord)`; `DeleteSubKeyTree(ClassesRoot, "AllFilesystemObjects\shellex\ContextMenuHandlers\SendTo")`; `DeleteSubKeyTree(ClassesRoot, "UserLibraryFolder\shellex\ContextMenuHandlers\SendTo")`.
    - Remove (reverses each of the above individually — per RESEARCH.md Pattern 6, replace the source's temp-.reg-file-plus-`regedit.exe /S` import with direct `SetValue` calls for each named value, eliminating the temp file and process spawn entirely): `DeleteSubKeyTree(CurrentUser, "Software\Classes\CLSID\{86ca1aa0-34aa-4e8b-a509-50c905bae2a2}")`; `DeleteValue(LocalMachine, "SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer", "NoCustomizeThisFolder")`; for `Folder\shell\pintohome`: `SetValue` `AppliesTo`=`System.ParsingName:&lt;&gt;"::{f874310e-b6b7-47dc-bc84-b9e6b38f5903}" AND System.ParsingName:&lt;&gt;"::{679f85cb-0220-4080-b29b-5540cc05aab6}" AND System.IsFolder:=System.StructuredQueryType.Boolean#True"`, `CommandStateHandler`=`{b455f46e-e4af-4035-b0a4-cf18d2f6f28e}`, `CommandStateSync`="", `MUIVerb`=`@shell32.dll,-51601`, `SkipCloudDownload`=0 (DWord), and under `Folder\shell\pintohome\command`: `DelegateExecute`=`{b455f46e-e4af-4035-b0a4-cf18d2f6f28e}`; for `*\shell\pintohomefile`: `CommandStateHandler`=`{b455f46e-e4af-4035-b0a4-cf18d2f6f28e}`, `CommandStateSync`="", `MUIVerb`=`@shell32.dll,-51608`, `NeverDefault`="", `SkipCloudDownload`=0 (DWord), and under `*\shell\pintohomefile\command`: `DelegateExecute`=`{b455f46e-e4af-4035-b0a4-cf18d2f6f28e}`; `SetValue(ClassesRoot, "exefile\shellex\ContextMenuHandlers\Compatibility", "", "{1d27f844-3a1f-4410-85ac-14651078412d}", String)`; `DeleteSubKeyTree(LocalMachine, "SOFTWARE\Microsoft\Windows\CurrentVersion\Shell Extensions\Blocked")` (deletes the whole key, matching the source's `reg delete .../Blocked /f`, not just the 3 values Add wrote); `SetValue(ClassesRoot, "Folder\ShellEx\ContextMenuHandlers\Library Location", "", "{3dad6c5d-2167-4cae-9914-f99e41c12cfa}", String)`; `SetValue(ClassesRoot, "AllFilesystemObjects\shellex\ContextMenuHandlers\ModernSharing", "", "{e2bf9676-5f8f-435c-97eb-11607a5bedf7}", String)`; `DeleteValue(LocalMachine, "SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer", "NoPreviousVersionsPage")`; `SetValue(ClassesRoot, "AllFilesystemObjects\shellex\ContextMenuHandlers\SendTo", "", "{7BA4C740-9E81-11CF-99D3-00AA004AE837}", String)`; `SetValue(ClassesRoot, "UserLibraryFolder\shellex\ContextMenuHandlers\SendTo", "", "{7BA4C740-9E81-11CF-99D3-00AA004AE837}", String)`.
  </action>
  <verify>
    <automated>dotnet build src/AkariToolbox.App/AkariToolbox.App.csproj -c Debug -p:Platform=x64</automated>
  </verify>
  <acceptance_criteria>
    - `ContextMenuService.Add`/`Remove` handle the `context_menu_classic` key without a default/unmatched-case fall-through — bringing the total handled keys to all 13 across Tasks 2-3.
    - `context_menu_classic`'s Remove never calls `DeleteSubKeyTree` on a bare ancestor like `Folder\shell` or `AllFilesystemObjects\shellex\ContextMenuHandlers` — every delete/write targets the exact named leaf key/value from the source script.
  </acceptance_criteria>
  <done>The 13th Misc entry (D-10's classic-context-menu-restore toggle) is fully wired against real IRegistryService calls, completing all 13 Misc catalog entries across Tasks 1-3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Regression tests — 13-entry shape, confirmation gating, idempotency, hive-asymmetry lock</name>
  <files>src/AkariToolbox.Tests/MiscContextMenuTests.cs</files>
  <read_first>
    src/AkariToolbox.Tests/GamingWindowsTweaksTests.cs lines 604-720 (the private nested `FakeRegistryService : IRegistryService` implementation to replicate verbatim in this new test file — Seed/SetValue/GetValue/DeleteValue/SetSubKeyNames/GetSubKeyNames/DeleteSubKeyTree/WasDeleted/WasSubKeyTreeDeleted)
    src/AkariToolbox.Tests/DebloatCatalogTests.cs (FakeDialogService pattern and the confirmation-gate test style, e.g. `DebloatViewModel_confirmation_gates_only_run_direction`)
  </read_first>
  <behavior>
    - Test: `MiscViewModel.Items.Count == 13`, in the exact declared order (cmd_admin first, context_menu_classic last).
    - Test: `AddAsync` for `take_own` calls `FakeDialogService.ConfirmAsync` exactly once; when the fake returns `false`, `FakeRegistryService` records zero `SetValue` calls for any `TakeOwnership`-related path.
    - Test: `AddAsync`/`RemoveAsync` for each of the other 12 keys never calls `ConfirmAsync`.
    - Test: calling `ContextMenuService.Add("cmd_admin")` twice in a row against the same `FakeRegistryService` does not throw and results in the same final values (idempotent overwrite, not an error or a duplicate sibling key).
    - Test: calling `ContextMenuService.Remove("cmd_admin")` against a `FakeRegistryService` that was never seeded with `cmd_admin`'s keys does not throw.
    - Test: `ContextMenuService.Add("kill_nr")` writes only under `RegistryHive.ClassesRoot`; `ContextMenuService.Remove("kill_nr")` deletes only under `RegistryHive.LocalMachine` + `"SOFTWARE\Classes\DesktopBackground\Shell"` (via `FakeRegistryService.WasSubKeyTreeDeleted`).
    - Test: `ContextMenuService.Add("context_menu_classic")` and `.Remove("context_menu_classic")` each touch only their documented named keys/values (spot-check via `FakeRegistryService` that no ancestor-level key like `"Folder\shell"` bare is ever passed to `DeleteSubKeyTree`).
  </behavior>
  <action>
    Create `MiscContextMenuTests.cs` in `AkariToolbox.Tests` with a private nested `FakeRegistryService : IRegistryService` (copy `GamingWindowsTweaksTests`'s implementation verbatim per this project's per-file fake-duplication convention) and a private nested `FakeDialogService : IDialogService` (copy `DebloatCatalogTests`'s implementation). Implement the `[Fact]`/`[Theory]` tests from the behaviors above against real `ContextMenuService`/`MiscViewModel` instances constructed with these fakes plus a real or minimal fake `ILogConsoleService`.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj -c Debug -p:Platform=x64 --filter "FullyQualifiedName~MiscContextMenuTests"</automated>
  </verify>
  <acceptance_criteria>
    - All `MiscContextMenuTests` facts pass.
    - The confirmation-gating test fails loudly if a future edit accidentally adds `ConfirmAsync` gating to a non-`take_own` entry, or removes it from `take_own`.
  </acceptance_criteria>
  <done>The 13-entry shape, D-11 confirmation gating, idempotent Add/Remove behavior, and the kill_nr hive asymmetry are all locked by automated regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App -> Windows Registry (HKCR/HKLM/HKCU) | Elevated process writing/deleting context-menu registry entries with compiled-in paths/values — no user input reaches any registry path or value |
| App -> explorer.exe process | Kills and relaunches the user's Explorer shell after every Add/Remove |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Elevation of Privilege | `take_own`'s `icacls /grant *S-1-3-4:F` (Everyone:FullControl, recursive on directories) | high | mitigate | D-11's `IDialogService.ConfirmAsync` gate before every Add — already the correct, locked mitigation; Task 4's test locks that this gate cannot be silently removed |
| T-04-12 | Tampering | Registry-key squatting on any of the 13 entries' context-menu keys | medium | mitigate | `IRegistryService.SetValue`'s open-then-create pattern (already implemented, no new work this phase) — matches ARCHITECTURE.md Anti-Pattern 4 |
| T-04-13 | Denial of Service | `RestartExplorer()` killing all `explorer.exe` processes | low | accept | Direct port of the predecessor's exact, already-shipped behavior — killing/relaunching Explorer is the standard, necessary mechanism for Windows to pick up new context-menu registrations; no safer alternative exists for this class of change |
| T-04-14 | Tampering | 13th entry's Remove branch over-deleting a shared ancestor key | medium | mitigate | Task 3's action text and Task 4's test both explicitly scope every delete to the exact named leaf key/value the source script's Default branch reverses — never a bare ancestor |
</threat_model>

<verification>
`dotnet build` succeeds; `dotnet test --filter MiscContextMenuTests` passes; manual smoke check (Home -> Misc card, or nav -> Misc) renders 13 entries, Take Ownership shows a confirm dialog, the other 12 apply with one click.
</verification>

<success_criteria>
Misc page fully reachable and functional for all 13 entries; D-11 confirmation gate proven and locked by tests; MISC-02 formally deferred per D-12 with no invented content.
</success_criteria>

<output>
Create `.planning/phases/04-downloads-misc/04-04-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's contribution)
- `MiscItem` class (Models) — `Key, Title, Description, RequiresConfirmation, IsAdded`
- `IContextMenuService`/`ContextMenuService` (Services) — `Add(string)`, `Remove(string)`, 13-key dispatch
- `MiscViewModel` (ViewModels) — `Items, AddCommand, RemoveCommand, RestartExplorer`
- `MiscPage`/`.xaml.cs` (Views)
- `MiscContextMenuTests` (Tests) — including `FakeRegistryService`, `FakeDialogService`
</output>
