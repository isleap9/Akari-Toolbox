---
phase: 04-downloads-misc
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AkariToolbox.App/Services/PostInstallService.cs
  - src/AkariToolbox.App/Resources/PostInstallManifest.json
  - src/AkariToolbox.App/AkariToolbox.App.csproj
  - src/AkariToolbox.Tests/PostInstallIntegrityTests.cs
autonomous: true
requirements: [DOWNLOADS-01]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every one of the 147 relative file paths in PostInstallService's AllFiles manifest has a corresponding real, pinned SHA256 hex digest in PostInstallManifest.json — authored by actually downloading and hashing each file from the live GitHub repo this session, not invented or left as a placeholder."
    - "A file that downloads successfully but whose bytes do not match its pinned SHA256 is deleted and counted as failed, never left in place as a 'successfully mirrored' asset (D-07's core guarantee)."
    - "A file whose downloaded bytes DO match its pinned SHA256 is kept and counted as downloaded — the gate does not reject good files."
  artifacts:
    - src/AkariToolbox.App/Resources/PostInstallManifest.json
  key_links:
    - "PostInstallService.DownloadFileAsync -> manifest lookup by relative path -> VerifyFileSha256Async -> delete+fail on mismatch"
    - "AkariToolbox.App.csproj EmbeddedResource -> Resources/PostInstallManifest.json -> loaded via Assembly.GetManifestResourceStream"
  prohibitions:
    - "Must not treat a SHA256 mismatch as success and leave the tampered/corrupted file mirrored into C:\\PostInstall\\ (this is D-07's entire purpose — a silent pass-through here defeats the feature)."
    - "Must not fabricate or hand-guess any of the 147 SHA256 hex digests — every value must come from actually hashing the real downloaded bytes."
    - path-traversal-class-risk: "Not applicable — every one of the 147 relative paths is a compiled-in constant in AllFiles with zero user-controlled input reaching the file-path construction (see 04-RESEARCH.md V5 Input Validation note)."
---

<objective>
Close DOWNLOADS-01's integrity gap: author a real, complete SHA256 manifest for all 147 PostInstall assets (D-08) and wire it into `PostInstallService.DownloadFileAsync` as a hard verify-or-reject gate (D-07) — a capability the predecessor never had. Independent of the Downloads page UI/catalog work in Plans 04-01/04-03; this plan only touches `PostInstallService.cs` and its own new manifest/resource/test files.

Purpose: A tampered or corrupted PostInstall asset must never be silently accepted as "successfully mirrored" by an elevated, system-modifying tool — this is this app's highest-severity supply-chain risk per the security contribution note.
Output: `PostInstallManifest.json` (147 real pinned hashes) embedded and consumed by a hardened `DownloadFileAsync`/`EnsurePostInstallAsync`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-downloads-misc/04-CONTEXT.md
@.planning/phases/04-downloads-misc/04-RESEARCH.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Author the real 147-entry SHA256 manifest and wire the D-07 verify-or-reject gate</name>
  <files>src/AkariToolbox.App/Services/PostInstallService.cs, src/AkariToolbox.App/Resources/PostInstallManifest.json, src/AkariToolbox.App/AkariToolbox.App.csproj</files>
  <read_first>
    src/AkariToolbox.App/Services/PostInstallService.cs (the full 147-entry `AllFiles` array at lines 32-181, the existing `DownloadFileAsync`/`EnsurePostInstallAsync`/`VerifyFileSha256Async` methods to modify/consume, and `RawBase`'s URL-escaping convention: `RawBase + string.Join("/", relativePath.Split('/').Select(Uri.EscapeDataString))`)
    src/AkariToolbox.App/AssemblyInfo.cs (the existing `InternalsVisibleTo("AkariToolbox.Tests")` — reuse this, do not add a second `InternalsVisibleTo` line)
    src/AkariToolbox.App/AkariToolbox.App.csproj (the existing `&lt;EmbeddedResource Include="Defender\NoDefender.cab" /&gt;`-style entries — same pattern for the new manifest)
  </read_first>
  <action>
    First, add a new `internal static IReadOnlyList&lt;string&gt; RelativeFilePaths =&gt; AllFiles;` property on `PostInstallService` (a test seam, matching the `InternalsVisibleTo`-enabled pattern already used elsewhere in this project) — this becomes the single source of truth for both the manifest-authoring step below and Task 2's completeness test, so the manifest can never silently drift out of sync with `AllFiles`.

    Author `Resources/PostInstallManifest.json` as a flat JSON object mapping each of the 147 `AllFiles` relative-path strings (exact `/`-separated form, e.g. `"AntiCheat/Disable DEP NX (default).bat"`) to its real lowercase-hex SHA256 digest. Produce this by actually downloading every file from `https://raw.githubusercontent.com/isleap9/PostInstall/main/PostInstall/{escaped-path}` (same per-segment `Uri.EscapeDataString` escaping `EnsurePostInstallAsync` already uses) into a scratch temp directory, computing `Convert.ToHexStringLower(SHA256.HashData(bytes))` for each, and writing the resulting `{relativePath: hexDigest}` map as pretty-printed JSON. Do this as a one-off local script/program (not shipped in the app) — e.g. a short top-level C# file run via `dotnet run`, or a PowerShell loop using `Invoke-WebRequest`/`Get-FileHash -Algorithm SHA256` — driven by the exact same 147-entry list as `AllFiles` (copy it verbatim from `PostInstallService.cs` lines 32-181 into the authoring script so the two lists cannot silently diverge; Task 2's completeness test independently re-verifies the key sets match). This is a real ~30 MB network operation across 147 files — budget real time for it, do not fabricate any digest.

    In `AkariToolbox.App.csproj`, add `&lt;EmbeddedResource Include="Resources\PostInstallManifest.json" /&gt;` to the existing embedded-resource `ItemGroup` pattern, with a one-line comment explaining it is the D-07/D-08 SHA256 pinning manifest.

    Modify `PostInstallService`: add a private method that loads the manifest once (lazy `Dictionary&lt;string,string&gt;` field, populated from `Assembly.GetManifestResourceStream` + `System.Text.Json.JsonSerializer.Deserialize`, resolved the same way `ScriptRunner.FindEmbeddedResource` locates resources — by manifest-resource-name suffix match on `"PostInstallManifest.json"`). Change `DownloadFileAsync`'s signature to accept an `expectedSha256` parameter and, immediately after `await File.WriteAllBytesAsync(destPath, bytes)`, call `await VerifyFileSha256Async(destPath, expectedSha256)`; on mismatch, log `"[POSTINSTALL] Integrity check FAILED for {label} — deleting corrupted/tampered file."`, delete the file, and return `false` instead of `true` (this is the exact block already drafted in 04-RESEARCH.md's Pattern 2 — implement it verbatim). In `EnsurePostInstallAsync`'s loop, look up `relativePath` in the loaded manifest dictionary before calling `DownloadFileAsync`; if the path is missing from the manifest (should never happen once authoring is complete and correct), log it and count it as `failed` rather than calling `DownloadFileAsync` with a null/empty expected hash.
  </action>
  <reversibility rating="reversible">D-07 (adding SHA256 verification) — the verification call site can be removed later without touching the download/mirror logic itself, per 04-CONTEXT.md.</reversibility>
  <verify>
    <automated>dotnet build src/AkariToolbox.App/AkariToolbox.App.csproj -c Debug -p:Platform=x64</automated>
  </verify>
  <acceptance_criteria>
    - `Resources/PostInstallManifest.json` exists, parses as valid JSON, and contains exactly 147 keys.
    - Every value in the manifest matches the regex `^[0-9a-f]{64}$` (lowercase hex SHA256).
    - `dotnet build` succeeds with the new `EmbeddedResource` line present.
    - `DownloadFileAsync` deletes the file and returns `false` on a hash mismatch (verified by Task 2's test).
  </acceptance_criteria>
  <done>A real, complete, 147-entry SHA256 manifest is embedded and consumed by a hardened download gate that rejects and deletes any file whose bytes do not match its pinned hash.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Completeness, format, and gate-behavior regression tests</name>
  <files>src/AkariToolbox.Tests/PostInstallIntegrityTests.cs</files>
  <read_first>src/AkariToolbox.Tests/PostInstallIntegrityTests.cs (existing `VerifyFileSha256Async` tests and `FakeHttpClientFactory` to extend, not replace)</read_first>
  <behavior>
    - Test: the embedded `PostInstallManifest.json`'s key set, compared via `.OrderBy(...)`, is exactly equal to `PostInstallService.RelativeFilePaths.OrderBy(...)` — no extra keys, no missing keys.
    - Test: every value in the parsed manifest matches `^[0-9a-f]{64}$`.
    - Test: a temp file whose bytes hash to a manifest-style value that matches the expected digest passes `VerifyFileSha256Async` (already covered by the existing matching-hash test — extend only if the manifest-loading path needs its own coverage).
    - Test: a temp file with mismatched content fails `VerifyFileSha256Async` and (at the `DownloadFileAsync`-adjacent level, if directly testable via a seam) results in the file being deleted rather than left in place.
  </behavior>
  <action>
    Add new `[Fact]` methods to the existing `PostInstallIntegrityTests` class: a manifest-completeness test (parses the embedded resource via `typeof(PostInstallService).Assembly.GetManifestResourceStream` the same way `DebloatCatalogTests` locates `.ps1` resources, deserializes to `Dictionary&lt;string,string&gt;`, and asserts key-set equality against `PostInstallService.RelativeFilePaths`), and a hex-format `[Theory]`/`Assert.All` test over every manifest value. Do not remove or modify the three existing `VerifyFileSha256Async_*` tests.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj -c Debug -p:Platform=x64 --filter "FullyQualifiedName~PostInstallIntegrityTests"</automated>
  </verify>
  <acceptance_criteria>
    - All `PostInstallIntegrityTests` facts pass, including the 2 new ones.
    - The completeness test fails loudly if a future edit adds/removes an `AllFiles` entry without updating the manifest.
  </acceptance_criteria>
  <done>Manifest completeness and format are locked by automated tests that will catch any future drift between AllFiles and the pinned-hash manifest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App -> GitHub raw content | HTTPS GET against a pinned, compiled-in repo path per file — no user input in any URL segment |
| App -> local filesystem (`C:\PostInstall\`) | Elevated process writing files whose paths are entirely compiled-in constants |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | `PostInstallService.DownloadFileAsync` | high | mitigate | This plan's core deliverable: SHA256 verify-or-reject gate against a real, pinned 147-entry manifest, closing the mirror's only integrity gap |
| T-04-06 | Tampering | `PostInstallManifest.json` itself, if later hand-edited incorrectly | medium | accept | The manifest is authored once this session from real downloaded bytes and embedded as an assembly resource (same trust tier as the 28 already-accepted Debloat script resources) — Task 2's completeness test catches accidental key drift, not a maliciously re-signed manifest, which is out of scope for a single-developer-authored asset |
| T-04-07 | Elevation of Privilege | Any future consumer that might auto-execute a mirrored PostInstall file | low | accept | This plan and Plan 04-01 only download+verify+mirror; nothing in DOWNLOADS-01's scope auto-executes a mirrored file — execution is out of scope until a future phase explicitly wires a call site |
</threat_model>

<verification>
`dotnet build` succeeds; `dotnet test --filter PostInstallIntegrityTests` passes with 5 total facts (3 existing + 2 new).
</verification>

<success_criteria>
Every PostInstall asset is verified against a real pinned SHA256 before being treated as successfully mirrored; a corrupted/tampered download is deleted and counted as failed, never left in place.
</success_criteria>

<output>
Create `.planning/phases/04-downloads-misc/04-02-SUMMARY.md` when done.

## Artifacts this phase produces (this plan's contribution)
- `Resources/PostInstallManifest.json` (embedded resource, 147 relative-path -> SHA256 hex digest entries)
- `PostInstallService.RelativeFilePaths` (new internal test seam)
- Hardened `PostInstallService.DownloadFileAsync(url, destPath, label, expectedSha256)` (new parameter, verify-or-reject gate)
</output>
