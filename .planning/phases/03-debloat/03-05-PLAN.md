---
phase: 03-debloat
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - src/AkariToolbox.App/AkariToolbox.App.csproj
  - src/AkariToolbox.App/Resources/DebloatScripts/diskcleanup.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/tempfiles.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/removeonedrive.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/removeonedrive-undo.ps1
  - src/AkariToolbox.Tests/DebloatCatalogTests.cs
autonomous: true
requirements: [DEBLOAT-01, DEBLOAT-02]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "'Disk Cleanup — Run' and 'Temporary Files — Remove' are fully functional Run-only actions (no Undo — matches DebloatCatalog's UndoResourceSuffix: null for both)."
    - "'OneDrive — Remove' is a fully functional Run+Undo pair, and its Run path prompts for confirmation first (D-11 — RequiresConfirmation=true, already wired in 03-01-PLAN.md's catalog) since removing OneDrive risks stranding cloud-only Files On-Demand placeholders."
  artifacts:
    - path: "src/AkariToolbox.App/Resources/DebloatScripts/removeonedrive.ps1"
      provides: "The Cleanup category's third D-11-confirmation-gated action to get a real embedded script"
  key_links:
    - from: "src/AkariToolbox.App/AkariToolbox.App.csproj"
      to: "src/AkariToolbox.App/Resources/DebloatScripts/*.ps1"
      via: "EmbeddedResource Include entries"
      pattern: "EmbeddedResource Include"
---

<objective>
Embed the 3 Cleanup-category actions that are plain direct carries (no extraction complexity): Disk Cleanup, Temporary Files, and OneDrive Remove. The other 3 Cleanup actions (Unwanted Apps — Remove, Microsoft Edge — Debloat, Microsoft Edge — Remove) are D-03/D-06/D-08 script replacements requiring careful branch extraction and are deliberately deferred to a dedicated plan (03-06) rather than mixed into this mechanical batch.

Purpose: dispatch the "easy half" of Cleanup before the complex extraction work.
Output: 4 new embedded `.ps1` resources; `DebloatCatalogTests` extended for these 3 actions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-debloat/03-RESEARCH.md
@src/AkariToolbox.App/Services/DebloatCatalog.cs
@src/AkariToolbox.App/AkariToolbox.App.csproj
@src/AkariToolbox.Tests/DebloatCatalogTests.cs

# Predecessor source scripts to port byte-for-byte
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/DiskCleanup.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/TempFiles.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/RemoveOneDrive.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/RemoveOneDrive-Undo.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embed Disk Cleanup, Temporary Files, and OneDrive Remove scripts</name>
  <files>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Resources/DebloatScripts/diskcleanup.ps1, src/AkariToolbox.App/Resources/DebloatScripts/tempfiles.ps1, src/AkariToolbox.App/Resources/DebloatScripts/removeonedrive.ps1, src/AkariToolbox.App/Resources/DebloatScripts/removeonedrive-undo.ps1</files>
  <read_first>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Services/DebloatCatalog.cs, C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/RemoveOneDrive.ps1</read_first>
  <action>
    Read each of the 4 predecessor source files first and confirm they are already non-interactive (no self-elevation, no menu loop). Then create the matching embedded resource files under `src/AkariToolbox.App/Resources/DebloatScripts/` with exact byte-for-byte content: `DiskCleanup.ps1` → `diskcleanup.ps1` (no Undo, matches `DebloatCatalog.cs`'s `UndoResourceSuffix: null` for "diskcleanup"); `TempFiles.ps1` → `tempfiles.ps1` (no Undo, matches "tempfiles"'s `UndoResourceSuffix: null`); `RemoveOneDrive.ps1`/`RemoveOneDrive-Undo.ps1` → `removeonedrive.ps1`/`removeonedrive-undo.ps1`.

    In `AkariToolbox.App.csproj`, append 4 new `<EmbeddedResource Include="Resources\DebloatScripts\...\.ps1" />` lines to the existing Debloat `<ItemGroup>`.
  </action>
  <verify>
    <automated>cd src && dotnet build AkariToolbox.sln -c Debug</automated>
  </verify>
  <done>All 4 new files exist and are embedded; solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the 3 direct-carry Cleanup actions' resource resolution</name>
  <files>src/AkariToolbox.Tests/DebloatCatalogTests.cs</files>
  <read_first>src/AkariToolbox.Tests/DebloatCatalogTests.cs</read_first>
  <behavior>
    - Cleanup_direct_carry_actions_have_resolvable_resources: for the 3 actions with keys "diskcleanup", "tempfiles", "removeonedrive", `RunResourceSuffix` resolves in the assembly manifest for all 3, and `UndoResourceSuffix` resolves for "removeonedrive" while being `null` for "diskcleanup" and "tempfiles".
  </behavior>
  <action>
    Add one new `[Fact]` named `Cleanup_direct_carry_actions_have_resolvable_resources` to the existing `src/AkariToolbox.Tests/DebloatCatalogTests.cs`, asserting exactly the shape described in this task's `<behavior>` block — do not assert on the 3 remaining Cleanup actions ("bloatware", "edgesettings", "edgewebview") in this fact; those are covered by 03-06-PLAN.md's own dedicated test, since their scripts are not embedded yet.
  </action>
  <verify>
    <automated>cd src && dotnet test AkariToolbox.Tests --filter "FullyQualifiedName~DebloatCatalogTests"</automated>
  </verify>
  <done>New fact passes; full DebloatCatalogTests suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — extends 03-01-PLAN.md's threat model.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Tampering | 3 newly embedded direct-carry scripts | low | accept | Byte-for-byte ports of predecessor production scripts, same reasoning as T-03-05/T-03-06/T-03-08 |
| T-03-10 | Repudiation (accidental data loss) | OneDrive — Remove now has a real embedded script behind its D-11 confirmation gate | medium | mitigate | Confirmation gate (already implemented, 03-01-PLAN.md) surfaces before `OneDriveSetup.exe -uninstall` runs, giving the user a chance to confirm any cloud-only Files On-Demand placeholders are synced first; the dialog text itself does not enumerate unsynced files (out of scope — the predecessor never did this either) |

</threat_model>

<verification>
1. `dotnet build AkariToolbox.sln -c Debug` succeeds.
2. `dotnet test AkariToolbox.Tests --filter "FullyQualifiedName~DebloatCatalogTests"` — full suite green.
3. Manual smoke check: run Disk Cleanup and Temp Files (Run-only, confirm no Undo button renders); run OneDrive Remove and confirm the confirmation dialog appears before anything executes, then confirm Undo (reinstall) works.
</verification>

<success_criteria>
3 of Cleanup's 6 actions are fully functional (25 of 28 actions phase-wide); only the 3 D-03/D-06/D-08 replacement actions remain, handled in 03-06.
</success_criteria>

## Artifacts this phase produces

- `src/AkariToolbox.App/Resources/DebloatScripts/{diskcleanup,tempfiles,removeonedrive,removeonedrive-undo}.ps1` — 4 new embedded resource files
- `AkariToolbox.Tests.DebloatCatalogTests.Cleanup_direct_carry_actions_have_resolvable_resources` (new test method)

<output>
Create `.planning/phases/03-debloat/03-05-SUMMARY.md` when done
</output>
