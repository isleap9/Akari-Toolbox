---
phase: 03-debloat
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/AkariToolbox.App/AkariToolbox.App.csproj
  - src/AkariToolbox.App/Resources/DebloatScripts/visualeffects.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/visualeffects-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/services.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/services-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/deliveryoptimization.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/deliveryoptimization-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/disablebitlocker.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/disablebitlocker-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/hibernation.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/hibernation-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/storagesense.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/storagesense-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/wpbt.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/wpbt-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/utc.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/utc-undo.ps1
  - src/AkariToolbox.Tests/DebloatCatalogTests.cs
autonomous: true
requirements: [DEBLOAT-01, DEBLOAT-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 8 System & Performance actions (Visual Effects, Services, Delivery Optimization, BitLocker, Hibernation, Storage Sense, WPBT, Set Time to UTC) are fully functional Run+Undo pairs. (BitLocker's Undo, per D-13, opens the BitLocker Control Panel for manual re-enable rather than actively re-encrypting the drive — this is the accepted, as-authored behavior of the new source script, not a bug.)"
    - "Clicking Run on 'BitLocker — Disable' or 'Hibernation — Disable' shows the IDialogService confirmation prompt first (D-11) — this is unchanged catalog data from 03-01, this plan only proves it against real, now-embedded scripts instead of a FileNotFoundException short-circuit."
  artifacts:
    - path: "src/AkariToolbox.App/Resources/DebloatScripts/disablebitlocker.ps1"
      provides: "The first D-11-confirmation-gated action with a real, embedded script behind it — sourced via D-12/D-13 branch-extraction from the 'Ultimate' collection's `1 BitLocker.ps1`, not a predecessor byte-for-byte carry"
  key_links:
    - from: "src/AkariToolbox.App/AkariToolbox.App.csproj"
      to: "src/AkariToolbox.App/Resources/DebloatScripts/*.ps1"
      via: "EmbeddedResource Include entries"
      pattern: "EmbeddedResource Include"
---

<objective>
Complete the System & Performance category (DEBLOAT-01) by embedding its 8 actions' scripts — Visual Effects, Services, Delivery Optimization, BitLocker, Hibernation, Storage Sense, WPBT, Set Time to UTC. Seven of the eight (Visual Effects, Services, Delivery Optimization, Hibernation, Storage Sense, WPBT, Set Time to UTC) are direct, byte-for-byte carries from the predecessor. The eighth, BitLocker — Disable, is branch-extracted from the "Ultimate" collection's `1 BitLocker.ps1` (D-12/D-13, added mid-planning 2026-09-01) rather than carried from the predecessor's `DisableBitLocker.ps1`/`DisableBitLocker-Undo.ps1` — its Undo branch ("BitLocker: On") only opens the BitLocker Control Panel for manual re-enable; it does NOT call `Enable-BitLocker` the way the predecessor's active Undo script did, an explicit, accepted scope reduction on Undo's capability (D-13). This is the first plan to give real backing scripts to two of the 5 D-11-confirmation-gated actions (BitLocker, Hibernation) — their `RequiresConfirmation=true` catalog data and the `IDialogService.ConfirmAsync` gate already exist and are already tested (03-01-PLAN.md Task 2); this plan only proves the confirmation flow against real scripts rather than a not-yet-embedded resource.

Purpose: close out the second of 5 categories.
Output: 16 new embedded `.ps1` resources; `DebloatCatalogTests` extended to lock this category's resource resolution.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-debloat/03-RESEARCH.md
@src/AkariToolbox.App/Services/DebloatCatalog.cs
@src/AkariToolbox.App/AkariToolbox.App.csproj
@src/AkariToolbox.Tests/DebloatCatalogTests.cs

# Predecessor source scripts to port byte-for-byte (7 direct carries)
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/VisualEffects.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/VisualEffects-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/Services.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/Services-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/DeliveryOptimization.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/DeliveryOptimization-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/Hibernation.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/Hibernation-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/StorageSense.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/StorageSense-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/WPBT.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/WPBT-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/UTC.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/UTC-Undo.ps1

# New replacement source for BitLocker — Disable (D-12/D-13, branch-extracted, not a direct carry)
@C:/Users/isleap/Desktop/AkariOS Tweaks/3 Setup/1 BitLocker.ps1
</context>

<!-- planner-discipline-allow: Pause -->
<!-- The literal is the exact source-script token Task 2 instructs the executor to strip
     (the blocking-input call before each branch's exit) and Task 2's own <verify> greps
     for its absence, per the Comment-Text Discipline allowlist escape hatch
     (gsd-core/references/planner-antipatterns.md #429). -->

<tasks>

<task type="auto">
  <name>Task 1: Embed the 7 remaining direct-carry System &amp; Performance scripts</name>
  <files>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Resources/DebloatScripts/visualeffects.ps1, src/AkariToolbox.App/Resources/DebloatScripts/visualeffects-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/services.ps1, src/AkariToolbox.App/Resources/DebloatScripts/services-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/deliveryoptimization.ps1, src/AkariToolbox.App/Resources/DebloatScripts/deliveryoptimization-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/hibernation.ps1, src/AkariToolbox.App/Resources/DebloatScripts/hibernation-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/storagesense.ps1, src/AkariToolbox.App/Resources/DebloatScripts/storagesense-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/wpbt.ps1, src/AkariToolbox.App/Resources/DebloatScripts/wpbt-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/utc.ps1, src/AkariToolbox.App/Resources/DebloatScripts/utc-undo.ps1</files>
  <read_first>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Services/DebloatCatalog.cs, C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/VisualEffects.ps1</read_first>
  <action>
    For each of these 7 source-file pairs, read the predecessor file first and confirm it is already non-interactive (no self-elevation block, no menu loop — expected for all 7, unlike this plan's separate BitLocker task). Then create the matching embedded resource file under `src/AkariToolbox.App/Resources/DebloatScripts/` with exact byte-for-byte content: `VisualEffects.ps1`/`VisualEffects-Undo.ps1` → `visualeffects.ps1`/`visualeffects-undo.ps1`; `Services.ps1`/`Services-Undo.ps1` → `services.ps1`/`services-undo.ps1`; `DeliveryOptimization.ps1`/`DeliveryOptimization-Undo.ps1` → `deliveryoptimization.ps1`/`deliveryoptimization-undo.ps1`; `Hibernation.ps1`/`Hibernation-Undo.ps1` → `hibernation.ps1`/`hibernation-undo.ps1`; `StorageSense.ps1`/`StorageSense-Undo.ps1` → `storagesense.ps1`/`storagesense-undo.ps1`; `WPBT.ps1`/`WPBT-Undo.ps1` → `wpbt.ps1`/`wpbt-undo.ps1`; `UTC.ps1`/`UTC-Undo.ps1` → `utc.ps1`/`utc-undo.ps1`. These filenames exactly match the `RunResourceSuffix`/`UndoResourceSuffix` values already present in `DebloatCatalog.cs`.

    In `AkariToolbox.App.csproj`, append 14 new `<EmbeddedResource Include="Resources\DebloatScripts\...\.ps1" />` lines to the existing Debloat `<ItemGroup>` (do not create a new one), one per file above. BitLocker's two resources are added separately in this plan's Task 2.
  </action>
  <verify>
    <automated>cd src && dotnet build AkariToolbox.sln -c Debug</automated>
  </verify>
  <done>All 14 new files exist and are embedded; solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract "BitLocker — Disable" branches from 1 BitLocker.ps1 (D-12/D-13)</name>
  <files>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Resources/DebloatScripts/disablebitlocker.ps1, src/AkariToolbox.App/Resources/DebloatScripts/disablebitlocker-undo.ps1</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/3 Setup/1 BitLocker.ps1</read_first>
  <action>
    Read `1 BitLocker.ps1` in full first (added mid-planning per D-12/D-13 — replaces the predecessor's `DisableBitLocker.ps1`/`DisableBitLocker-Undo.ps1` pair, no longer used as a source). Like the Cleanup-category replacements extracted in 03-06-PLAN.md, this source is a self-elevating console menu — drop the `# SCRIPT RUN AS ADMIN` self-elevation block and the `$Host.UI.RawUI...`/`Clear-Host` console-cosmetic lines that precede the menu, and drop the `Write-Host "1. BitLocker..."` menu prompt/input loop itself; copy only the chosen branch's body. Unlike 03-06's 3 scripts, this source has no internet-connectivity pre-flight to strip, but each branch calls `Pause` immediately before its own `exit` — `Pause` internally waits on user input, which hangs forever against `IScriptRunner`'s non-redirected-stdin child process (the same hang class as 03-06's Pitfall 2) — drop that call in both branches, keeping the `exit` line that follows it.

    Build `disablebitlocker.ps1`: branch 1's ("BitLocker: Off") full body verbatim — from its `Clear-Host` line (disabling BitLocker on every volume via `Disable-BitLocker`, then opening the BitLocker Control Panel via `control.exe` and printing `manage-bde -status`) through the `manage-bde -status` line, omitting the following blocking-input call, ending in a bare `exit` in its place.

    Build `disablebitlocker-undo.ps1` the same way using branch 2's ("BitLocker: On") full body verbatim — from its `Clear-Host` line through `manage-bde -status`, same blocking-input line dropped, ending in a bare `exit`. Per D-13, this branch ONLY opens the BitLocker Control Panel and prints status — it does not call `Enable-BitLocker` or otherwise re-encrypt anything; do not add any active re-enable logic that isn't present in the source branch.

    These two output filenames stay `disablebitlocker.ps1`/`disablebitlocker-undo.ps1`, exactly matching the `RunResourceSuffix`/`UndoResourceSuffix` values already present in `DebloatCatalog.cs` (03-01-PLAN.md) and its `RequiresConfirmation=true` classification (D-11, unchanged) — only the source content changes, per this revision.

    In `AkariToolbox.App.csproj`, append `<EmbeddedResource Include="Resources\DebloatScripts\disablebitlocker.ps1" />` and `<EmbeddedResource Include="Resources\DebloatScripts\disablebitlocker-undo.ps1" />` to the existing Debloat `<ItemGroup>` (the same one this plan's Task 1 and 03-01-PLAN.md's Task 1 populate), with an inline comment noting this is a D-12/D-13 branch-extracted replacement (not a direct carry, unlike this plan's other 7 scripts).
  </action>
  <verify>
    <automated>cd src && dotnet build AkariToolbox.sln -c Debug && for f in src/AkariToolbox.App/Resources/DebloatScripts/disablebitlocker.ps1 src/AkariToolbox.App/Resources/DebloatScripts/disablebitlocker-undo.ps1; do grep -v '^#' "$f" | grep -c "^Pause$"; done | grep -v '^0$' | wc -l</automated>
  </verify>
  <done>Both files exist, are embedded, contain zero occurrences of a bare "Pause" statement (confirming the blocking-input call was stripped), both end in "exit"; `disablebitlocker-undo.ps1` contains no `Enable-BitLocker` call (confirms D-13's settings-panel-only Undo, no active re-encryption); solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 3: Lock System &amp; Performance resource resolution</name>
  <files>src/AkariToolbox.Tests/DebloatCatalogTests.cs</files>
  <read_first>src/AkariToolbox.Tests/DebloatCatalogTests.cs</read_first>
  <behavior>
    - SystemPerformance_category_all_actions_have_resolvable_resources: for every `DebloatAction` in `new DebloatCatalog().Actions` where `Category == "System & Performance"` (8 actions), both `RunResourceSuffix` and `UndoResourceSuffix` resolve in the assembly manifest.
  </behavior>
  <action>
    Add one new `[Fact]` named `SystemPerformance_category_all_actions_have_resolvable_resources` to the existing `src/AkariToolbox.Tests/DebloatCatalogTests.cs`, following the exact pattern `PrivacyTelemetry_category_all_actions_have_resolvable_resources` (03-02-PLAN.md) already established, filtering on `Category == "System & Performance"`.
  </action>
  <verify>
    <automated>cd src && dotnet test AkariToolbox.Tests --filter "FullyQualifiedName~DebloatCatalogTests"</automated>
  </verify>
  <done>New fact passes for all 8 System & Performance actions; full DebloatCatalogTests suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — extends 03-01-PLAN.md's threat model.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | 7 newly embedded direct-carry scripts (Visual Effects, Services, Delivery Optimization, Hibernation, Storage Sense, WPBT, Set Time to UTC) | low | accept | Byte-for-byte ports of predecessor production scripts, same reasoning as T-03-05 |
| T-03-07 | Elevation of Privilege (accidental) | BitLocker/Hibernation now have real embedded scripts behind their D-11 confirmation gate | high | mitigate | Confirmation gate already implemented and tested in 03-01-PLAN.md — this plan is the first to exercise it against a real, non-FileNotFoundException-short-circuited script; manual smoke verification confirms the dialog actually appears before `Disable-BitLocker`/hibernation removal runs |
| T-03-17 | Information Disclosure | `disablebitlocker-undo.ps1` (D-12/D-13 branch-extracted from `1 BitLocker.ps1` branch 2 — Control-Panel-only, no active re-encryption) | medium | accept | D-13 explicit, already-made user decision: a user who clicks "BitLocker — Disable" Undo expecting active re-encryption (the predecessor's prior behavior) only gets the BitLocker Control Panel opened for manual re-enable — the drive stays unencrypted until the user manually re-enables it, a real, accepted capability gap versus the predecessor, not a bug; no code mitigation compensates for this, per the user's informed decision |
| T-03-18 | Denial of Service (accidental, hang risk) | `Pause` calls in `1 BitLocker.ps1`'s both branches | medium | mitigate | Task 2's extraction strips the blocking `Pause` call from both branches (the same hang class as 03-06's Pitfall 2) and its automated `<verify>` greps for zero remaining occurrences — a genuinely new mitigation this task adds |

</threat_model>

<verification>
1. `dotnet build AkariToolbox.sln -c Debug` succeeds.
2. `dotnet test AkariToolbox.Tests --filter "FullyQualifiedName~DebloatCatalogTests"` — full suite green.
3. Manual smoke check: click "BitLocker — Disable" Run and confirm the confirmation dialog appears before anything runs; cancel it and confirm no script executes (no log line beyond the confirmation cancel). Repeat for "Hibernation — Disable".
4. Manual smoke check: confirm "BitLocker — Disable" Run's script disables BitLocker and opens the BitLocker Control Panel; confirm its Undo opens the BitLocker Control Panel and prints `manage-bde -status` but does NOT silently re-encrypt the drive (per D-13 — this is expected, not a bug).
</verification>

<success_criteria>
System & Performance category is 100% functional (8/8 actions); D-11 confirmation gate proven against a real script for the first time.
</success_criteria>

## Artifacts this phase produces

- `src/AkariToolbox.App/Resources/DebloatScripts/{visualeffects,services,deliveryoptimization,hibernation,storagesense,wpbt,utc}.ps1` (+ `-undo` counterparts) — 14 new direct-carry embedded resource files
- `src/AkariToolbox.App/Resources/DebloatScripts/disablebitlocker.ps1`, `disablebitlocker-undo.ps1` — 2 new D-12/D-13 branch-extracted embedded resource files (sourced from `1 BitLocker.ps1`, not the predecessor)
- `AkariToolbox.Tests.DebloatCatalogTests.SystemPerformance_category_all_actions_have_resolvable_resources` (new test method)

<output>
Create `.planning/phases/03-debloat/03-03-SUMMARY.md` when done
</output>
