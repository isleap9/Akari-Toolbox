---
phase: 03-debloat
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/AkariToolbox.App/AkariToolbox.App.csproj
  - src/AkariToolbox.App/Resources/DebloatScripts/activityhistory.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/activityhistory-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/locationtracking.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/locationtracking-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/ps7telemetry.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/ps7telemetry-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/windowsai.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/windowsai-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/consumerfeatures.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/consumerfeatures-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/disablebgapps.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/disablebgapps-undo.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/storesearch.ps1
  - src/AkariToolbox.App/Resources/DebloatScripts/storesearch-undo.ps1
  - src/AkariToolbox.Tests/DebloatCatalogTests.cs
autonomous: true
requirements: [DEBLOAT-01, DEBLOAT-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 8 Privacy & Telemetry actions (Telemetry, Activity History, Location Tracking, PS7 Telemetry, Windows AI, Consumer Features, Background Apps, Store Search) are fully functional Run+Undo pairs — clicking any of them streams real script output and completes without a FileNotFoundException."
    - "6 of the 7 newly embedded scripts (Activity History, Location Tracking, PS7 Telemetry, Consumer Features, Background Apps, Store Search) are byte-for-byte identical to their predecessor source file — a pure port with zero logic changes, per DEBLOAT-01's direct-carry scope. The 7th, Windows AI (windowsai.ps1/windowsai-undo.ps1), is branch-extracted from the 'Ultimate' collection's `9 Copilot.ps1` per D-14 (added mid-planning 2026-09-01) — NOT a predecessor byte-for-byte carry — with its self-elevation/menu scaffolding stripped and only the chosen branch's body copied verbatim."
  artifacts:
    - path: "src/AkariToolbox.App/Resources/DebloatScripts/activityhistory.ps1"
      provides: "One of 7 newly embedded direct-carry Privacy & Telemetry Run scripts completing the category"
  key_links:
    - from: "src/AkariToolbox.App/AkariToolbox.App.csproj"
      to: "src/AkariToolbox.App/Resources/DebloatScripts/*.ps1"
      via: "EmbeddedResource Include entries, resolved at runtime by IScriptRunner.RunEmbeddedScriptAsync's EndsWith(resourceSuffix) manifest-name search"
      pattern: "EmbeddedResource Include"
---

<objective>
Complete the Privacy & Telemetry category (DEBLOAT-01) by embedding the 7 remaining actions' scripts — Activity History, Location Tracking, PS7 Telemetry, Windows AI, Consumer Features, Background Apps, Store Search. Six of the seven (Activity History, Location Tracking, PS7 Telemetry, Consumer Features, Background Apps, Store Search) are direct, byte-for-byte carries from the predecessor. The seventh, Windows AI — Disable, is branch-extracted from the "Ultimate" collection's `9 Copilot.ps1` (D-14, added mid-planning 2026-09-01) rather than carried from the predecessor's `WindowsAI.ps1`/`WindowsAI-Undo.ps1` — the new script is Copilot-only in scope (it does not touch Recall, `WSAIFabricSvc`, or Notepad AI, unlike the predecessor's broader script), an explicit, accepted scope reduction. The catalog metadata for all 7 already exists (03-01-PLAN.md's `DebloatCatalog`, windowsai's Description column already updated for the narrower scope); this plan only adds the missing `.ps1` resources and their `<EmbeddedResource>` entries, so no ViewModel/catalog C# code changes beyond the extended test file.

Purpose: close out the first of 5 categories so the Debloat page's Privacy & Telemetry group is 100% functional, not just correctly labeled.
Output: 14 new embedded `.ps1` resources; `DebloatCatalogTests` extended to lock this category's resource resolution.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-debloat/03-RESEARCH.md
@src/AkariToolbox.App/Services/DebloatCatalog.cs
@src/AkariToolbox.App/AkariToolbox.App.csproj
@src/AkariToolbox.Tests/DebloatCatalogTests.cs

# Predecessor source scripts to port byte-for-byte (6 direct carries)
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/ActivityHistory.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/ActivityHistory-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/LocationTracking.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/LocationTracking-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/PS7Telemetry.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/PS7Telemetry-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/ConsumerFeatures.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/ConsumerFeatures-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/DisableBGApps.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/DisableBGApps-Undo.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/StoreSearch.ps1
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/StoreSearch-Undo.ps1

# New replacement source for Windows AI — Disable (D-14, branch-extracted, not a direct carry)
@C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/9 Copilot.ps1
</context>

<!-- planner-discipline-allow: Read-Host -->
<!-- The literal is the exact source-script token Task 2 instructs the executor to strip
     (the menu/self-elevation loop) and Task 2's own <verify> greps for its absence, per
     the Comment-Text Discipline allowlist escape hatch
     (gsd-core/references/planner-antipatterns.md #429). -->

<tasks>

<task type="auto">
  <name>Task 1: Embed the 6 remaining direct-carry Privacy & Telemetry scripts</name>
  <files>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Resources/DebloatScripts/activityhistory.ps1, src/AkariToolbox.App/Resources/DebloatScripts/activityhistory-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/locationtracking.ps1, src/AkariToolbox.App/Resources/DebloatScripts/locationtracking-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/ps7telemetry.ps1, src/AkariToolbox.App/Resources/DebloatScripts/ps7telemetry-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/consumerfeatures.ps1, src/AkariToolbox.App/Resources/DebloatScripts/consumerfeatures-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/disablebgapps.ps1, src/AkariToolbox.App/Resources/DebloatScripts/disablebgapps-undo.ps1, src/AkariToolbox.App/Resources/DebloatScripts/storesearch.ps1, src/AkariToolbox.App/Resources/DebloatScripts/storesearch-undo.ps1</files>
  <read_first>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Services/DebloatCatalog.cs, C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Scripts/ActivityHistory.ps1</read_first>
  <action>
    For each of these 6 source-file pairs, read the predecessor file first and confirm it is already non-interactive (no `[Security.Principal.WindowsPrincipal]` self-elevation block, no menu/input loop — expected for all 6, since RESEARCH.md's directory-listing verification already confirmed their existence and the predecessor's Debloat scripts were never written as interactive console menus, unlike the 3 D-03/D-06/D-08 replacement scripts and this plan's separate Windows AI task). Then create the matching embedded resource file under `src/AkariToolbox.App/Resources/DebloatScripts/` with the exact byte-for-byte content (no transformation): `ActivityHistory.ps1`/`ActivityHistory-Undo.ps1` → `activityhistory.ps1`/`activityhistory-undo.ps1`; `LocationTracking.ps1`/`LocationTracking-Undo.ps1` → `locationtracking.ps1`/`locationtracking-undo.ps1`; `PS7Telemetry.ps1`/`PS7Telemetry-Undo.ps1` → `ps7telemetry.ps1`/`ps7telemetry-undo.ps1`; `ConsumerFeatures.ps1`/`ConsumerFeatures-Undo.ps1` → `consumerfeatures.ps1`/`consumerfeatures-undo.ps1`; `DisableBGApps.ps1`/`DisableBGApps-Undo.ps1` → `disablebgapps.ps1`/`disablebgapps-undo.ps1`; `StoreSearch.ps1`/`StoreSearch-Undo.ps1` → `storesearch.ps1`/`storesearch-undo.ps1`. These filenames exactly match the `RunResourceSuffix`/`UndoResourceSuffix` values already present in `DebloatCatalog.cs` from 03-01-PLAN.md — do not rename or alter them.

    In `AkariToolbox.App.csproj`, append 12 new `<EmbeddedResource Include="Resources\DebloatScripts\...\.ps1" />` lines to the existing Debloat `<ItemGroup>` created in 03-01-PLAN.md (do not create a second `<ItemGroup>`), one per file listed above, in the same order as listed. Windows AI's two resources are added separately in this plan's Task 2.
  </action>
  <verify>
    <automated>cd src && dotnet build AkariToolbox.sln -c Debug</automated>
  </verify>
  <done>All 12 new files exist under Resources/DebloatScripts/, all 12 have matching EmbeddedResource entries in the csproj, and the solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract "Windows AI — Disable" branches from 9 Copilot.ps1 (D-14)</name>
  <files>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Resources/DebloatScripts/windowsai.ps1, src/AkariToolbox.App/Resources/DebloatScripts/windowsai-undo.ps1</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/9 Copilot.ps1</read_first>
  <action>
    Read `9 Copilot.ps1` in full first (added mid-planning per D-14 — replaces the predecessor's `WindowsAI.ps1`/`WindowsAI-Undo.ps1` pair, no longer used as a source). Unlike this plan's other 6 direct-carry ports, this script is a self-elevating console menu (same shape as the D-03/D-06/D-08 Cleanup-category replacements extracted in 03-06-PLAN.md) — apply the same extraction technique: drop the `# SCRIPT RUN AS ADMIN` self-elevation block and the `$Host.UI.RawUI...`/`Clear-Host` console-cosmetic lines that precede it, keep the `# SCRIPT SILENT` / `$progresspreference = 'silentlycontinue'` line, then drop the `Write-Host "1. Copilot..."`/`Read-Host` menu-selection loop entirely and copy ONLY the chosen branch's body. This source has neither a `Test-Connection`/`Pause` internet pre-flight (03-06's Pitfall 2) nor a trailing `show-menu` loop-back (03-06's Pitfall 1) — every branch already ends in a bare `exit`, so no ending modification is required.

    Build `windowsai.ps1`: the `$progresspreference = 'silentlycontinue'` line, then branch 1's ("Copilot: Off") full body verbatim — from its `Clear-Host` line (stopping Edge/Copilot-related processes, removing the Copilot AppX package via `Remove-AppxPackage`, writing the `TurnOffWindowsCopilot` registry policy under both `HKCU` and `HKLM`) through its own `exit` line, copied unchanged.

    Build `windowsai-undo.ps1` the same way using branch 2's ("Copilot: Default") full body verbatim — from its `Clear-Host` line (reinstalling the Copilot AppX package via `Add-AppxPackage`, deleting the `TurnOffWindowsCopilot` registry policy under both `HKCU` and `HKLM`) through its own `exit` line. Per D-14, this Undo does NOT restore Recall, `WSAIFabricSvc`, or Notepad AI — it only reverses what branch 1 did (the Copilot package plus the one registry policy) — do not add any transformation beyond copying the branch body verbatim.

    These two output filenames stay `windowsai.ps1`/`windowsai-undo.ps1`, exactly matching the `RunResourceSuffix`/`UndoResourceSuffix` values already present in `DebloatCatalog.cs` (03-01-PLAN.md) — only the source content changes, per this revision.

    In `AkariToolbox.App.csproj`, append `<EmbeddedResource Include="Resources\DebloatScripts\windowsai.ps1" />` and `<EmbeddedResource Include="Resources\DebloatScripts\windowsai-undo.ps1" />` to the existing Debloat `<ItemGroup>` (the same one this plan's Task 1 and 03-01-PLAN.md's Task 1 populate), with an inline comment noting this is a D-14 branch-extracted replacement (not a direct carry, unlike this plan's other 6 scripts).
  </action>
  <verify>
    <automated>cd src && dotnet build AkariToolbox.sln -c Debug && for f in src/AkariToolbox.App/Resources/DebloatScripts/windowsai.ps1 src/AkariToolbox.App/Resources/DebloatScripts/windowsai-undo.ps1; do grep -v '^#' "$f" | grep -c "Read-Host"; done | grep -v '^0$' | wc -l</automated>
  </verify>
  <done>Both files exist, are embedded, contain zero occurrences of a non-comment "Read-Host" (confirming the menu/input loop was stripped), both end in "exit"; solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 3: Lock Privacy & Telemetry resource resolution</name>
  <files>src/AkariToolbox.Tests/DebloatCatalogTests.cs</files>
  <read_first>src/AkariToolbox.Tests/DebloatCatalogTests.cs</read_first>
  <behavior>
    - PrivacyTelemetry_category_all_actions_have_resolvable_resources: for every `DebloatAction` in `new DebloatCatalog().Actions` where `Category == "Privacy & Telemetry"` (8 actions), `typeof(App).Assembly.GetManifestResourceNames()` contains an entry ending with `RunResourceSuffix` and (since all 8 have a non-null `UndoResourceSuffix`) an entry ending with `UndoResourceSuffix`, both case-insensitive.
  </behavior>
  <action>
    Add one new `[Fact]` named `PrivacyTelemetry_category_all_actions_have_resolvable_resources` to the existing `src/AkariToolbox.Tests/DebloatCatalogTests.cs` (created in 03-01-PLAN.md — do not create a second test class), iterating `new DebloatCatalog().Actions.Where(a => a.Category == "Privacy & Telemetry")` and asserting both suffixes resolve via `typeof(AkariToolbox.App.App).Assembly.GetManifestResourceNames().Any(n => n.EndsWith(suffix, StringComparison.OrdinalIgnoreCase))`, same pattern Task 1's own `Telemetry_action_resources_resolve_in_assembly_manifest` fact already established. This covers all 8 actions in the category regardless of whether each one's script is a direct carry (6 of them) or D-14 branch-extracted (windowsai) — the test only checks resource-suffix resolution, not provenance.
  </action>
  <verify>
    <automated>cd src && dotnet test AkariToolbox.Tests --filter "FullyQualifiedName~DebloatCatalogTests"</automated>
  </verify>
  <done>The new fact passes for all 8 Privacy & Telemetry actions; the full DebloatCatalogTests suite (from 03-01 plus this addition) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan only adds static, compiled-in script content already covered by 03-01-PLAN.md's threat model (T-03-01 through T-03-04).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | 6 newly embedded direct-carry scripts (Activity History, Location Tracking, PS7 Telemetry, Consumer Features, Background Apps, Store Search) | low | accept | Byte-for-byte ports of scripts already shipped and used in production by the predecessor app for this exact purpose — no new logic, no new attack surface beyond T-03-01 (already mitigated: fixed resource-suffix strings, no user-input injection path) |
| T-03-16 | Tampering | `windowsai.ps1`/`windowsai-undo.ps1` (D-14 branch-extracted from the "Ultimate" collection's `9 Copilot.ps1`, not a predecessor byte-for-byte carry) | low | accept | Same risk class as the predecessor's `WindowsAI.ps1`/`WindowsAI-Undo.ps1` (AppX package removal + a single registry policy write, no network download) — narrower in scope than the predecessor (Copilot-only vs. also Recall/`WSAIFabricSvc`/Notepad AI per D-14), so the risk surface shrinks, not grows; extraction follows 03-06-PLAN.md's proven menu-stripping technique with no remaining blocking-input call (verified by this plan's Task 2 automated grep) |

</threat_model>

<verification>
1. `dotnet build AkariToolbox.sln -c Debug` succeeds.
2. `dotnet test AkariToolbox.Tests --filter "FullyQualifiedName~DebloatCatalogTests"` — all facts (03-01's 6 plus this plan's 1) pass.
3. Manual smoke check: run each of the 7 newly wired actions' Run and Undo buttons on the Debloat page and confirm real script output streams into the log dock with no error.
</verification>

<success_criteria>
Privacy & Telemetry category is 100% functional (8/8 actions, both Run and Undo working); regression test suite grows to cover it.
</success_criteria>

## Artifacts this phase produces

- `src/AkariToolbox.App/Resources/DebloatScripts/{activityhistory,locationtracking,ps7telemetry,consumerfeatures,disablebgapps,storesearch}.ps1` (+ `-undo` counterparts) — 12 new direct-carry embedded resource files
- `src/AkariToolbox.App/Resources/DebloatScripts/windowsai.ps1`, `windowsai-undo.ps1` — 2 new D-14 branch-extracted embedded resource files (sourced from `9 Copilot.ps1`, not the predecessor)
- `AkariToolbox.Tests.DebloatCatalogTests.PrivacyTelemetry_category_all_actions_have_resolvable_resources` (new test method on the existing test class)

<output>
Create `.planning/phases/03-debloat/03-02-SUMMARY.md` when done
</output>
