---
phase: 03-debloat
plan: 07
type: execute
wave: 7
depends_on: ["03-06"]
files_modified:
  - src/AkariToolbox.App/Models/DebloatActionItem.cs
  - src/AkariToolbox.App/ViewModels/DebloatViewModel.cs
  - src/AkariToolbox.App/Views/DebloatPage.xaml
  - src/AkariToolbox.Tests/DebloatCatalogTests.cs
autonomous: true
requirements: [DEBLOAT-01, DEBLOAT-02, DEBLOAT-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every one of the 28 Debloat actions across all 5 categories is fully functional Run (and Undo where applicable), verified end-to-end by the full DebloatCatalogTests suite plus a new DebloatViewModel-level regression lock — DEBLOAT-01 is now completely satisfied, not partially (as it was across Waves 1-6)."
    - "A row whose action is D-11 risk-classified (RequiresConfirmation=true) or whose Undo downloads an unverified binary (UndoDownloadsUnverifiedBinary=true) shows a visible caption on the Debloat page — a user scanning the list can tell which of the 28 actions carry extra risk without having to click Run first, addressing RESEARCH.md's Pitfall 5 UX recommendation to surface risk clearly."
    - "DebloatPage.xaml.cs still contains zero business logic after this plan's changes — the new per-row caption is pure data-binding (x:Bind against new DebloatActionItem fields), not a code-behind computation (DEBLOAT-03 still holds)."
  artifacts:
    - path: "src/AkariToolbox.App/Models/DebloatActionItem.cs"
      provides: "RequiresConfirmation (already present) plus the new UndoDownloadsUnverifiedBinary field, both driving the page's per-row risk captions"
  key_links:
    - from: "src/AkariToolbox.App/Views/DebloatPage.xaml"
      to: "src/AkariToolbox.App/Models/DebloatActionItem.cs"
      via: "x:Bind caption TextBlock Visibility bound to RequiresConfirmation / UndoDownloadsUnverifiedBinary"
      pattern: "x:Bind RequiresConfirmation"
---

<objective>
Close out Phase 3 (DEBLOAT-01/02/03 fully satisfied): confirm the whole 28-action catalog is functional end-to-end via a ViewModel-level regression lock (complementing 03-06's catalog-level `All_28_catalog_actions_have_resolvable_run_resources` theory), and add the one piece of production polish RESEARCH.md's Pitfall 5 flagged but earlier plans deliberately deferred — a per-row visual disclosure so a user can see, before clicking, which actions require confirmation or download an unverified binary on Undo.

Purpose: phase-closing verification pass, matching Phase 1/2's precedent of a dedicated final-integration plan (01-07-PLAN.md, 02-07-PLAN.md) that re-proves the whole assembled catalog rather than trusting each incremental plan's own narrow test.
Output: a `DebloatViewModel`-level regression test; two new bound caption elements on `DebloatPage.xaml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-debloat/03-RESEARCH.md
@src/AkariToolbox.App/Models/DebloatActionItem.cs
@src/AkariToolbox.App/ViewModels/DebloatViewModel.cs
@src/AkariToolbox.App/Views/DebloatPage.xaml
@src/AkariToolbox.App/Services/DebloatCatalog.cs
@src/AkariToolbox.Tests/DebloatCatalogTests.cs
@src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-row risk captions (RequiresConfirmation / UndoDownloadsUnverifiedBinary)</name>
  <files>src/AkariToolbox.App/Models/DebloatActionItem.cs, src/AkariToolbox.App/ViewModels/DebloatViewModel.cs, src/AkariToolbox.App/Views/DebloatPage.xaml</files>
  <read_first>src/AkariToolbox.App/Models/DebloatActionItem.cs, src/AkariToolbox.App/ViewModels/DebloatViewModel.cs, src/AkariToolbox.App/Views/DebloatPage.xaml</read_first>
  <behavior>
    - DebloatViewModel_populates_UndoDownloadsUnverifiedBinary_from_catalog: the "edgewebview" and "edgesettings" items in `CategoryGroups` have `UndoDownloadsUnverifiedBinary == true`; every other item has it `false`.
    - DebloatViewModel_populates_RequiresConfirmation_from_catalog_unchanged: the 5 D-11 items still report `RequiresConfirmation == true` after this plan's changes (regression guard — this field already existed since 03-01, this fact proves Task 1 did not accidentally break it while adding the new field).
  </behavior>
  <action>
    In `src/AkariToolbox.App/Models/DebloatActionItem.cs`, add `public bool UndoDownloadsUnverifiedBinary { get; init; }` to `DebloatActionItem` (alongside the existing `RequiresConfirmation`).

    In `src/AkariToolbox.App/ViewModels/DebloatViewModel.cs`'s constructor, extend the existing `DebloatActionItem` object-initializer inside the `CategoryGroups` LINQ projection to also set `UndoDownloadsUnverifiedBinary = a.UndoDownloadsUnverifiedBinary`.

    In `src/AkariToolbox.App/Views/DebloatPage.xaml`, inside the inner `ItemsRepeater.ItemTemplate`'s `DataTemplate x:DataType="models:DebloatActionItem"`, add two small `TextBlock` captions directly below the existing Title/Description `StackPanel` (still inside the left `Grid.Column="0"` stack, `Style="{StaticResource CaptionTextBlockStyle}"`, a muted/warning-toned `Foreground` such as `{ThemeResource SystemFillColorCautionBrush}`): one with `Text="Requires confirmation before running"` and `Visibility="{x:Bind RequiresConfirmation}"`, another with `Text="Undo downloads a binary that is not signature-verified"` and `Visibility="{x:Bind UndoDownloadsUnverifiedBinary}"` (x:Bind's built-in bool-to-Visibility conversion applies to both, no converter needed, matching 03-01-PLAN.md's `HasUndo`/`IsRunning` bindings).
  </action>
  <verify>
    <automated>cd src && dotnet build AkariToolbox.sln -c Debug</automated>
  </verify>
  <done>Solution builds clean; the Debloat page shows a caution caption on BitLocker/Bloatware/Edge&amp;WebView-Remove/Hibernation/OneDrive rows and a separate caption on Edge&amp;WebView-Remove/Edge-Debloat rows (Edge&amp;WebView-Remove shows both), with no caption on the other 23 rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase-closing DebloatViewModel regression lock</name>
  <files>src/AkariToolbox.Tests/DebloatCatalogTests.cs</files>
  <read_first>src/AkariToolbox.Tests/DebloatCatalogTests.cs, src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs</read_first>
  <behavior>
    - DebloatViewModel_CategoryGroups_matches_predecessors_exact_category_sequence_and_counts: constructing a `DebloatViewModel` against the real `DebloatCatalog` yields `CategoryGroups` with names exactly `["Privacy & Telemetry", "System & Performance", "Cleanup", "Explorer & UI", "Tools"]` in that order, and action counts exactly `[8, 8, 6, 5, 1]` — the DEBLOAT-01/DEBLOAT-03 closing invariant, proven at the ViewModel layer (not just the catalog layer 03-01/03-06 already covered), since the ViewModel is what the page actually binds to.
    - DebloatViewModel_total_action_count_is_28: summing every group's `Actions.Count` equals 28.
  </behavior>
  <action>
    Add a final `[Fact]` pair to `src/AkariToolbox.Tests/DebloatCatalogTests.cs`, named exactly as in this task's `<behavior>` block, constructing `new DebloatViewModel(new DebloatCatalog(), new LogConsoleService(dispatcher: null), fakeScriptRunner, fakeDialogService)` (reuse Task 2 of 03-01-PLAN.md's `FakeScriptRunner`/`FakeDialogService`) and asserting on `viewModel.CategoryGroups` directly — this is the phase's closing regression-test lock, the `DebloatViewModel`-layer analog to `TweakHandlerOrderingTests`'s "resolve exactly 32/11, no gaps" pattern for Tweaks/Gaming.
  </action>
  <verify>
    <automated>cd src && dotnet test AkariToolbox.Tests --filter "FullyQualifiedName~DebloatCatalogTests"</automated>
  </verify>
  <done>All facts across the full DebloatCatalogTests suite (03-01 through 03-07) pass; `dotnet build` remains clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan is UI polish + test-only, extending 03-01-PLAN.md's threat model.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15 | Information Disclosure (positive — risk transparency) | Per-row risk captions | n/a | mitigate | This plan's own Task 1 directly improves the user's ability to recognize risky actions before clicking, closing RESEARCH.md's Pitfall 5 UX gap — listed here for completeness, not because it introduces a new risk |

</threat_model>

<verification>
1. `dotnet build AkariToolbox.sln -c Debug` succeeds.
2. `dotnet test AkariToolbox.Tests --filter "FullyQualifiedName~DebloatCatalogTests"` — the complete phase-wide suite (all facts from 03-01 through 03-07) passes.
3. Manual UAT pass: launch the app elevated, visit the Debloat page, confirm all 28 actions across 5 categories render, confirm the 5 D-11 rows and 2 unverified-download rows show their captions, spot-check 3-4 actions across different categories (at least one Run-only, one Run+Undo, one D-11-gated) execute successfully end-to-end with streamed log output.
</verification>

<success_criteria>
DEBLOAT-01, DEBLOAT-02, and DEBLOAT-03 are all fully satisfied. Phase 3 (Debloat) is feature-complete: 28/28 actions functional, streamed output proven, zero-logic code-behind proven, D-11 confirmation gate proven, D-10 accepted-risk disclosure proven both in logs and now in the UI.
</success_criteria>

## Artifacts this phase produces

- `DebloatActionItem.UndoDownloadsUnverifiedBinary` (new bindable field)
- Two new caption `TextBlock` elements per row in `DebloatPage.xaml` (conditionally visible)
- `AkariToolbox.Tests.DebloatCatalogTests.DebloatViewModel_CategoryGroups_matches_predecessors_exact_category_sequence_and_counts`, `.DebloatViewModel_total_action_count_is_28`, `.DebloatViewModel_populates_UndoDownloadsUnverifiedBinary_from_catalog`, `.DebloatViewModel_populates_RequiresConfirmation_from_catalog_unchanged` (new test methods)

<output>
Create `.planning/phases/03-debloat/03-07-SUMMARY.md` when done
</output>
