---
phase: 01-foundation-akari-os-tweaks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AkariToolbox.slnx
  - Directory.Build.props
  - Directory.Packages.props
  - global.json
  - nuget.config
  - src/AkariToolbox.App/AkariToolbox.App.csproj
  - src/AkariToolbox.App/App.xaml
  - src/AkariToolbox.App/App.xaml.cs
  - src/AkariToolbox.App/MainWindow.xaml
  - src/AkariToolbox.App/MainWindow.xaml.cs
  - src/AkariToolbox.App/NavigationItem.cs
  - src/AkariToolbox.App/app.manifest
  - src/AkariToolbox.App/Assets/AppIcon.ico
  - src/AkariToolbox.App/Assets/AppIcon.png
  - src/AkariToolbox.App/Views/HomePage.xaml
  - src/AkariToolbox.App/Views/HomePage.xaml.cs
  - src/AkariToolbox.App/ViewModels/HomeViewModel.cs
  - src/AkariToolbox.App/Models/TweakItem.cs
  - src/AkariToolbox.App/Services/ITweakHandler.cs
  - src/AkariToolbox.App/Services/ITweakCatalog.cs
  - src/AkariToolbox.App/Services/TweakCatalog.cs
  - src/AkariToolbox.App/Services/TweakHandlers/WifiTweakHandler.cs
  - src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs
  - src/AkariToolbox.App/Views/AkariOSTweaksPage.xaml
  - src/AkariToolbox.App/Views/AkariOSTweaksPage.xaml.cs
  - src/AkariToolbox.Framework/AkariToolbox.Framework.csproj
  - src/AkariToolbox.Framework/Services/IRegistryService.cs
  - src/AkariToolbox.Framework/Services/RegistryService.cs
  - src/AkariToolbox.Framework/ServiceCollectionExtensions.Primitives.cs
  - src/AkariToolbox.Tests/AkariToolbox.Tests.csproj
autonomous: true
requirements: [APP-01, APP-02, APP-03, APP-05, HOME-01, TWEAKS-01, TWEAKS-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "App launches under administrator elevation (UAC prompt appears) with the 'Akari Toolbox' identity — title bar text, window title, and assembly identity all read 'Akari Toolbox', not 'App Template'."
    - "MainWindow.xaml.cs sets SystemBackdrop = new MicaBackdrop() and no Themes/Colors.xaml- or Themes/Controls.xaml-equivalent file exists anywhere under src/ — verified by absence check (APP-03, no WPF-UI theme carried over)."
    - "Home page renders exactly 5 destination cards (Akari OS Tweaks, Gaming Tweaks, Debloat, Downloads, Misc); exactly 1 (Akari OS Tweaks) is enabled/interactive and 4 are visibly disabled 'Coming soon' and do not navigate on click (HOME-01, D-09/D-10/D-11)."
    - "The WiFi toggle on the Akari OS Tweaks page reflects the real live WlanSvc\\Start registry value on page load (not a cached/default value), and flipping it writes the real registry value and reverts to the real prior value on flip-back (TWEAKS-01, TWEAKS-03, D-03/D-04)."
    - "ITweakCatalog.SetStateAsync calls handler.GetState() before handler.SetState() and skips the write (no-op) when the requested value already equals the live-read current value — verified by a unit test with a fake ITweakHandler that fails the test if SetState is invoked on a same-value call (TWEAKS-01 idempotency)."
    - "Calling ITweakCatalog.SetStateAsync for the same key twice in rapid succession does not race: the second call either awaits the first's completion or the triggering UI toggle is disabled while a SetState for that key is in flight (TWEAKS-01/TWEAKS-03 concurrency)."
    - "ITweakCatalog captures each handler's live GetState() the first time SetStateAsync mutates that key in the app session and restores that captured value on the next call that reverts it, rather than a hardcoded default (TWEAKS-03 idempotency — capture is per-session, in-memory only, and does not survive an app restart)."
    - { statement: "If zero ITweakHandler implementations are registered (a broken assembly scan), AkariOSTweaksViewModel's construction does not throw and Tweaks renders as an empty ObservableCollection rather than crashing the page.", verification: backstop }
    - "ITweakHandler exposes an int Order property; AkariOSTweaksViewModel sorts catalog.Handlers by Order ascending before building TweakItems, and WifiTweakHandler.Order matches its position (0) in the predecessor's known 32-tweak sequence (wifi, tsx, actioncenter, dep, clipboard, bluetooth, bootmenu, vpn, ntfsenc, fso, notifications, prefetch, cdrom, spooler, nolazy, uacadmin, vr, uac, startmenu, hyperv, vbs, wallpaperq, mpo, transparency, lockscreen, animations, dcom, nvme, largecache, sysprofile, defender, mitigation) — verified by a unit test asserting Order values are unique and monotonic with insertion (TWEAKS-01 ordering)."
    - "Disabled Home cards and disabled nav-sidebar items render with IsEnabled=false (not merely styled to look disabled) so WinUI itself suppresses hover/press visual states and click handling — a click on a disabled card/item does not invoke navigation (no silent no-op deception)."
  artifacts:
    - path: "src/AkariToolbox.App/Services/ITweakHandler.cs"
      provides: "Self-describing tweak-handler contract (Key/Title/Description/Order/GetState/SetState) every later handler batch implements"
    - path: "src/AkariToolbox.App/Services/TweakCatalog.cs"
      provides: "ITweakCatalog implementation — reflection-based ITweakHandler auto-registration, capture-then-write orchestration, per-key serialization"
    - path: "src/AkariToolbox.Framework/Services/RegistryService.cs"
      provides: "IRegistryService — OpenSubKey/GetValue null-checked reads, CreateSubKey-gated writes, OpenRealUserHive(string) real-user-HKCU-under-elevation primitive"
    - path: "src/AkariToolbox.App/Services/TweakHandlers/WifiTweakHandler.cs"
      provides: "First real ITweakHandler — proves the vertical slice end-to-end"
    - path: "src/AkariToolbox.App/app.manifest"
      provides: "requireAdministrator elevation (APP-01)"
  key_links:
    - from: "src/AkariToolbox.App/Views/AkariOSTweaksPage.xaml"
      to: "src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs"
      via: "x:Bind ToggleSwitch.IsOn to TweakItem.IsOn, TweakItem.PropertyChanged drives catalog.SetStateAsync"
      pattern: "_tweaks\\.(GetStateAsync|SetStateAsync)"
    - from: "src/AkariToolbox.App/Services/TweakCatalog.cs"
      to: "src/AkariToolbox.Framework/Services/RegistryService.cs"
      via: "WifiTweakHandler constructor-injects IRegistryService, calls GetValue/SetValue"
      pattern: "IRegistryService"
    - from: "src/AkariToolbox.App/App.xaml.cs"
      to: "src/AkariToolbox.Framework/ServiceCollectionExtensions.Primitives.cs"
      via: "BuildHost() calls builder.Services.AddAkariSystemPrimitives() and a reflection-based AddTweakHandlers()"
      pattern: "AddAkariSystemPrimitives|AddTweakHandlers"
  prohibitions:
    - "MUST NOT let a disabled 'Coming soon' Home card or nav-sidebar item appear interactive (hover/press visual states) or silently no-op on click — disabled destinations must be behaviorally non-interactive via IsEnabled=false, not a fake-disabled style that still responds to input."
---

<objective>
Stand up the rebranded, elevated Akari Toolbox solution (copied/renamed from `WinUI-3-MVVM-Framework`) and prove the entire architecture end-to-end on one real tweak (WiFi) before expanding to the other 31 — this is the phase's Walking Skeleton (see `SKELETON.md`). Also deliver the full 5-destination Home dashboard and nav sidebar (D-09/D-10/D-11), since both share the same shell files the tracer already touches.

**As a** user setting up a fresh Windows machine, **I want to** launch a single elevated Akari Toolbox app and see a Home dashboard with all my tool categories, then flip a real system tweak and see it actually take effect, **so that** I can trust every toggle in this app reflects and controls the real state of my machine, not a fake UI checkbox.

Purpose: prove the copy/rename + elevation + primitive/handler/catalog architecture works before any of the other 31 tweaks are built on top of it, and ship the always-visible navigation shell every later phase depends on.
Output: an elevated, running "Akari Toolbox" app with a 5-card Home page, a working nav sidebar, and one fully real, revertible tweak toggle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-akari-os-tweaks/01-CONTEXT.md
@.planning/phases/01-foundation-akari-os-tweaks/01-RESEARCH.md
@.planning/phases/01-foundation-akari-os-tweaks/01-PATTERNS.md
@.planning/phases/01-foundation-akari-os-tweaks/SKELETON.md

# Framework template to copy (read-only source, do not modify in place)
@C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/README.md
@C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.App/App.xaml.cs
@C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.App/MainWindow.xaml
@C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.App/MainWindow.xaml.cs
@C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.App/NavigationItem.cs
@C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.App/app.manifest
@C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.App/Views/HomePage.xaml
@C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.App/Views/HomePage.xaml.cs
@C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.App/ViewModels/HomeViewModel.cs
@C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.Framework/ServiceCollectionExtensions.cs

# Predecessor source to port from (read-only, do not modify)
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/app.manifest
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Services/TweakService.cs
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Models/TweakItem.cs
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/ViewModels/AkariOSTweaksViewModel.cs
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/ViewModels/HomeViewModel.cs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Copy/rename framework solution, elevate, wire WiFi tweak end-to-end</name>
  <files>AkariToolbox.slnx, Directory.Build.props, Directory.Packages.props, global.json, nuget.config, src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/App.xaml, src/AkariToolbox.App/App.xaml.cs, src/AkariToolbox.App/app.manifest, src/AkariToolbox.App/Assets/AppIcon.ico, src/AkariToolbox.App/Assets/AppIcon.png, src/AkariToolbox.App/MainWindow.xaml, src/AkariToolbox.App/MainWindow.xaml.cs, src/AkariToolbox.App/NavigationItem.cs, src/AkariToolbox.App/Models/TweakItem.cs, src/AkariToolbox.App/Services/ITweakHandler.cs, src/AkariToolbox.App/Services/ITweakCatalog.cs, src/AkariToolbox.App/Services/TweakCatalog.cs, src/AkariToolbox.App/Services/TweakHandlers/WifiTweakHandler.cs, src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs, src/AkariToolbox.App/Views/AkariOSTweaksPage.xaml, src/AkariToolbox.App/Views/AkariOSTweaksPage.xaml.cs, src/AkariToolbox.Framework/AkariToolbox.Framework.csproj, src/AkariToolbox.Framework/Services/IRegistryService.cs, src/AkariToolbox.Framework/Services/RegistryService.cs, src/AkariToolbox.Framework/ServiceCollectionExtensions.Primitives.cs, src/AkariToolbox.Tests/AkariToolbox.Tests.csproj</files>
  <read_first>C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/README.md (rename checklist), C:/Users/isleap/Documents/GitHub/AkariOS-Companion/app.manifest, C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Services/TweakService.cs lines 164-183 (SetWifi) and lines 1-31 (interface shape), C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Models/TweakItem.cs</read_first>
  <action>
    Copy the entire `C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework` tree (`AppTemplate.slnx`, `Directory.Build.props`, `Directory.Packages.props`, `global.json`, `nuget.config`, `src/AppTemplate.App/`, `src/AppTemplate.Framework/`, `src/AppTemplate.Tests/`) into this repo's root (`C:/Users/isleap/Documents/GitHub/Akari-Toolbox`), excluding `bin/`/`obj/`. Rename every occurrence of `AppTemplate` to `AkariToolbox` across file names, folder names, `.csproj` `RootNamespace`/`AssemblyName`, the `.slnx` project references, and every `namespace`/`using` statement in every copied `.cs`/`.xaml`/`.xaml.cs` file (mechanical find-and-replace across the whole copied tree — this includes files not otherwise touched by this task, e.g. `Navigation/FrameNavigationService.cs`, `Messaging/*`, `Collections/*`, `Converters/*`, `Behaviors/*`, `Logging/FileLoggerProvider.cs`, `Services/IDialogService.cs`, `Services/IWindowService.cs`, `Services/IInfoBarService.cs`, `Services/IFilePickerService.cs`, `Services/ICultureService.cs`, `Services/IThemeService.cs`, `Services/ISettingsService.cs`/`ISettingsStorage.cs`/`FileSettingsStorage.cs`, `ServiceCollectionExtensions.cs`, `ObjectExtensions.cs`, and `AppTemplate.Tests`'s test files — none of these need behavior changes in this task, only the namespace rename).

    **Checkpoint (quality-degradation control for this task's unusually broad file scope):** once the copy+rename pass above is complete, run `dotnet build AkariToolbox.slnx -c Debug` before writing any new primitive/handler/catalog code below. Confirm the renamed template alone compiles cleanly first — this isolates rename-only build breaks (missed namespace occurrence, stale project reference) from new-code build breaks, and keeps this task's quality bar consistent across its full breadth before layering the WiFi vertical slice on top.

    In `App.xaml.cs`: change `public static string AppName => "App Template";` to `"Akari Toolbox"`. Change `SettingsFolder` from `Path.Combine(..., "AppTemplate")` to `Path.Combine(..., "AkariToolbox")`. In `BuildHost()`, after the existing `builder.Services.AddMvvmFramework();` line, add `builder.Services.AddAkariSystemPrimitives();` and `builder.Services.AddTweakHandlers();`.

    Create `src/AkariToolbox.Framework/ServiceCollectionExtensions.Primitives.cs` with a static class `AkariPrimitivesServiceCollectionExtensions` exposing `AddAkariSystemPrimitives(this IServiceCollection services)` that registers `services.AddSingleton<IRegistryService, RegistryService>();` only (later plans extend this same method — do not create a second method or a second file for primitives). In the same file (or a sibling `TweakHandlerRegistration.cs` in `AkariToolbox.App`), add `AddTweakHandlers(this IServiceCollection services)` that reflection-scans `typeof(WifiTweakHandler).Assembly.GetTypes()` for every non-abstract class implementing `ITweakHandler` and registers each as `services.AddSingleton(typeof(ITweakHandler), type)` — this is the ONE registration call site; every later tweak-handler batch plan adds classes only, never touches this method again. Also register `services.AddSingleton<ITweakCatalog, TweakCatalog>();` in `AddTweakHandlers`.

    Add elevation to `src/AkariToolbox.App/app.manifest`: insert the `<trustInfo xmlns="urn:schemas-microsoft-com:asm.v2"><security><requestedPrivileges xmlns="urn:schemas-microsoft-com:asm.v3"><requestedExecutionLevel level="requireAdministrator" uiAccess="false" /></requestedPrivileges></security></trustInfo>` block (verbatim from the predecessor's `app.manifest`) as a sibling of the existing `<compatibility>`/`<application>` blocks; keep those existing blocks unchanged. Update `<assemblyIdentity name="AppTemplate.App.app" />` to `name="AkariToolbox.App.app"`.

    Replace `src/AkariToolbox.App/Assets/AppIcon.ico` and `AppIcon.png` with copies of the predecessor's `C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Assets/AkariLogo.ico` and `AkariLogo.png` (same filenames as the framework expects — `AppIcon.ico`/`AppIcon.png` — so `AkariToolbox.App.csproj`'s `<ApplicationIcon>` and `MainWindow.xaml.cs`'s `LoadAppIcon()`/icon-path logic need no changes).

    Create `src/AkariToolbox.App/Models/TweakItem.cs`: copy the predecessor's `TweakItem` verbatim (CommunityToolkit.Mvvm `ObservableObject`, `[ObservableProperty] private bool _isOn`, init-only `Key`/`Title`/`Description` string properties), namespace `AkariToolbox.App.Models`.

    Create `src/AkariToolbox.App/Services/ITweakHandler.cs` in namespace `AkariToolbox.App.Services` with: `string Key { get; }`, `string Title { get; }`, `string Description { get; }`, `int Order { get; }`, `bool GetState();`, `void SetState(bool enabled);`. This is the contract every one of the remaining 31 handlers (Plans 01-04, 01-05, 01-06) implements — do not add async members here (Defender's own async orchestration in Plan 01-06 lives outside this interface, dispatched by TweakCatalog as a special case keyed on `"defender"`).

    Create `src/AkariToolbox.App/Services/ITweakCatalog.cs`: `IReadOnlyList<ITweakHandler> Handlers { get; }` (populated once at construction from the DI-resolved `IEnumerable<ITweakHandler>`, sorted by `Order` ascending), `Task<bool> GetStateAsync(string key)`, `Task SetStateAsync(string key, bool enabled)`.

    Create `src/AkariToolbox.App/Services/TweakCatalog.cs` implementing `ITweakCatalog`: constructor takes `IEnumerable<ITweakHandler> handlers`, builds a `Dictionary<string, ITweakHandler>` keyed by `Key` and the sorted `Handlers` list. Maintain an in-memory `Dictionary<string, bool> _priorState` and a `Dictionary<string, SemaphoreSlim> _locks` (one per key, created lazily). `SetStateAsync(key, enabled)`: resolve the lock for `key`, `await`-acquire it, read `bool current = handler.GetState()`; if `current == enabled`, release and return (no-op, satisfies the idempotency truth); otherwise if `!_priorState.ContainsKey(key)` store `_priorState[key] = current` (first-mutation-of-session capture, satisfies TWEAKS-03), call `handler.SetState(enabled)` on a background thread via `Task.Run` (registry/service calls are synchronous), then release the lock. `GetStateAsync(key)` calls `handler.GetState()` via `Task.Run` (a live read has no side effects, no lock needed). The `"defender"` key is out of scope for this task (Plan 01-06 wires it) — `TweakCatalog` should look it up through the same `_handlers` dictionary once a `DefenderTweakHandler` is registered later; no special-casing code is needed in this task since Defender doesn't exist yet.

    Create `src/AkariToolbox.App/Services/TweakHandlers/WifiTweakHandler.cs` implementing `ITweakHandler`: `Key => "wifi"`, `Title => "Disable WiFi"`, `Description => "Toggle WiFi On or Off"`, `Order => 0`. Constructor takes `IRegistryService registry`. `GetState()` returns true when `registry.GetValue(RegistryHive.LocalMachine, @"SYSTEM\CurrentControlSet\Services\WlanSvc", "Start") is int start && start == 4`. `SetState(bool disable)` writes DWORD `Start` = 4 (disable) or 2 (enable) to `SYSTEM\CurrentControlSet\Services\WlanSvc`, 4 or 1 to `...\vwififlt`, 4 or 3 to `...\netprofm`, 4 or 2 to `...\NlaSvc`, all under `RegistryHive.LocalMachine` — exact values ported from `TweakService.cs:164-183`. Do NOT port `HasState`/`SaveState`/`ClearState` — the `if (HasState(...)) return;` idempotency guard is replaced by `TweakCatalog`'s live `GetState()` comparison (D-03/D-04 anti-pattern strip, RESEARCH Pitfall 4).

    Create `src/AkariToolbox.Framework/Services/IRegistryService.cs` in namespace `AkariToolbox.Framework.Services`: `object? GetValue(RegistryHive hive, string subKeyPath, string valueName);`, `void SetValue(RegistryHive hive, string subKeyPath, string valueName, object value, RegistryValueKind kind);`, `void DeleteValue(RegistryHive hive, string subKeyPath, string valueName);`, `RegistryKey OpenRealUserHive(string subKeyPath);`. Create `RegistryService.cs` implementing it: `GetValue` uses `RegistryKey.OpenBaseKey(hive, RegistryView.Registry64).OpenSubKey(subKeyPath)` with a null-check before calling `.GetValue(valueName)` (never throws on a missing key — CLAUDE.md registry-squatting-safe convention). `SetValue` uses `OpenSubKey(subKeyPath, writable: true)`, and if null, falls back to `CreateSubKey(subKeyPath, writable: true)` (only creates when the parent path is legitimately absent, per CLAUDE.md's "prefer CreateSubKey only when the tweak is explicitly meant to create the key"). `OpenRealUserHive(subKeyPath)` ports `CreateRealHkcuSubKey` from `TweakService.cs:141-155` verbatim (the `OpenProcessToken`/`WindowsIdentity` P/Invoke trick, hard-throws `InvalidOperationException("explorer.exe not found.")` if no `explorer` process exists — preserve this hard failure exactly, per D-14, no fallback).

    Create `src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs`: constructor takes `ITweakCatalog catalog` and `DispatcherQueue dispatcher` (resolve via `DispatcherQueue.GetForCurrentThread()` at construction, or inject a `Func<DispatcherQueue>` following the framework's existing provider pattern). Expose `ObservableCollection<TweakItem> Tweaks { get; }`. In the constructor, for each `handler` in `catalog.Handlers` (already `Order`-sorted), create a `TweakItem { Key = handler.Key, Title = handler.Title, Description = handler.Description, IsOn = false }`, add to `Tweaks` immediately (do not block construction on registry reads), then `_ = Task.Run(() => catalog.GetStateAsync(handler.Key)).ContinueWith(t => dispatcher.RunOnUIThreadAsync(() => item.IsOn = t.Result))` per handler (parallel async reads, each marshaled back individually — satisfies APP-05 and avoids the predecessor's synchronous blocking-constructor pattern per RESEARCH Pitfall 3). Wire `item.PropertyChanged` exactly as the predecessor does: when `IsOn` changes, call `_ = catalog.SetStateAsync(item.Key, item.IsOn)` (fire-and-forget is acceptable here since `TweakCatalog` internally serializes per-key and the UI toggle itself already reflects the user's intent; log failures via a try/catch that swallows-and-logs, never silently swallows without a trace — Plan 01-02 wires `ILogConsoleService` into this catch once it exists, for this task a `System.Diagnostics.Debug.WriteLine` placeholder is acceptable since `ILogConsoleService` does not exist yet). Only the `"wifi"` handler exists at this point, so `Tweaks` will contain exactly one item — this is expected and correct for the tracer.

    Create `src/AkariToolbox.App/Views/AkariOSTweaksPage.xaml` + `.xaml.cs`: a `Page` with an `ItemsRepeater` (or simple `StackPanel` of rows for this first pass — an `ItemsRepeater`/`GridView` full layout is fine to defer to visual polish, but the ToggleSwitch binding must be real) bound to `AkariOSTweaksViewModel.Tweaks`, each row showing `Title`, `Description`, and a `ToggleSwitch` with `IsOn="{x:Bind IsOn, Mode=TwoWay}"`. Register `AkariOSTweaksViewModel` as `AddTransient<AkariOSTweaksViewModel>()` in `App.xaml.cs`'s `BuildHost()`, and register the page's DI construction the same way the framework already does for `HomePage`/`SettingsPage` (via `FrameNavigationService`'s `ActivatorUtilities.CreateInstance`, no extra registration needed beyond the ViewModel itself being resolvable).

    Update `MainWindow.xaml.cs`'s `NavItems` to include a `new("Akari OS Tweaks", "\uE713", typeof(AkariOSTweaksPage))` entry alongside the existing `Home` entry (the remaining 3 disabled entries are added in Task 2 of this plan, together with the Home-card work, to keep this task focused on the vertical slice).

    Build the solution (`dotnet build AkariToolbox.slnx -c Debug`) and launch it from an elevated terminal (`dotnet run --project src/AkariToolbox.App -c Debug`) to smoke-test the elevation manifest + Mica shell + WiFi toggle end-to-end before moving to Task 2.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <human-check>From an elevated PowerShell/terminal, run `dotnet run --project src/AkariToolbox.App -c Debug` (or launch the built exe as Administrator). Confirm: (1) a UAC elevation prompt appears before the window opens (or the process is already elevated with no manifest-merge `c1010001` build error per RESEARCH Pitfall 1); (2) the window title bar reads "Akari Toolbox"; (3) the shell renders with a Mica backdrop (translucent, follows Windows theme); (4) navigating to "Akari OS Tweaks" shows exactly one toggle row ("Disable WiFi"); (5) toggling it on, then checking `reg query "HKLM\SYSTEM\CurrentControlSet\Services\WlanSvc" /v Start` in a separate elevated terminal shows value `4`; (6) toggling it back off restores the real prior value (not always `2`) — set `WlanSvc\Start` to `3` manually via `reg add` before the first toggle-on to confirm the revert restores `3`, not a hardcoded `2`.</human-check>
  </verify>
  <done>Solution builds; app launches elevated with "Akari Toolbox" branding and Mica backdrop; the WiFi toggle reads real registry state on load, writes real state on toggle, and restores the real captured prior state on revert.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - App window title bar text equals "Akari Toolbox" (not "App Template").
    - `src/AkariToolbox.App/app.manifest` contains `requestedExecutionLevel level="requireAdministrator"`.
    - `reg query "HKLM\SYSTEM\CurrentControlSet\Services\WlanSvc" /v Start` returns `4` after toggling WiFi off, and returns the pre-toggle value (not a hardcoded `2`) after toggling back on.
    - `grep -c "HasState\|SaveState\|ClearState" src/AkariToolbox.App/Services/TweakHandlers/WifiTweakHandler.cs` returns `0`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full Home dashboard (5 cards) and nav sidebar (5 entries, 4 disabled)</name>
  <files>src/AkariToolbox.App/Views/HomePage.xaml, src/AkariToolbox.App/Views/HomePage.xaml.cs, src/AkariToolbox.App/ViewModels/HomeViewModel.cs, src/AkariToolbox.App/NavigationItem.cs, src/AkariToolbox.App/MainWindow.xaml, src/AkariToolbox.App/MainWindow.xaml.cs</files>
  <read_first>C:/Users/isleap/Documents/GitHub/AkariOS-Companion/ViewModels/HomeViewModel.cs, C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.App/Views/HomePage.xaml, C:/Users/isleap/Documents/GitHub/WinUI-3-MVVM-Framework/src/AppTemplate.App/NavigationItem.cs</read_first>
  <action>
    Extend `NavigationItem` (`public sealed record NavigationItem(string Label, string Glyph, Type PageType);`) to `public sealed record NavigationItem(string Label, string Glyph, Type PageType, bool IsEnabled = true);`. Update `MainWindow.xaml`'s `NavigationView.MenuItemTemplate` `DataTemplate` to add `IsEnabled="{x:Bind IsEnabled}"` on the `NavigationViewItem` element (alongside the existing `Content`/`Tag` bindings).

    Update `MainWindow.xaml.cs`'s `NavItems` to the full 5-entry set, all `IsEnabled` explicit: `new("Home", "\uE80F", typeof(HomePage))`, `new("Akari OS Tweaks", "\uE713", typeof(AkariOSTweaksPage))` (added in Task 1, keep as-is, `IsEnabled: true` default), `new("Gaming Tweaks", "\uE7FC", typeof(HomePage), IsEnabled: false)`, `new("Debloat", "\uE74D", typeof(HomePage), IsEnabled: false)`, `new("Downloads", "\uE896", typeof(HomePage), IsEnabled: false)`, `new("Misc", "\uE712", typeof(HomePage), IsEnabled: false)` (disabled entries point at `typeof(HomePage)` as an inert placeholder target since their real pages don't exist yet and `IsEnabled=false` already prevents `OnNavSelectionChanged` from ever navigating to it).

    Create `src/AkariToolbox.App/ViewModels/HomeViewModel.cs`: a `HomeCard` model (`Title`, `Description`, `Glyph`, `Type Target`, `bool IsEnabled = true`) and a `HomeViewModel : ObservableObject` exposing `IReadOnlyList<HomeCard> Cards` with exactly 5 entries, content ported from the predecessor's `HomeViewModel.cs` verbatim: `("Gaming Tweaks", "GPU, latency & service tuning for peak FPS", "\uE7FC", typeof(HomePage), false)`, `("Akari OS Tweaks", "Toggle deep system modifications & services", "\uE713", typeof(AkariOSTweaksPage), true)`, `("Downloads", "Playbooks, drivers & recommended utilities", "\uE896", typeof(HomePage), false)`, `("Misc", "Context-menu entries & extra tools", "\uE712", typeof(HomePage), false)`, plus a new 5th card `("Debloat", "Run 28 PowerShell-backed debloat actions", "\uE74D", typeof(HomePage), false)` (D-10 — Debloat gets equal footing; `\uE74D` is a delete/cleanup-adjacent Segoe Fluent Icons glyph, Claude's discretion per CONTEXT.md). Add a `[RelayCommand] private void Open(Type pageType)` that resolves `INavigationService` (constructor-injected) and calls `_navigation.NavigateTo(pageType)` — do NOT reuse the predecessor's static `AppNavigation.Navigate` helper (framework already DI-registers `INavigationService`).

    Update `Views/HomePage.xaml` to bind an `ItemsRepeater` or `GridView` (2-3 column responsive grid, matching Fluent 2 conventions — no WPF-UI styling) to `HomeViewModel.Cards`, each card showing `Glyph` (via `FontIcon`), `Title`, `Description`, bound `IsEnabled="{x:Bind IsEnabled}"` on the card's root `Button`/`Grid` control (same IsEnabled-suppresses-interaction mechanism as the nav sidebar — do not implement disabled state as a style-only visual treatment), and a small "Coming soon" text badge visible only when `IsEnabled` is false (e.g. via a converter or a second `TextBlock` with `Visibility` bound off `IsEnabled`). Clicking an enabled card invokes `OpenCommand` with its `Target` type.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <human-check>Launch the app elevated. On Home: confirm exactly 5 cards render (Akari OS Tweaks, Gaming Tweaks, Debloat, Downloads, Misc), only "Akari OS Tweaks" is clickable and navigates to the Tweaks page, the other 4 show a visible "Coming soon" treatment and clicking them does nothing (no navigation, no visual press feedback). In the nav sidebar: confirm all 5 entries appear, only "Home" and "Akari OS Tweaks" are clickable, the other 4 are visibly greyed/disabled and unclickable.</human-check>
  </verify>
  <done>Home page shows all 5 destination cards with exactly 1 enabled; nav sidebar shows all 5 entries with exactly 2 enabled (Home, Akari OS Tweaks); disabled destinations are behaviorally non-interactive, not just styled.</done>
  <acceptance_criteria>
    - `HomeViewModel.Cards.Count == 5` and exactly 1 has `IsEnabled == true`.
    - `MainWindow.NavItems.Count == 5` and exactly 2 have `IsEnabled == true` (Home, Akari OS Tweaks).
    - `NavigationItem` record has an `IsEnabled` parameter with default `true`.
    - Clicking a disabled Home card or nav item produces no navigation (manually confirmed) because `IsEnabled=false` on the underlying control, not a style-only treatment.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elevated process -> Windows registry (HKLM) | The whole app runs `requireAdministrator`; every `IRegistryService` write is a privileged mutation of system-wide state — the only access boundary is the manifest-level elevation gate itself (no in-app privilege separation, matches predecessor, RESEARCH Security Domain V4) |
| Copied framework template -> this repo | Untrusted only in the sense that a mechanical rename touches every file; no external network/input involved in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | RegistryService.SetValue / OpenRealUserHive | high | mitigate | Never call `SetValue`/`CreateSubKey` without first `OpenSubKey`+null-check per CLAUDE.md's registry-squatting-safe convention (implemented in Task 1); `CreateSubKey` is only reached as an explicit fallback when the parent path is legitimately absent, never as the default path |
| T-01-02 | Elevation of Privilege | app.manifest requireAdministrator | medium | accept | Standard Win32 unpackaged elevation gate — same mechanism the predecessor already ships; no new elevation surface introduced by this plan |
| T-01-03 | Information Disclosure | AkariOSTweaksViewModel constructor reading 32 (eventually) tweak states on every page load | low | accept | Reads are local-machine registry queries only, no network/IPC exposure; the values read are the same ones the toggle UI displays to the same elevated local user who already has full registry access |
| T-01-04 | Denial of Service | Reflection-based ITweakHandler assembly scan on every DI container build | low | accept | Scan runs once at process startup against a single, small, first-party assembly — bounded cost, no external input |
</threat_model>

<verification>
- `dotnet build AkariToolbox.slnx -c Debug` succeeds with zero errors.
- App launches elevated with "Akari Toolbox" branding, Mica backdrop, no WPF-UI theme files present under `src/`.
- Home page: exactly 5 cards, 1 enabled. Nav sidebar: exactly 5 entries, 2 enabled (Home, Akari OS Tweaks).
- WiFi toggle: real read on load, real write on toggle, real prior-value restore on revert (manually verified via `reg query`).
- `grep -rn "HasState\|SaveState\|ClearState\|Software\\\\AkariTool" src/AkariToolbox.App/Services/TweakHandlers/WifiTweakHandler.cs` returns no matches (D-03 anti-pattern strip).
</verification>

<success_criteria>
Elevated "Akari Toolbox" app launches from the copied/renamed framework solution, shows a 5-card Home dashboard and 5-entry nav sidebar (1/2 enabled respectively), and the WiFi tweak toggle proves the full IRegistryService -> ITweakHandler -> ITweakCatalog -> ViewModel -> View chain works with real live-state reads and real prior-state reverts.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-akari-os-tweaks/01-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `ITweakHandler` (interface: `Key`, `Title`, `Description`, `Order`, `GetState()`, `SetState(bool)`)
- `ITweakCatalog` / `TweakCatalog` (`Handlers`, `GetStateAsync`, `SetStateAsync`)
- `IRegistryService` / `RegistryService` (`GetValue`, `SetValue`, `DeleteValue`, `OpenRealUserHive`)
- `WifiTweakHandler`
- `TweakItem` (model), `AkariOSTweaksViewModel`, `AkariOSTweaksPage`
- `HomeCard`, `HomeViewModel` (5-card list), extended `HomePage`
- `NavigationItem` extended with `IsEnabled`
- `AddAkariSystemPrimitives()`, `AddTweakHandlers()` DI extension methods
- Renamed solution/project identifiers: `AkariToolbox.slnx`, `AkariToolbox.App`, `AkariToolbox.Framework`, `AkariToolbox.Tests`, `App.AppName = "Akari Toolbox"`, `SettingsFolder = "AkariToolbox"`

## Flagged Assumptions (edge-probe items resolved as inapplicable, not force-fit)

- **APP-01 concurrency** ("if interrupted or run in parallel, what is guaranteed?"): single-instance concurrent-launch behavior is unspecified in Phase 1 — the copied framework template already has single-instance forwarding via `AppInstance.FindOrRegisterForKey` (kept as-is, not modified by this plan); this matches the predecessor's lack of any additional guarantee and is not a regression.
- **APP-02 empty/null input**: APP-02 concerns static build-time identity values (`AppName`, namespace, assembly name, icon) fixed at compile time, not runtime input — the empty/null/single-element probe does not apply.
- **APP-02 encoding**: app identity strings are fixed ASCII literals ("Akari Toolbox"); no user-supplied text and no encoding-ambiguity surface exists in Phase 1.
- **TWEAKS-01 adjacency** ("when two things are exactly equal or just touch, do they merge, collide, or separate?"): does not map onto a flat tweak-toggle list — there is no merge/collision concept between independent registry-backed toggles; resolved as inapplicable rather than force-fitting a synthetic acceptance criterion.
