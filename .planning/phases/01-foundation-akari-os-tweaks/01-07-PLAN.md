---
phase: 01-foundation-akari-os-tweaks
plan: 07
type: execute
wave: 4
depends_on: ["01-01", "01-04", "01-05", "01-06"]
files_modified:
  - src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs
  - src/AkariToolbox.App/Services/TweakCatalog.cs
  - src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs
autonomous: true
requirements: [APP-03, APP-05, TWEAKS-01, TWEAKS-03]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Exactly 32 ITweakHandler implementations are registered (wifi + 22 registry + 4 service-backed + 4 bcdedit/DISM-hybrid + defender), each with a unique Order value spanning 0-31 with no gaps or duplicates, matching the predecessor's exact sequence (wifi, tsx, actioncenter, dep, clipboard, bluetooth, bootmenu, vpn, ntfsenc, fso, notifications, prefetch, cdrom, spooler, nolazy, uacadmin, vr, uac, startmenu, hyperv, vbs, wallpaperq, mpo, transparency, lockscreen, animations, dcom, nvme, largecache, sysprofile, defender, mitigation) — verified by an automated unit test."
    - "A handler whose GetState() throws during AkariOSTweaksViewModel's per-item async read logs the exception via ILogConsoleService and the corresponding TweakItem.IsOn defaults to false, rather than crashing page load or leaving the toggle in an indeterminate state — the app's core value statement (every tweak reports accurate state) requires a visible failure over a silent wrong-state guess."
    - "No file under src/AkariToolbox.App/Services/TweakHandlers/ or src/AkariToolbox.App/Services/TweakCatalog.cs contains HasState, SaveState, ClearState, or a reference to HKCU\\Software\\AkariTool (case-sensitive path, the predecessor's exact old hive) — verified by a repo-wide grep across the full TweakHandlers directory, not just per-batch as in Plans 01-04/01-05 (catches any cross-file drift)."
    - "No Themes/Colors.xaml- or Themes/Controls.xaml-equivalent WPF-UI theme file exists anywhere under src/, and MainWindow.xaml.cs still sets SystemBackdrop = new MicaBackdrop() — re-verified at full-phase completion (APP-03), not just after Plan 01-01."
    - "Launching the app elevated shows all 32 tweaks in Akari OS Tweaks, each toggle reflecting real live system state on load (spot-checked against a handful of registry values via reg query) and reverting to the real captured prior state on toggle-off."
  artifacts:
    - path: "src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs"
      provides: "Automated regression test for the 32-handler Order-uniqueness/sequence-match invariant, so a future phase adding/removing a handler cannot silently break TWEAKS-01 ordering"
  key_links:
    - from: "src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs"
      to: "src/AkariToolbox.Framework/Services/ILogConsoleService.cs"
      via: "Per-handler GetStateAsync continuation's catch block routes exceptions to ILogConsoleService.Log instead of letting them escape unobserved"
      pattern: "ILogConsoleService"
  prohibitions: []
---

<objective>
Close out the phase: verify the full 32-tweak catalog (built across Plans 01-01, 01-04, 01-05, 01-06 with zero shared-file coordination) actually holds together as one coherent, correctly-ordered, resilient page, and add the error-handling polish that only matters once there are 31 more handlers that could individually fail. Because the tracer's `AkariOSTweaksViewModel`/`AkariOSTweaksPage` were designed in Plan 01-01 to enumerate `catalog.Handlers` dynamically, no new wiring code is needed for the other 31 tweaks to appear — this plan is verification + resilience, not feature-building.

Purpose: prove the phase's TWEAKS-01/TWEAKS-03/APP-03/APP-05 success criteria hold across the assembled whole, not just per-plan in isolation.
Output: an automated ordering-invariant test, resilient per-handler error handling, and a verified, fully-populated Akari OS Tweaks page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-akari-os-tweaks/01-CONTEXT.md
@.planning/phases/01-foundation-akari-os-tweaks/01-RESEARCH.md
@.planning/phases/01-foundation-akari-os-tweaks/01-PATTERNS.md
@src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs
@src/AkariToolbox.App/Services/TweakCatalog.cs
@src/AkariToolbox.App/Services/ITweakHandler.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Order-uniqueness regression test + per-handler error resilience</name>
  <files>src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs, src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs</files>
  <read_first>src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs, src/AkariToolbox.App/Services/ITweakHandler.cs</read_first>
  <behavior>
    - Test: resolving IEnumerable&lt;ITweakHandler&gt; from a DI container built via AddAkariSystemPrimitives()+AddTweakHandlers() yields exactly 32 handlers.
    - Test: the 32 handlers' Order values, sorted ascending, are exactly [0,1,2,...,31] with no duplicates and no gaps.
    - Test: sorting handlers by Order and taking their Key values produces the exact sequence wifi, tsx, actioncenter, dep, clipboard, bluetooth, bootmenu, vpn, ntfsenc, fso, notifications, prefetch, cdrom, spooler, nolazy, uacadmin, vr, uac, startmenu, hyperv, vbs, wallpaperq, mpo, transparency, lockscreen, animations, dcom, nvme, largecache, sysprofile, defender, mitigation.
    - Test: a fake ITweakHandler whose GetState() throws does not propagate out of AkariOSTweaksViewModel's construction/read path (assert the ViewModel's async-read continuation catches and does not rethrow — this may require a small internal seam/overload for testability if the constructor is not directly unit-testable against a fake DI container; use a fake ITweakCatalog if that is more testable than reaching through real DI).
  </behavior>
  <action>
    Create `src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs` (xUnit, matching the existing `AkariToolbox.Tests` project conventions) implementing the four behaviors above: build a real `IServiceCollection` with `AddMvvmFramework()`, `AddAkariSystemPrimitives()`, `AddTweakHandlers()`, resolve `IEnumerable<ITweakHandler>`, and assert count/Order/Key-sequence exactly as specified. For the error-resilience test, either construct a minimal fake `ITweakHandler` (`GetState()` throws `InvalidOperationException`) and a fake/mock `ITweakCatalog`/`ILogConsoleService` to drive `AkariOSTweaksViewModel` directly, or — if `AkariOSTweaksViewModel`'s current per-handler read logic is embedded inline in its constructor and not separately unit-testable — extract the per-handler "read live state, catch, log, default to false" logic into a small internal/testable method (e.g. `private async Task<bool> TryGetStateAsync(ITweakHandler handler)` returning `false` and calling `_log.Log(...)` on any exception) so this task's test can call it directly without standing up the full WinUI page.

    Update `AkariOSTweaksViewModel`'s constructor read-loop (built in Plan 01-01 Task 1) to route through this new `TryGetStateAsync` helper instead of a bare `catalog.GetStateAsync(handler.Key)` call, so a throwing handler's exception is caught, logged via the constructor-injected `ILogConsoleService` (added to this ViewModel's constructor in Plan 01-02 Task 3) with a message like `$"[TWEAK] {handler.Key} GetState failed: {ex.Message}"`, and the corresponding `TweakItem.IsOn` is set to `false` rather than left unset or crashing the async continuation.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~TweakHandlerOrdering"</automated>
  </verify>
  <done>All 4 ordering/resilience tests pass; a throwing handler no longer risks crashing the Tweaks page.</done>
  <acceptance_criteria>
    - `dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~TweakHandlerOrdering"` passes with 4/4 tests green.
    - Resolved `IEnumerable<ITweakHandler>` count equals exactly `32`.
    - Sorted `Order` values equal `[0..31]` with no gaps/duplicates.
    - A fake handler whose `GetState()` throws does not propagate past `TryGetStateAsync` — the corresponding item defaults to `IsOn=false` and `ILogConsoleService.Lines` contains a matching error entry.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full-catalog D-03/APP-03 verification pass + elevated launch check</name>
  <files>src/AkariToolbox.App/Services/TweakCatalog.cs</files>
  <read_first>src/AkariToolbox.App/Services/TweakCatalog.cs</read_first>
  <action>
    Run the repo-wide D-03 anti-pattern grep across the ENTIRE `TweakHandlers` directory and `TweakCatalog.cs` (not just per-batch as prior plans did): search for `HasState`, `SaveState`, `ClearState`, and the literal path `Software\AkariTool` (predecessor's old hive) — confirm zero matches outside `DefenderTweakHandler.cs`'s explicitly-scoped, D-01-exempt `Software\AkariToolbox\DefenderState` key (which is a DIFFERENT path/namespace, not the forbidden one, and is a deliberate, documented exception per Plan 01-06). If any match is found in a non-Defender file, fix it before proceeding (this should not happen if Plans 01-04/01-05 were followed correctly — this is a regression backstop, not expected work).

    Re-verify APP-03: confirm no file matching `Themes/Colors.xaml`, `Themes/Controls.xaml`, or any WPF-UI-style resource dictionary exists anywhere under `src/`, and `MainWindow.xaml.cs` still contains `SystemBackdrop = new MicaBackdrop()` unchanged from the framework's original (Plans 01-02/01-03's MainWindow edits should not have touched this line — confirm it wasn't accidentally removed).

    Build and launch the app elevated for a final full-catalog smoke test.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <automated>grep -rn "HasState\|SaveState\|ClearState" src/AkariToolbox.App/Services/TweakHandlers/ src/AkariToolbox.App/Services/TweakCatalog.cs || echo "clean"</automated>
    <automated>grep -rln "Themes/Colors.xaml\|Themes/Controls.xaml" src/ || echo "no WPF-UI theme files present"</automated>
    <human-check>Launch the app elevated. Confirm the Akari OS Tweaks page shows exactly 32 toggle rows. Spot-check 3 tweaks against real system state via a separate elevated terminal: (1) "Disable Bluetooth" toggle state matches `reg query "HKLM\SYSTEM\CurrentControlSet\Services\bthserv" /v Start`; (2) "Disable Print Spooler" toggle state matches `reg query "HKLM\SYSTEM\CurrentControlSet\Services\Spooler" /v Start`; (3) "Enable Process Mitigation" toggle state matches `reg query "HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Memory Management" /v FeatureSettingsOverride`. Flip one non-Defender, non-destructive tweak (e.g. "Disable Action Center") on then off, and confirm the registry value round-trips to its real original value both times. Confirm the Home page and nav sidebar still render correctly (5 cards/5 nav entries, same enabled/disabled split as Plan 01-01).</human-check>
  </verify>
  <done>Zero D-03 anti-pattern matches outside the documented Defender exception; no WPF-UI theme files present; Mica backdrop intact; all 32 tweaks render and spot-checked toggles match real registry state.</done>
  <acceptance_criteria>
    - `grep -rn "HasState\|SaveState\|ClearState" src/AkariToolbox.App/Services/TweakHandlers/ src/AkariToolbox.App/Services/TweakCatalog.cs` returns no matches.
    - `grep -rln "Themes/Colors.xaml\|Themes/Controls.xaml" src/` returns no matches.
    - `grep -n "SystemBackdrop = new MicaBackdrop()" src/AkariToolbox.App/MainWindow.xaml.cs` returns exactly one match.
    - Elevated manual launch: Akari OS Tweaks page shows exactly 32 rows; 3 spot-checked tweaks match `reg query` output; Home (5 cards/1 enabled) and nav (5 entries/2 enabled) unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full assembled 32-handler catalog | Same elevated-process-to-registry/service boundary established in every prior plan of this phase — this plan only verifies the assembly, it introduces no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-15 | Information Disclosure | AkariOSTweaksViewModel logging handler exceptions (including registry paths) to ILogConsoleService | low | accept | Log content stays local to the same elevated user's own visible console; exception messages describe this app's own known registry paths, not sensitive external data |
| T-01-16 | Denial of Service | A single throwing handler previously risking a full page-load crash across all 32 tweaks | medium | mitigate | Task 1's TryGetStateAsync isolation ensures one handler's failure cannot prevent the other 31 from rendering correctly |
</threat_model>

<verification>
- `dotnet test` passes the new ordering/resilience test suite.
- `dotnet build` succeeds.
- Both grep verification commands return clean.
- Elevated manual launch confirms all 32 tweaks render with correct live state (human-check).
</verification>

<success_criteria>
The full 32-tweak Akari OS Tweaks page works end-to-end: correctly ordered, D-03-compliant, resilient to a single handler's failure, and verified against real system state on a real elevated launch — closing out Phase 1's Success Criteria #3 (TWEAKS-01/TWEAKS-03) and re-confirming #5 (APP-03/APP-05) hold across the fully assembled phase.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-akari-os-tweaks/01-07-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `TweakHandlerOrderingTests` (xUnit test class)
- `AkariOSTweaksViewModel.TryGetStateAsync` (new private/internal helper method)

## Flagged Assumptions (edge-probe items resolved as inapplicable, not force-fit; `must_haves.prohibitions: []` explained below)

- **Full-catalog adjacency** ("when two things are exactly equal or just touch, do they merge, collide, or separate?"): resolved by the `Order`-uniqueness/no-gaps assertion itself (`[0..31]` with no duplicates) — this test IS the adjacency probe for this domain, not a separate predicate to add.
- **Full-catalog empty/null (zero-handlers-registered)**: already covered as a `backstop`-verification truth in Plan 01-01's must_haves (empty `ObservableCollection`, no crash) — not re-tested here to avoid duplicate coverage; this plan's own ordering test would additionally fail loudly (`count != 32`) if registration silently dropped a handler, which functions as an equivalent regression backstop at the full-catalog level.
- **APP-03 encoding/empty-input**: n/a — the APP-03 re-verification in Task 2 is a static resource-file-absence check (no `Themes/Colors.xaml`/`Themes/Controls.xaml`) and a `MicaBackdrop` presence check, neither of which has an input-encoding surface.
- **`prohibitions: []` rationale**: this plan's actual prohibitions (no D-03 anti-pattern drift across the full assembled tree, no accidental removal of the Mica backdrop line) are enforced as automated `<verify>` greps in Task 2 rather than restated as prose — the stronger, machine-checked form of the same constraint.
