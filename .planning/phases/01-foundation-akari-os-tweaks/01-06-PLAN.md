---
phase: 01-foundation-akari-os-tweaks
plan: 06
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/AkariToolbox.App/Services/IPostInstallService.cs
  - src/AkariToolbox.App/Services/PostInstallService.cs
  - src/AkariToolbox.Framework/ServiceCollectionExtensions.Primitives.cs
  - src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs
  - src/AkariToolbox.Tests/PostInstallIntegrityTests.cs
autonomous: true
requirements: [TWEAKS-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "DefenderTweakHandler ports SetDefenderAsync, IsDefenderTamperProtectionOn, DefenderScheduleCleanup, DefenderRunElevatedPsFileAsync, DefenderRunElevatedPsAsync, DefenderRunAsTrustedInstallerAsync, DefenderBuildServiceBat byte-for-byte from TweakService.cs — the only new code is the thin ITweakHandler wrapper and the ILogConsoleService-routed logging (D-01, this task's citation)."
    - "The Tamper Protection early-return path (Tamper Protection ON -> log guidance, return without proceeding) is preserved exactly — no partial state change occurs when Tamper Protection blocks the disable attempt."
    - "IPostInstallService's EnsureDefenderFilesAsync/EnsureMinSudoAsync/EnsurePostInstallAsync ports the predecessor's file-presence-then-download fallback chain (AllFiles manifest, LocalRoot/MinSudoPath/PowerRunPath/NoDefenderPath constants, MinSudoPresent/PowerRunPresent/NoDefenderPresent/IsFullyInstalled) so the Defender tweak does not throw/fail at runtime on a machine without a pre-populated C:\\PostInstall\\ (RESEARCH Pitfall 5)."
    - "HttpClient usage for the PostInstall download goes through IHttpClientFactory registered in DI (services.AddHttpClient()), not a bare `new HttpClient()` per call, per CLAUDE.md's forbidden-pattern rule."
    - "Before copying NoDefender.cab into C:\\Windows or executing DisableDefender.ps1, DefenderTweakHandler verifies each file's live SHA256 digest against a pinned expected hash via IPostInstallService.VerifyFileSha256Async — a mismatch aborts the disable flow before any file is copied/executed, with a visible log message (T-01-SC mitigation, added in revision per checker BLOCKER finding; the full ~130-entry DOWNLOADS-01 asset-manifest SHA256 pass remains Phase 4 scope, only the two Defender-critical files are covered here)."
  artifacts:
    - path: "src/AkariToolbox.App/Services/PostInstallService.cs"
      provides: "Minimal PostInstall asset-presence/download-fallback service — Defender's dependency only, not the full Downloads-page feature (that is Phase 4/DOWNLOADS-01) — plus the new, not-ported-from-predecessor VerifyFileSha256Async integrity primitive (T-01-SC mitigation)"
    - path: "src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs"
      provides: "The two-phase Defender-disable workflow (TWEAKS-02), ported as a direct carry-over per D-01, gated by a new pre-execution SHA256 integrity check on the two Defender-critical downloaded files"
    - path: "src/AkariToolbox.Tests/PostInstallIntegrityTests.cs"
      provides: "Unit coverage for VerifyFileSha256Async's match/mismatch/missing-file behavior, independent of the real downloaded PostInstall assets"
  key_links:
    - from: "src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs"
      to: "src/AkariToolbox.App/Services/PostInstallService.cs"
      via: "SetDefenderAsync calls postInstall.EnsureDefenderFilesAsync()/EnsureMinSudoAsync() before proceeding"
      pattern: "EnsureDefenderFilesAsync|EnsureMinSudoAsync"
    - from: "src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs"
      to: "src/AkariToolbox.App/Services/PostInstallService.cs"
      via: "SetDefenderAsync's disable path calls postInstall.VerifyFileSha256Async for NoDefender.cab and DisableDefender.ps1 before copying/executing either file — aborts on mismatch"
      pattern: "VerifyFileSha256Async"
    - from: "src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs"
      to: "src/AkariToolbox.Framework/Services/ILogConsoleService.cs"
      via: "Every App.Tool?.Log(msg) call site is replaced with the constructor-injected ILogConsoleService.Log(msg)"
      pattern: "ILogConsoleService"
  prohibitions:
    - "MUST NOT let the Defender-disable RunOnce cleanup script (Phase 2, executing after reboot outside the app's process lifetime) complete with no user-visible indication that Defender was actually disabled — Phase 1 log messages must explicitly tell the user Phase 1 is complete, a restart is required, and Phase 2 finishes automatically on next login (this is already present in the predecessor's own log lines — this task must not drop them during the port)."
---

<objective>
Port the two-phase Windows Defender disable/re-enable workflow (TWEAKS-02) as a direct, byte-for-byte carry-over of the predecessor's `SetDefenderAsync` call graph — per D-01, this specific code path is explicitly NOT decomposed into the `ITweakHandler`/primitive architecture the other 31 tweaks use, and is NOT refactored or "improved" in any way. Because `SetDefenderAsync` depends on `PostInstallService.EnsureDefenderFilesAsync()`/`EnsureMinSudoAsync()`, this plan also ports the minimal subset of `PostInstallService` those two calls need (RESEARCH Pitfall 5) — not the full Downloads-page asset mirror, which is Phase 4/DOWNLOADS-01 scope.

Purpose: TWEAKS-02 requires this exact workflow with its exact warnings, ported verbatim on explicit, twice-repeated user instruction (D-01).
Output: `DefenderTweakHandler` (the 32nd and final tweak in the Akari OS Tweaks list) and a minimal `PostInstallService`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-akari-os-tweaks/01-CONTEXT.md
@.planning/phases/01-foundation-akari-os-tweaks/01-RESEARCH.md
@.planning/phases/01-foundation-akari-os-tweaks/01-PATTERNS.md

# Predecessor source to port byte-for-byte (read-only, do not modify)
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Services/TweakService.cs
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Services/PostInstallService.cs

# This phase's already-built primitives (read, do not modify)
@src/AkariToolbox.Framework/Services/ILogConsoleService.cs
@src/AkariToolbox.App/Services/ITweakHandler.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Minimal IPostInstallService port + SHA256 integrity primitive</name>
  <files>src/AkariToolbox.App/Services/IPostInstallService.cs, src/AkariToolbox.App/Services/PostInstallService.cs, src/AkariToolbox.Framework/ServiceCollectionExtensions.Primitives.cs, src/AkariToolbox.Tests/PostInstallIntegrityTests.cs</files>
  <read_first>C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Services/PostInstallService.cs (full file — AllFiles manifest lines 27-176, EnsureMinSudoAsync/EnsureDefenderFilesAsync/EnsurePostInstallAsync lines 196-240+)</read_first>
  <action>
    Create `src/AkariToolbox.App/Services/IPostInstallService.cs` with an instance-based interface (the predecessor's `PostInstallService` is `static`; convert to an injectable singleton so it fits this app's DI-first convention — this is a structural adaptation, not a behavior change): `string LocalRoot { get; }`, `string MinSudoPath { get; }`, `string PowerRunPath { get; }`, `string NoDefenderPath { get; }`, `bool MinSudoPresent { get; }`, `bool PowerRunPresent { get; }`, `bool NoDefenderPresent { get; }`, `bool IsFullyInstalled { get; }`, `Task<bool> EnsureMinSudoAsync();`, `Task<bool> EnsureDefenderFilesAsync();`, `Task<bool> EnsurePostInstallAsync();`, plus one NEW member not present in the predecessor: `Task<bool> VerifyFileSha256Async(string filePath, string expectedHexSha256);`.

    Create `PostInstallService.cs` implementing it: constructor-injects `IHttpClientFactory httpClientFactory` and `ILogConsoleService log` (replacing the predecessor's static `App.Tool?.Log` calls and the static lazily-constructed `HttpClient` field — call `httpClientFactory.CreateClient("PostInstall")` per request instead of caching a static field, per CLAUDE.md's `IHttpClientFactory` requirement). Port `LocalRoot = @"C:\PostInstall"`, `MinSudoPath/PowerRunPath/NoDefenderPath` (same relative paths as the predecessor's `_minSudoPath`/`_powerRunPath`/`_noDefenderPath`), the full `AllFiles` string array (copy verbatim from `PostInstallService.cs:27-176` — all ~130 entries, unchanged), `MinSudoPresent`/`PowerRunPresent`/`NoDefenderPresent`/`IsFullyInstalled` (unchanged `File.Exists` logic), and `EnsureMinSudoAsync`/`EnsureDefenderFilesAsync`/`EnsurePostInstallAsync` (unchanged control flow — `EnsureDefenderFilesAsync` checks the 5 Defender-specific files listed in the predecessor at lines 204-211 before falling through to `EnsurePostInstallAsync`'s full-manifest download loop). Register the named HttpClient via `services.AddHttpClient("PostInstall", c => { c.DefaultRequestHeaders.Add("User-Agent", "AkariToolbox"); c.Timeout = TimeSpan.FromMinutes(10); });` and `services.AddSingleton<IPostInstallService, PostInstallService>();` — add both lines to `AddAkariSystemPrimitives` in `ServiceCollectionExtensions.Primitives.cs` (extend the existing method, do not create a new one).

    Additionally, implement the new `VerifyFileSha256Async(string filePath, string expectedHexSha256)` integrity primitive — this closes BLOCKER T-01-SC from phase-plan review (the predecessor never verified `NoDefender.cab`/`DisableDefender.ps1` before execution; this method is net-new, not ported, and does not touch any of the byte-for-byte ported `Ensure*`/Defender logic). Implement using `System.Security.Cryptography.SHA256` (in-box BCL, no new package — STACK.md already establishes this for downloaded-asset verification): if `filePath` does not exist, log `log.Log($"[POSTINSTALL] Integrity check: file not found: {filePath}")` and return `false` (never throw — this is a gate, not an exceptional path); otherwise open a `FileStream`, compute the digest via `await SHA256.HashDataAsync(stream)`, convert to a lowercase hex string via `Convert.ToHexStringLower(hash)`, and return `string.Equals(computed, expectedHexSha256, StringComparison.OrdinalIgnoreCase)`, logging a mismatch (`log.Log($"[POSTINSTALL] Integrity check FAILED for {filePath}")`) before returning `false`. Deliberately generic (any file/hash pair, not Defender-specific) so it is directly unit-testable without needing the real downloaded PostInstall assets.

    Create `src/AkariToolbox.Tests/PostInstallIntegrityTests.cs` (xUnit, matching `AkariToolbox.Tests` conventions): write a temp file with known bytes, compute its real SHA256 via the same BCL API used by the production code, and assert `VerifyFileSha256Async(tempFile, correctHex)` returns `true`; assert `VerifyFileSha256Async(tempFile, new string('0', 64))` returns `false`; assert `VerifyFileSha256Async(nonExistentPath, anyHex)` returns `false` without throwing.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~PostInstallIntegrity"</automated>
  </verify>
  <done>IPostInstallService compiles, registers via AddHttpClient/AddSingleton, its Ensure* methods match the predecessor's control flow exactly, and VerifyFileSha256Async correctly matches/mismatches/handles a missing file per the unit tests.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - `AllFiles` array in `PostInstallService.cs` contains the same number of entries as the predecessor's `PostInstallService.cs` (~130).
    - `grep -n "new HttpClient()" src/AkariToolbox.App/Services/PostInstallService.cs` returns no match (uses `IHttpClientFactory.CreateClient("PostInstall")` exclusively).
    - `EnsureDefenderFilesAsync()` checks all 5 Defender-specific files before falling through to `EnsurePostInstallAsync()`, matching `TweakService.cs:204-211`.
    - `dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~PostInstallIntegrity"` passes 3/3 tests (match / mismatch / missing-file).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: DefenderTweakHandler — byte-for-byte port of SetDefenderAsync</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs</files>
  <read_first>C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Services/TweakService.cs lines 806-1038 (full Defender call graph: SetDefender, SetDefenderAsync, DefenderScheduleCleanup, IsDefenderTamperProtectionOn, DefenderBuildServiceBat, DefenderRunElevatedPsFileAsync, DefenderRunElevatedPsAsync, DefenderRunAsTrustedInstallerAsync)</read_first>
  <reversibility rating="one-way">D-01 (CONTEXT.md, already an explicit, twice-repeated user directive — the door is already walked through, this rating is carried for the record per the reversibility taxonomy, not gated with a fresh checkpoint): porting the Defender workflow as an unrefactored carry-over, rather than decomposing it into ITweakHandler now, means any future decomposition (tracked as SEC-01, v2) is a deliberate migration, not a drop-in swap.</reversibility>
  <action>
    Create `DefenderTweakHandler.cs` in namespace `AkariToolbox.App.Services.TweakHandlers`, implementing `ITweakHandler`: Key "defender", Title "Disable Defender", Description "Toggle Windows Defender On or Off", Order 30. Constructor-injects `IPostInstallService postInstall`, `ILogConsoleService log`, `IRegistryService registry`. Directly above the class declaration, add the code comment `// ITweakHandler here is a thin routing wrapper only; SetDefenderAsync's internals below are an unmodified port per D-01 — see 01-RESEARCH.md 'Defender carry-over scope'.` (per D-01, this ITweakHandler implementation is an explicit, research-documented exemption from the primitive-layer decomposition pattern the other 31 handlers follow — this citation prevents a future auditor from independently, and incorrectly, flagging the wrapper itself as a D-01 violation).

    Add two pinned-hash constants (NOT ported from the predecessor — new, Phase-1-scoped security addition closing BLOCKER T-01-SC from phase-plan review): `private const string ExpectedNoDefenderCabSha256 = "<64-hex-char SHA256 digest>";` and `private const string ExpectedDisableDefenderPs1Sha256 = "<64-hex-char SHA256 digest>";`. Because these two files are fetched at runtime from the pinned GitHub PostInstall repo (RESEARCH Pitfall 5) and their exact bytes are not available during planning, compute the real digests once during implementation — after `EnsureDefenderFilesAsync()` has downloaded them to `C:\PostInstall\` on a real dev machine, run `Get-FileHash -Algorithm SHA256 <path>` (PowerShell, in-box) against the actual `NoDefender.cab` and `Defender\DisableDefender.ps1`, and pin the resulting hex digests as these two constants with a code comment recording the date computed and the exact paths hashed (e.g. `// SHA256 pinned <date> from C:\PostInstall\NoDefender.cab`). This is a trust-on-first-use pin consistent with STACK.md's SHA256-verification guidance, narrowly scoped to the two Defender-critical files (not the full ~130-entry AllFiles manifest, which stays Phase 4/DOWNLOADS-01 scope).

    Port these private members verbatim (same field/method names and logic, only the logging mechanism changes from `App.Tool?.Log` to the injected `log.Log`):
    - `DefenderServices` (16-entry string array: `MsSecCore, MsSecFlt, MsSecWfp, SecurityHealthService, Sense, WdBoot, WdFilter, WdNisDrv, WdNisSvc, WinDefend, wscsvc, MDCoreSvc, SgrmAgent, SgrmBroker, webthreatdefsvc, webthreatdefusersvc`).
    - `DefenderScheduledTasks` (4-entry string array of scheduled task paths under `\Microsoft\Windows\Windows Defender\`).
    - `IsDefenderTamperProtectionOn()`: reads LocalMachine `SOFTWARE\Microsoft\Windows Defender\Features`, value `TamperProtection`; returns true unless the value is exactly the integer `4` (via `registry.GetValue`, wrapped in try/catch returning true on any exception — exact predecessor semantics, `TweakService.cs:976-986`).
    - `DefenderBuildServiceBat(int startValue)`: builds one `Reg.exe add "HKLM\SYSTEM\ControlSet001\Services\{svc}" /v "Start" /t REG_DWORD /d "{startValue}" /f` line per `DefenderServices` entry.
    - `DefenderScheduleCleanup()`: writes the Phase-2 `.bat` to `{postInstall.LocalRoot}\Defender\AkariDefenderCleanup.bat` (disable real-time monitoring via `Set-MpPreference`, run every `DefenderBuildServiceBat(4)` line through `"{postInstall.PowerRunPath}" /SW:0 "{cmd.exe path}" /c {line}`, remove `SecurityHealth` from the Run key, disable/replace `smartscreen.exe`, write SmartScreen/CI/DeviceGuard registry keys, disable each `DefenderScheduledTasks` entry via `schtasks.exe /change /disable`, self-delete the `RunOnce` value and the `.bat` file itself) — port every line from `TweakService.cs:899-974` unchanged, then write the `RunOnce` registry value `HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\RunOnce\AkariDefenderCleanup` pointing at `"{cmd.exe path}" /c "{batPath}"`.
    - `DefenderRunElevatedPsFileAsync(string ps1Path)`: `Process.Start` with `FileName="powershell.exe"`, `Arguments="-ExecutionPolicy Bypass -NonInteractive -NoLogo -NoProfile -File \"{ps1Path}\""`, `UseShellExecute=true`, `Verb="runas"` — this IS a second elevation prompt even though the app is already elevated; this is intentional pre-existing predecessor behavior (RESEARCH "Known Threat Patterns" table) — do not remove or "fix" the redundant `runas`, add a one-line code comment noting it is intentional so a future reviewer does not silently drop it.
    - `DefenderRunElevatedPsAsync(string command)`: `Process.Start` with `powershell.exe -NonInteractive -NoLogo -NoProfile -C "{command}"`, `UseShellExecute=false`, `CreateNoWindow=true`.
    - `DefenderRunAsTrustedInstallerAsync(IEnumerable<string> commands)`: writes a temp `.bat`, runs it via `"{postInstall.MinSudoPath}" --NoLogo --TrustedInstaller --Privileged cmd /c "{tmpBat}"` with `UseShellExecute=true`, deletes the temp file in a `finally`.

    Implement `SetDefenderAsync(bool disable)` (private async method) porting `TweakService.cs:830-897` exactly: call `postInstall.EnsureDefenderFilesAsync()` (disable) / `EnsureMinSudoAsync()` (enable); if files aren't ready, `log.Log("[DEFENDER] Could not obtain required files. Check your internet connection and try again.")` and return without proceeding. On the disable path, immediately after `EnsureDefenderFilesAsync()` confirms files are ready — and BEFORE the `IsDefenderTamperProtectionOn()` check — call `await postInstall.VerifyFileSha256Async(postInstall.NoDefenderPath, ExpectedNoDefenderCabSha256)` and `await postInstall.VerifyFileSha256Async(Path.Combine(postInstall.LocalRoot, "Defender", "DisableDefender.ps1"), ExpectedDisableDefenderPs1Sha256)`; if either returns `false`, `log.Log("[DEFENDER] ERROR: Integrity check failed for a downloaded PostInstall asset — refusing to proceed. Delete C:\\PostInstall\\Defender and retry to re-download.")` and return without proceeding — same early-return shape as the Tamper Protection gate below, no partial state change, no file copy, no script execution. This integrity gate wraps the ported call graph as a new pre-condition; it does not modify any of the byte-for-byte ported logic (D-01 compliant — additive wrapper code only). Next, check `IsDefenderTamperProtectionOn()` — if true, log the exact 4-line guidance (`"[DEFENDER] ERROR: Tamper Protection is ON."`, `"[DEFENDER] Go to: Windows Security -> Virus & threat protection"`, `"[DEFENDER]   -> Manage settings -> Tamper Protection -> Off"`, `"[DEFENDER] Then try again."`) and return WITHOUT proceeding further (no partial state change — this is the truth this plan's must_haves cites). If Tamper Protection is off, copy `postInstall.NoDefenderPath` to `C:\Windows\NoDefender.cab` (overwrite), run `DefenderRunElevatedPsFileAsync(Path.Combine(postInstall.LocalRoot, @"Defender\DisableDefender.ps1"))`, run `DefenderScheduleCleanup()`, write the app's own Defender-state flag (see below), and log `"[DEFENDER] Phase 1 complete. Please restart now."` + `"[DEFENDER] On next login, Phase 2 will finish disabling Defender automatically."`. Enable path: run `DefenderRunElevatedPsAsync(...)` to remove the NoDefender package via `Remove-WindowsPackage`, run `DefenderRunAsTrustedInstallerAsync(DefenderBuildServiceBat(startValue: 2))` to restore services, clear the app's Defender-state flag, log `"[DEFENDER] Defender re-enabled. Restart required."`. Wrap the whole method in try/catch logging `$"[DEFENDER] ERROR: {ex.Message}"` on any exception (never let an exception escape unlogged — this closes out the APP-05/silent-swallow prohibition for this specific handler too).

    `GetState()`/`SetState(bool)` (the `ITweakHandler` contract, per D-01's explicit exemption from D-03's live-registry-read rule — RESEARCH's Live-state-reader table): since this app deliberately never creates the predecessor's `HKCU\Software\AkariTool` hive (Pitfall 4, applies to every other handler), give Defender its OWN small, explicitly-scoped flag instead of reusing that exact path: read/write a single DWord value `DisableDefender` under CurrentUser `Software\AkariToolbox\DefenderState` via the injected `IRegistryService` (`GetValue`/`SetValue`/`DeleteValue`). `GetState()` returns true when that value is present and non-zero. `SetState(bool disable)` sets the flag to `1` (disable path) or deletes it (enable path) AFTER `SetDefenderAsync` completes its synchronous portion, then calls `_ = SetDefenderAsync(disable);` fire-and-forget — mirroring the predecessor's own `SetDefender(bool disable) => _ = SetDefenderAsync(disable);` shape exactly (`TweakService.cs:828`); do NOT await inside `SetState` since `ITweakHandler.SetState` is a synchronous contract member and the predecessor's own top-level dispatch is already fire-and-forget for this specific tweak.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <human-check>On a real Windows 10/11 test machine with Tamper Protection currently ON: launch the app elevated, navigate to Akari OS Tweaks, flip the "Disable Defender" toggle, and confirm the log console shows the exact 4-line Tamper Protection guidance and the toggle does NOT flip to a disabled-looking state (no partial mutation). Turn Tamper Protection OFF (Windows Security -> Virus & threat protection -> Manage settings), ensure the pinned `ExpectedNoDefenderCabSha256`/`ExpectedDisableDefenderPs1Sha256` constants match the actually-downloaded files' real hashes (compute via `Get-FileHash` if unsure), flip the toggle again, and confirm: NoDefender.cab is copied to C:\Windows, a second elevated PowerShell window/prompt appears (the intentional redundant runas), the log shows "Phase 1 complete. Please restart now." and the Phase 2 auto-completion message, and a RunOnce registry value named AkariDefenderCleanup exists under HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\RunOnce pointing at a generated .bat under C:\PostInstall\Defender\. Separately, temporarily corrupt one byte of the local `NoDefender.cab` (or pass a deliberately wrong pinned constant) and confirm the toggle logs the integrity-check-failed message and neither copies nor executes the file — then restore the correct file/constant. Do NOT actually reboot the test machine as part of this check unless it is a disposable VM.</human-check>
  </verify>
  <done>DefenderTweakHandler compiles, appears as the 32nd tweak in the catalog, the SHA256 integrity gate runs before any file copy/execution, Tamper Protection early-return preserved exactly, Phase 1 completes with the correct log messages and RunOnce write on a real machine with Tamper Protection off and matching pinned hashes.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - `DefenderTweakHandler.Order == 30`.
    - `grep -n "VerifyFileSha256Async" src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs` returns matches for both `NoDefenderPath` and `DisableDefender.ps1`, appearing before the `NoDefender.cab` copy step in source order.
    - `grep -n "ITweakHandler here is a thin routing wrapper" src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs` returns a match (D-01 citation comment present).
    - Real-machine test with Tamper Protection ON: log shows the exact 4 guidance lines and no NoDefender.cab copy occurs.
    - Real-machine test with Tamper Protection OFF and matching pinned hashes: `HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\RunOnce\AkariDefenderCleanup` exists after the disable flow completes.
    - `grep -n "App.Tool" src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs` returns no match.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elevated app -> second `runas` elevation for `powershell.exe -File DisableDefender.ps1` | Intentional predecessor behavior, ported as-is per D-01 |
| Elevated app -> unverified download of `C:\PostInstall\Defender\NoDefender.cab`/`.ps1` from a GitHub-hosted repo | Pre-existing predecessor characteristic (HTTPS, pinned repo, no checksum verification) — SHA256 verification is DOWNLOADS-01/Phase-4 scope per STACK.md, not fixed in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `C:\PostInstall\Defender\NoDefender.cab`/`DisableDefender.ps1` executed under an elevated, TrustedInstaller-level process (`MinSudo.exe --TrustedInstaller`) if the GitHub-hosted PostInstall repo is ever compromised or DNS-spoofed | high | mitigate | Mitigated in Task 1/Task 2 of this plan (added in revision per checker BLOCKER finding, satisfies `.planning/config.json`'s `security_asvs_level: 1`/`security_block_on: high` gate): `IPostInstallService.VerifyFileSha256Async` computes a live SHA256 digest of `NoDefender.cab` and `DisableDefender.ps1` and compares each against a pinned expected hash (`DefenderTweakHandler.ExpectedNoDefenderCabSha256`/`ExpectedDisableDefenderPs1Sha256`, computed once via `Get-FileHash` during implementation and checked into source as a trust-on-first-use pin); `SetDefenderAsync`'s disable path aborts with a visible log message and no file copy/execution if either check fails. This is additive wrapper code around the ported call graph, not a modification of the ported Defender logic itself (D-01 compliant). The full ~130-entry `AllFiles` manifest SHA256 pass remains Phase 4/DOWNLOADS-01 scope — only the two Defender-critical files are covered here. |
| T-01-13 | Tampering | Direct disabling of Windows Defender, the OS's own malware protection | high | mitigate | Explicit, twice-warned, user-initiated toggle with Tamper Protection as a hard precondition gate (the OS's own safety mechanism) — the app cannot bypass Tamper Protection, it only proceeds when the user has already turned it off themselves via Windows Security |
| T-01-14 | Elevation of Privilege | Second `runas` elevation prompt for an already-elevated process | low | accept | Intentional, ported as-is per D-01 — not a new vulnerability, a UX/trust-signal quirk inherited from the predecessor; documented inline in code per this task's action |
</threat_model>

<verification>
- `dotnet build AkariToolbox.slnx -c Debug` succeeds.
- `dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~PostInstallIntegrity"` passes (SHA256 match/mismatch/missing-file behavior).
- Real-machine manual test confirms the Tamper-Protection-on early return, the SHA256 integrity gate rejecting a tampered/mismatched file, and a full Phase 1 run when Tamper Protection is off and hashes match (human-check above).
- `grep -rn "AppTemplate\|App\\.Tool" src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs` returns no matches (confirms the `App.Tool?.Log` static call sites were fully replaced with the injected `ILogConsoleService`).
</verification>

<success_criteria>
The two-phase Defender-disable workflow works end-to-end on a real Windows machine, ported byte-for-byte per D-01, gated by a SHA256 integrity check on the two downloaded Defender-critical files (T-01-SC), with Tamper Protection correctly gating the disable attempt and every step visibly logged.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-akari-os-tweaks/01-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `IPostInstallService` / `PostInstallService` (minimal subset)
- `IPostInstallService.VerifyFileSha256Async` (new, not-ported — T-01-SC integrity gate primitive)
- `PostInstallIntegrityTests` (xUnit coverage for the integrity primitive)
- `DefenderTweakHandler` (32nd and final tweak) — gated by the SHA256 integrity check before file copy/execution
- `AddHttpClient("PostInstall", ...)` DI registration

## Flagged Assumptions (edge-probe items resolved as inapplicable, not force-fit)

- **TWEAKS-02 adjacency** ("when two things are exactly equal or just touch, do they merge, collide, or separate?"): Defender is a single two-phase toggle, not a collection of comparable elements — no merge/collision concept applies.
- **TWEAKS-02 empty/null input**: TWEAKS-02's only input surface is the boolean toggle itself (enable/disable); there is no free-form/empty/null input to the workflow.
- **TWEAKS-02 ordering**: Defender is a single tweak entry (Order 30 in the 32-item list, per Plan 01-01's established convention), not a set of elements whose relative order could be ambiguous.
