---
phase: 01-foundation-akari-os-tweaks
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs
  - src/AkariToolbox.App/Services/TweakHandlers/BcdeditDismTweaks.cs
autonomous: true
requirements: [TWEAKS-01, TWEAKS-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The 4 service-backed handlers (clipboard, bluetooth, cdrom, spooler) read/write via IWindowsServiceController (not raw IRegistryService directly, except cdrom's IMAPI2-key-creation branch which is documented as an intentional exception), keeping the service-primitive boundary RESEARCH's Architectural Responsibility Map calls for."
    - "dep and bootmenu (pure bcdedit, zero registry footprint per D-12) derive GetState() by spawning `bcdedit /enum {current}` via IScriptRunner and parsing the relevant line — no hardcoded/write-only state, no cached flag."
    - "hyperv and vr derive GetState() from their registry portion only (HypervisorEnforcedCodeIntegrity\\Enabled for hyperv; a representative service Start value for vr) — never re-spawn bcdedit/DISM on a read, per RESEARCH Pitfall 6 (avoids a process-spawn-per-toggle-load performance trap)."
    - "None of these 8 handlers read/write HKCU\\Software\\AkariTool or call HasState/SaveState/ClearState — verified by grep across both files."
  artifacts:
    - path: "src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs"
      provides: "4 service-backed ITweakHandler implementations: clipboard, bluetooth, cdrom, spooler"
    - path: "src/AkariToolbox.App/Services/TweakHandlers/BcdeditDismTweaks.cs"
      provides: "4 bcdedit/DISM-hybrid ITweakHandler implementations: dep, bootmenu, hyperv, vr"
  key_links:
    - from: "src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs"
      to: "src/AkariToolbox.Framework/Services/IWindowsServiceController.cs"
      via: "Constructor-injected IWindowsServiceController.GetStartType/SetStartType"
      pattern: "IWindowsServiceController"
    - from: "src/AkariToolbox.App/Services/TweakHandlers/BcdeditDismTweaks.cs"
      to: "src/AkariToolbox.Framework/Services/IScriptRunner.cs"
      via: "DepTweakHandler/BootMenuTweakHandler spawn bcdedit /enum {current} to read state; all 4 handlers spawn bcdedit/DISM to write state"
      pattern: "IScriptRunner|bcdedit"
  prohibitions: []
---

<objective>
Implement the remaining 8 `ITweakHandler`s that are NOT pure registry reads: the 4 service-backed tweaks (clipboard, bluetooth, cdrom, spooler — read/write via the `IWindowsServiceController` primitive from Plan 01-02) and the 4 bcdedit/DISM-hybrid tweaks (dep, bootmenu, hyperv, vr — where at least one side of the read/write pair needs `IScriptRunner`).

Purpose: close out TWEAKS-01/TWEAKS-03 for every tweak whose live state cannot be read via a plain `IRegistryService.GetValue` call alone.
Output: 8 new `ITweakHandler` classes, auto-discovered exactly like Plans 01-04's 22 — no shared-file edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-akari-os-tweaks/01-CONTEXT.md
@.planning/phases/01-foundation-akari-os-tweaks/01-RESEARCH.md
@.planning/phases/01-foundation-akari-os-tweaks/01-PATTERNS.md

# Predecessor write-side source of truth (read-only, do not modify)
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Services/TweakService.cs

# This phase's already-built primitives (read, do not modify)
@src/AkariToolbox.Framework/Services/IWindowsServiceController.cs
@src/AkariToolbox.Framework/Services/IScriptRunner.cs
@src/AkariToolbox.Framework/Services/IRegistryService.cs
@src/AkariToolbox.App/Services/ITweakHandler.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service-backed tweaks (clipboard, bluetooth, cdrom, spooler)</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs</files>
  <read_first>C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Services/TweakService.cs lines 230-259 (SetClipboard, SetBluetooth), lines 413-461 (SetCdrom, SetPrintSpooler)</read_first>
  <action>
    Create `ServiceBackedTweaks.cs` in namespace `AkariToolbox.App.Services.TweakHandlers` with 4 sealed classes implementing `ITweakHandler`:

    1. `ClipboardTweakHandler`: Key "clipboard", Title "Enable Clipboard", Description "Toggle Clipboard service On or Off", Order 4. Constructor-injects `IWindowsServiceController serviceController`. `SetState(bool enable)` calls `serviceController.SetStartType("cbdhsvc", enable ? 2 : 4)`. `GetState()` returns `serviceController.GetStartType("cbdhsvc") == 2`.

    2. `BluetoothTweakHandler`: Key "bluetooth", Title "Disable Bluetooth", Description "Toggle Bluetooth On or Off", Order 5. Constructor-injects `IWindowsServiceController serviceController`. `SetState(bool disable)` writes `Start = disable ? 4 : 3` to all 15 services: `BthA4dp, BthEnum, BthHFEnum, BthLEEnum, BTHMODEM, Microsoft_Bluetooth_AvrcpTransport, BluetoothUserService, BthAvctpSvc, RFCOMM, bthserv, BTAGService, BTHUSB, BTHPORT, BthMini, HidBth` (loop `serviceController.SetStartType(svc, startVal)` for each). `GetState()` returns `serviceController.GetStartType("bthserv") == 4` (representative single-service read, matching the pattern already established for wifi/vpn/vr in this phase per RESEARCH Assumption A3).

    3. `CdromTweakHandler`: Key "cdrom", Title "CDROM", Description "Enable the CDROM service", Order 12. Constructor-injects `IRegistryService registry` directly (not `IWindowsServiceController` — this tweak's enable path re-creates the entire `IMAPI2` service registry key from scratch, which needs `IRegistryService.SetValue`/subkey-creation beyond a simple `Start` DWord toggle). `SetState(bool enable)`: on enable, write LocalMachine `SYSTEM\CurrentControlSet\Services\cdrom` `Start=3` (DWord), then create/populate LocalMachine `SYSTEM\CurrentControlSet\Services\IMAPI2` with `Description="@%SystemRoot%\system32\imapi2.dll,-2"` (ExpandString), `DisplayName="@%SystemRoot%\system32\imapi2.dll,-1"` (ExpandString), `ErrorControl=1` (DWord), `ImagePath="%SystemRoot%\system32\svchost.exe -k imapi"` (ExpandString), `ObjectName="LocalSystem"` (String), `Start=3` (DWord), `Type=32` (DWord), and a `Parameters` subkey with `ServiceDll="%SystemRoot%\system32\imapi2.dll"` (ExpandString) and `ServiceDllUnloadOnStop=1` (DWord); on disable, write `cdrom\Start=4`, `IMAPI2\Start=4`, `IMAPI2FS\Start=4` (all DWord, no key re-creation needed). `GetState()` returns `cdrom\Start == 3`.

    4. `PrintSpoolerTweakHandler`: Key "spooler", Title "Disable Print Spooler", Description "Toggle Print Spooler On or Off", Order 13. Constructor-injects `IWindowsServiceController serviceController`. `SetState(bool disable)` calls `serviceController.SetStartType("Spooler", disable ? 4 : 2)`. `GetState()` returns `serviceController.GetStartType("Spooler") == 4`.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <automated>grep -rn "HasState\|SaveState\|ClearState\|Software\\\\AkariTool" src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs || echo "clean"</automated>
  </verify>
  <done>4 service-backed handlers compile and read/write via IWindowsServiceController (cdrom via IRegistryService directly, documented exception); no anti-pattern matches.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - `grep -c "HasState\|SaveState\|ClearState" src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs` returns `0`.
    - `BluetoothTweakHandler.SetState(true)` writes `Start=4` to all 15 listed Bluetooth service names.
    - `CdromTweakHandler.SetState(true)` creates the `IMAPI2` key with `Type=32` and a `Parameters\ServiceDll` value when the key is absent.
    - Order values in this file are exactly `{4,5,12,13}` with no duplicates.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: bcdedit/DISM-hybrid tweaks (dep, bootmenu, hyperv, vr)</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/BcdeditDismTweaks.cs</files>
  <read_first>C:/Users/isleap/Documents/GitHub/AkariOS-Companion/Services/TweakService.cs lines 215-228 (SetDepNx), lines 261-274 (SetBootMenuPolicy), lines 497-519 (SetVr), lines 561-593 (SetHyperV)</read_first>
  <action>
    Create `BcdeditDismTweaks.cs` in namespace `AkariToolbox.App.Services.TweakHandlers` with 4 sealed classes implementing `ITweakHandler`, all constructor-injecting `IScriptRunner scriptRunner` (and `IRegistryService registry` where a registry-portion read applies):

    1. `DepTweakHandler`: Key "dep", Title "Disable DEP/NX", Description "Toggle Data Execution Prevention", Order 3. `SetState(bool disable)` runs `bcdedit /set NX AlwaysOff` (disable) or `bcdedit /set NX OptIn` (enable) via `scriptRunner.RunProcessAsync("bcdedit", ...).GetAwaiter().GetResult()`. `GetState()` (D-12): run `scriptRunner.RunProcessAsync("bcdedit", "/enum {current}")`, capture its stdout (extend `IScriptRunner`'s existing `RunProcessAsync` usage — since that method currently only returns an exit code per Plan 01-02's design, add a companion `Task<string> RunProcessCaptureOutputAsync(string fileName, string arguments)` to `IScriptRunner`/`ScriptRunner` in this task if the exit-code-only signature is insufficient; implement it by buffering the same `OutputDataReceived` lines into a `StringBuilder` instead of forwarding each to `ILogConsoleService`, then still logging the joined result once at the end so bcdedit output is not silently swallowed from the console), find the line containing `nx` (case-insensitive), and return true (disabled) when that line's value is `AlwaysOff` (case-insensitive); anything else (`OptIn`, `OptOut`, `AlwaysOn`) means enabled.

    2. `BootMenuTweakHandler`: Key "bootmenu", Title "BootMenuPolicy Standard", Description "Set Boot Menu Policy to Standard", Order 6. `SetState(bool standard)` runs `bcdedit.exe /set bootmenupolicy Standard` (standard=true) or `bcdedit.exe /set bootmenupolicy legacy` (false). `GetState()` runs `bcdedit /enum {current}`, finds the line containing `bootmenupolicy`, and returns true when its value is `Standard` (case-insensitive) — same parse pattern as `DepTweakHandler`, reuse the `RunProcessCaptureOutputAsync` helper added in item 1.

    3. `HyperVTweakHandler`: Key "hyperv", Title "Disable Hyper-V", Description "Toggle Hyper-V On or Off", Order 19. Also constructor-injects `IRegistryService registry`. `SetState(bool disable)`: on disable, run `bcdedit /set hypervisorlaunchtype off`, `bcdedit /set vm no`, `bcdedit /set vsmlaunchtype Off`, `bcdedit /set loadoptions DISABLE-LSA-ISO,DISABLE-VBS`, `DISM /Online /Disable-Feature:Microsoft-Hyper-V-All /Quiet /NoRestart` (all via `scriptRunner`), then write LocalMachine `SOFTWARE\Policies\Microsoft\Windows\DeviceGuard`: `EnableVirtualizationBasedSecurity=0, RequirePlatformSecurityFeatures=1, HypervisorEnforcedCodeIntegrity=0, HVCIMATRequired=0, LsaCfgFlags=0, ConfigureSystemGuardLaunch=0` (DWord), LocalMachine `SYSTEM\CurrentControlSet\Control\DeviceGuard` `RequireMicrosoftSignedBootChain=0` (DWord), LocalMachine `SYSTEM\CurrentControlSet\Control\DeviceGuard\Scenarios\HypervisorEnforcedCodeIntegrity` `Enabled=0` (DWord); on enable, run `bcdedit /set hypervisorlaunchtype auto`, `bcdedit /deletevalue vm`, `bcdedit /deletevalue loadoptions`, `DISM /Online /Enable-Feature:Microsoft-Hyper-V-All /Quiet /NoRestart`, then write LocalMachine `SYSTEM\CurrentControlSet\Control\DeviceGuard`: `RequireMicrosoftSignedBootChain=1, EnableVirtualizationBasedSecurity=1, RequirePlatformSecurityFeatures=1` (DWord), and `...\Scenarios\HypervisorEnforcedCodeIntegrity` `Enabled=1` — note the disable path writes the DeviceGuard policy keys under `SOFTWARE\Policies\...` while the enable path writes under `SYSTEM\CurrentControlSet\Control\...`; preserve this exact predecessor asymmetry, do not "fix" it. `GetState()` (Pitfall 6 — registry portion only, no bcdedit/DISM spawn on read) reads LocalMachine `SYSTEM\CurrentControlSet\Control\DeviceGuard\Scenarios\HypervisorEnforcedCodeIntegrity\Enabled` and returns true (disabled) when it equals 0 — both the disable and enable write paths converge on this one key, so it is a reliable single-key representative read.

    4. `VrTweakHandler`: Key "vr", Title "VR", Description "Enable VR Services", Order 16. `SetState(bool enable)` writes `Start` (DWord) under `SYSTEM\ControlSet001\Services\{svc}` for 9 services with per-service (enableVal, disableVal) pairs: `KSecPkg (0,4), LanmanWorkstation (2,4), mrxsmb (3,4), mrxsmb20 (3,4), rdbss (1,4), srv2 (2,4), QwaveDrv (3,4), Qwave (3,4), FontCache (2,4)` — write `enableVal` when `enable` else `disableVal`, for every service; then run `DISM /Online /Enable-Feature /FeatureName:SmbDirect /NoRestart` (enable) or `DISM /Online /Disable-Feature /FeatureName:SmbDirect /NoRestart` (disable) via `scriptRunner`. `GetState()` (registry portion only, per Pitfall 6) returns `KSecPkg\Start == 0` (KSecPkg's enable-value is 0, distinct from every other service's non-zero enable-value, making it the cleanest single-key discriminator).
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <automated>grep -rn "HasState\|SaveState\|ClearState\|Software\\\\AkariTool" src/AkariToolbox.App/Services/TweakHandlers/BcdeditDismTweaks.cs || echo "clean"</automated>
  </verify>
  <done>4 hybrid handlers compile; dep/bootmenu read via bcdedit-output parse, hyperv/vr read via registry-portion only (no process spawn per read); no anti-pattern matches.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - `grep -c "HasState\|SaveState\|ClearState" src/AkariToolbox.App/Services/TweakHandlers/BcdeditDismTweaks.cs` returns `0`.
    - `DepTweakHandler.GetState()` and `BootMenuTweakHandler.GetState()` invoke `bcdedit /enum {current}` via `IScriptRunner` (not a registry read) — confirmed by code inspection/grep for `"/enum {current}"`.
    - `HyperVTweakHandler.GetState()` and `VrTweakHandler.GetState()` read only from `IRegistryService` (no `IScriptRunner`/`bcdedit`/`DISM` call inside `GetState()`).
    - Order values in this file are exactly `{3,6,16,19}` with no duplicates.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elevated process -> spawned bcdedit/DISM processes | `DepTweakHandler`/`BootMenuTweakHandler`/`HyperVTweakHandler`/`VrTweakHandler` all spawn fixed, hardcoded `bcdedit`/`DISM` commands — no user-supplied input reaches these call sites in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Tampering | HyperVTweakHandler disables VBS/HVCI, a security-relevant OS feature | medium | accept | User-initiated, explicit toggle on a labeled "Disable Hyper-V" tweak — matches predecessor behavior exactly, no new capability introduced; this is the tool's documented purpose (system tweaking), not an unintended side effect |
| T-01-12 | Denial of Service | bcdedit /enum {current} output parse (DepTweakHandler/BootMenuTweakHandler) | low | mitigate | `IScriptRunner`'s existing timeout + exception handling (Plan 01-02) applies to the capture-output call the same as any other `RunProcessAsync`-family call — a hung `bcdedit` cannot block page load indefinitely |
</threat_model>

<verification>
- `dotnet build AkariToolbox.slnx -c Debug` succeeds for both files.
- Both grep anti-pattern checks return "clean".
</verification>

<success_criteria>
All 8 non-pure-registry tweak handlers exist, compile, correctly read live state via the appropriate primitive (IWindowsServiceController, IRegistryService directly for cdrom, or bcdedit-output-parse/registry-portion for the bcdedit/DISM hybrids), and contain no D-03 anti-pattern.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-akari-os-tweaks/01-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `ClipboardTweakHandler`, `BluetoothTweakHandler`, `CdromTweakHandler`, `PrintSpoolerTweakHandler`
- `DepTweakHandler`, `BootMenuTweakHandler`, `HyperVTweakHandler`, `VrTweakHandler`
- `IScriptRunner.RunProcessCaptureOutputAsync` (new member, added in Task 2 if not already sufficient from Plan 01-02)

## Flagged Assumptions (edge-probe items resolved as inapplicable, not force-fit; `must_haves.prohibitions: []` explained below)

- **TWEAKS-01 adjacency**: does not apply — these 8 tweaks are independent, no merge/collision concept between service-backed or bcdedit/DISM-hybrid handlers.
- **TWEAKS-01 empty/null input**: every value written (service names, `Start` DWORDs, `bcdedit`/`DISM` arguments) is a hardcoded literal ported from `TweakService.cs` — no free-form/empty/null input surface exists in this plan.
- **TWEAKS-01 ordering**: covered by explicit `Order`-uniqueness assertions in each task's `acceptance_criteria` (`{4,5,12,13}` for Task 1, `{3,6,16,19}` for Task 2).
- **Read-side strategy selection** (registry vs. `IWindowsServiceController` vs. bcdedit-output-parse vs. registry-portion-only) is itself the probe-derived "which edge case applies per tweak" resolution this domain needed — it is made explicit per-handler in each task's `<action>` text (RESEARCH Pitfall 6) rather than left implicit, so no separate prohibitions-list restatement is needed.
- **`prohibitions: []` rationale**: same as Plan 01-04 — the D-03 anti-pattern strip is enforced as an automated `<verify>` grep in both tasks (the stronger, machine-checked form of the same constraint) rather than restated as prose.
