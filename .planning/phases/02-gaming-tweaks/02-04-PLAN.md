---
phase: 02-gaming-tweaks
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs
  - src/AkariToolbox.Tests/GamingWindowsTweaksTests.cs
autonomous: true
requirements: [GAMING-01]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "NetworkIpv4OnlyTweakHandler.SetState(true) disables the exact 8 network-binding component IDs the source script disables (ms_lldp, ms_lltdio, ms_implat, ms_rspndr, ms_tcpip6, ms_server, ms_msclient, ms_pacer); SetState(false) re-enables those same 8 plus ms_tcpip (9 total on revert), matching 27 Network IPv4 Only.ps1's own asymmetric enable-list exactly."
    - "PowerPlanTweakHandler.SetState(false) does NOT call powercfg -restoredefaultschemes directly against live system state without first exporting every existing power scheme via powercfg -export to a per-session temp folder — this hardening (RESEARCH.md Assumption A4) turns 29 Power Plan.ps1's destructive, non-reversible revert into a real prior-state restore, which is what GAMING-01's literal 'same real-state and revert behavior as Tweaks' requirement demands; SetState(true) still enumerates and exports (not silently drops) every pre-existing scheme before duplicating Ultimate Performance and deleting the originals."
    - "TimerResolutionTweakHandler.SetState(true) probes File.Exists on the hardcoded csc.exe path before attempting compilation, and on a missing compiler logs a clear, visible failure via ILogConsoleService rather than silently no-oping the way 30 Timer Resolution.ps1's own -ErrorAction SilentlyContinue does."
    - { statement: "If the app is interrupted (crash/kill) mid-mutation for PowerPlanTweakHandler or TimerResolutionTweakHandler — both of which involve multi-step external process sequences (powercfg export/duplicate/delete loop; csc.exe compile + New-Service + registry write), not a single atomic registry SetValue — the next GetState() call still reads live, real system state and reports a value consistent with whatever step the interruption actually left the system in, rather than crashing or reporting a cached/impossible state.", verification: backstop }
  artifacts:
    - path: "src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs"
      provides: "NetworkIpv4OnlyTweakHandler (Order 107), PowerPlanTweakHandler (Order 109, hardened revert), TimerResolutionTweakHandler (Order 110) — completes all 6 Windows-folder toggles and all 11 Gaming stateful handlers"
  key_links:
    - from: "src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs"
      to: "src/AkariToolbox.Framework/Services/IScriptRunner.cs"
      via: "PowerPlanTweakHandler/TimerResolutionTweakHandler shell out via IScriptRunner.RunProcessAsync/RunProcessCaptureOutputAsync to powercfg.exe/csc.exe/sc.exe — never Microsoft.PowerShell.SDK in-process hosting, per CLAUDE.md"
      pattern: "RunProcessAsync|RunProcessCaptureOutputAsync"
  prohibitions:
    - "Never call powercfg -restoredefaultschemes as PowerPlanTweakHandler's SetState(false) implementation without the powercfg -export/-import hardening — the naive as-authored destructive revert deletes every power scheme on the system (including ones the app never touched), which this plan judges unacceptable for a requirement (GAMING-01) that explicitly promises real prior-state revert, unlike D-06's separate, explicitly user-accepted no-verification risk for downloaded binaries."
    - "Never let TimerResolutionTweakHandler's csc.exe compilation step fail via source's own -ErrorAction SilentlyContinue-equivalent silence — a missing/failed compiler must produce a visible, logged failure the user can see, since a silently-failed 'successful-looking' toggle directly violates this app's core value statement (every tweak must report accurate state)."
---

<objective>
Complete the `6 Windows` folder's toggle set (D-07) and, with it, all 11 of this phase's stateful `ITweakHandler`s: `NetworkIpv4OnlyTweakHandler` (PowerShell cmdlet-backed, no raw registry), `PowerPlanTweakHandler` (the phase's most consequential handler — its naive as-authored revert is destructive, hardened here per RESEARCH.md's Assumption A4), and `TimerResolutionTweakHandler` (runtime C# compilation + Windows Service install, hardened with a pre-flight compiler check per Pitfall 3). This plan owns the deliberate engineering decision to harden Power Plan's revert beyond what the source script does, because GAMING-01 literally requires "the same real-state and revert behavior as Tweaks (TWEAKS-01/TWEAKS-03)" and the source script's own revert cannot satisfy that as written.

Purpose: close out the toggle-handler surface of this phase with its two most technically involved handlers, and resolve RESEARCH.md's flagged open engineering question (Power Plan hardening) with a concrete, testable implementation rather than leaving it unresolved.
Output: all 11 Gaming stateful toggles live on the Gaming Tweaks page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-gaming-tweaks/02-RESEARCH.md
@src/AkariToolbox.Framework/Services/IScriptRunner.cs
@src/AkariToolbox.Framework/Services/IWindowsServiceController.cs
@src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs

# Canonical source scripts
@C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/27 Network IPv4 Only.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/29 Power Plan.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/30 Timer Resolution.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NetworkIpv4OnlyTweakHandler</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs, src/AkariToolbox.Tests/GamingWindowsTweaksTests.cs</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/27 Network IPv4 Only.ps1 lines 27-30, 53-56, src/AkariToolbox.Framework/Services/IScriptRunner.cs</read_first>
  <behavior>
    - Test: `SetState(true)`, using a fake `IScriptRunner`, calls `RunProcessAsync("powershell.exe", ...)` with an argument string invoking `Disable-NetAdapterBinding -Name "*" -ComponentID <id>` once per the 8 component IDs (`ms_lldp`, `ms_lltdio`, `ms_implat`, `ms_rspndr`, `ms_tcpip6`, `ms_server`, `ms_msclient`, `ms_pacer`).
    - Test: `SetState(false)` calls `Enable-NetAdapterBinding` for the same 8 IDs plus a 9th, `ms_tcpip` — assert exactly 9 enable calls are made (not 8).
  </behavior>
  <action>
    Add `NetworkIpv4OnlyTweakHandler(IScriptRunner scriptRunner) : ITweakHandler`: `Key => "netipv4only"`, `Title => "Force IPv4-Only Networking"`, `Description => "Disable IPv6 and other non-essential network bindings for lower latency"`, `Order => 107`, `Category => TweakCategory.Gaming`. No raw registry access — this handler shells to PowerShell cmdlets only (per `27 ...:27-30,53-56`). `SetState(true)`: for each of `ms_lldp, ms_lltdio, ms_implat, ms_rspndr, ms_tcpip6, ms_server, ms_msclient, ms_pacer`, call `scriptRunner.RunProcessAsync("powershell.exe", $"-NoProfile -Command \"Disable-NetAdapterBinding -Name '*' -ComponentID {id}\"")` (synchronously awaited inside the sync `SetState` via `.GetAwaiter().GetResult()`, matching Plan 02-01's `MsiModeTweakHandler` convention). `SetState(false)`: same list PLUS `ms_tcpip` (9 total), calling `Enable-NetAdapterBinding` instead. `GetState()` calls `scriptRunner.RunProcessCaptureOutputAsync("powershell.exe", "-NoProfile -Command \"(Get-NetAdapterBinding -Name '*' -ComponentID ms_tcpip6 | Select-Object -First 1).Enabled\"")` and returns `true` if the captured output trims to `"False"` (binding disabled means the tweak is "on") — treat any unparseable/empty output as `false`.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~NetworkIpv4Only"</automated>
  </verify>
  <done>NetworkIpv4OnlyTweakHandler compiles, tests pass, disable/enable lists match the source script's asymmetric 8-vs-9 shape exactly.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~NetworkIpv4Only"` passes both tests.
    - `NetworkIpv4OnlyTweakHandler.Order == 107`, `Category == TweakCategory.Gaming`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PowerPlanTweakHandler — hardened export/import revert</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs, src/AkariToolbox.Tests/GamingWindowsTweaksTests.cs</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/29 Power Plan.ps1 lines 21-269, .planning/phases/02-gaming-tweaks/02-RESEARCH.md "29 Power Plan.ps1 — full detail" and "Pitfall 2"</read_first>
  <action>
    Add `PowerPlanTweakHandler(IScriptRunner scriptRunner, IRegistryService registry) : ITweakHandler`: `Key => "powerplan"`, `Title => "Gaming Power Plan"`, `Description => "Create and activate a custom high-performance power scheme, disabling hibernate/sleep/fast-boot/power-throttling"`, `Order => 109`, `Category => TweakCategory.Gaming`.

    `SetState(true)`: BEFORE any deletion, run `powercfg /L` via `RunProcessCaptureOutputAsync`, parse every existing scheme GUID from the output, and for each one NOT equal to this app's own fixed custom-scheme GUID (`99999999-9999-9999-9999-999999999999`), export it via `powercfg -export "<tempDir>\<guid>.pow" <guid>` to a per-session temp folder (create via `Path.Combine(Path.GetTempPath(), "AkariToolbox-PowerPlanBackup")`, `Directory.CreateDirectory` if absent — this is the RESEARCH.md Assumption A4 hardening, not present in the source script). Then proceed exactly as `29 Power Plan.ps1:21-230` does: `powercfg -duplicatescheme e9a42b02-d5df-448d-aa00-03f14749eb61 99999999-9999-9999-9999-999999999999`, `powercfg -setactive 99999999-9999-9999-9999-999999999999`, then delete every OTHER scheme found in the earlier `/L` parse (now safely exported first) via `powercfg /delete <guid>`. Set the ~14 registry values (`HibernateEnabled`/`HibernateEnabledDefault`=0, `ShowLockOption`/`ShowSleepOption`=0, `HiberbootEnabled`=0, `PowerThrottlingOff`=1, under `HKLM\SYSTEM\CurrentControlSet\Control\...` per the source's exact paths — read them directly from `29 ...:1-64` during implementation) and the ~25 `powercfg /setacvalueindex`/`/setdcvalueindex` pairs against the new scheme GUID (hard-disk timeout, sleep/hibernate timers, USB selective suspend, PCIe link-state power management, min/max processor state 100%/100%, display timeout/brightness, battery-notification thresholds — full GUID pairs at `29 ...:65-227`, port verbatim).

    `SetState(false)`: if the per-session export folder from a prior `SetState(true)` call in this app session exists and contains `.pow` files, `powercfg -import` each one back, THEN call `powercfg -restoredefaultschemes` only as a final cleanup for any scheme this app's own On-branch created that wasn't in the original export set (i.e. the app's own custom `99999999-...` scheme), and delete the temp export folder. If no export folder exists for this session (e.g. `SetState(true)` was never called, or the app was restarted since — the export is session-scoped, not persisted to disk permanently, matching this codebase's existing session-scoped `_priorState` convention), fall back to the source script's own `powercfg -restoredefaultschemes` behavior and log via `ILogConsoleService` that no session-scoped backup was available, so the destructive fallback is visible, not silent. Reverse the ~14 registry values and re-enable hibernate/lock/sleep/fast-boot/power-throttling per `29 ...:239-269`.

    `GetState()` checks whether the fixed custom-scheme GUID `99999999-9999-9999-9999-999999999999` is the currently active scheme (`powercfg /getactivescheme` output contains that GUID) — this is an "is our scheme active" check, not a literal snapshot, matching how `VpnTweakHandler`/`BluetoothTweakHandler` already document their own GetState approximation caveat in Phase 1's STATE.md.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~PowerPlan"</automated>
  </verify>
  <done>PowerPlanTweakHandler exports every pre-existing scheme before deleting any, and imports them back on revert when a session-scoped export exists; the naive destructive-only revert is only a documented, logged fallback path, not the default.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~PowerPlan"` passes, including a test asserting `powercfg -export` is called for every non-app-owned scheme BEFORE any `powercfg /delete` call (ordering assertion against a fake `IScriptRunner` call sequence).
    - `grep -n "powercfg -export" src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs` matches at least once.
    - A test asserts that when a session export exists, `SetState(false)` calls `powercfg -import` rather than going straight to `powercfg -restoredefaultschemes`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TimerResolutionTweakHandler — pre-flight compiler check + service lifecycle</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs, src/AkariToolbox.Tests/GamingWindowsTweaksTests.cs</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/30 Timer Resolution.ps1 lines 21-266, src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs, .planning/phases/02-gaming-tweaks/02-RESEARCH.md "30 Timer Resolution.ps1 — full detail" and "Pitfall 3"</read_first>
  <behavior>
    - Test: `SetState(true)`, when a fake `File.Exists`-equivalent seam reports `csc.exe` absent, logs a clear failure via a fake `ILogConsoleService` and does NOT attempt `RunProcessAsync` for compilation (assert the fake script runner's compile call was never made).
    - Test: `SetState(true)`, when the compiler is present (fake seam reports true), calls `RunProcessAsync` with the hardcoded `csc.exe` path and the expected output `.exe` path argument.
    - Test: `GetState()` returns true only when the `GlobalTimerResolutionRequests` registry value equals `1`.
  </behavior>
  <action>
    Add `TimerResolutionTweakHandler(IScriptRunner scriptRunner, IRegistryService registry, IWindowsServiceController serviceController, ILogConsoleService log) : ITweakHandler`: `Key => "timerresolution"`, `Title => "High-Precision Timer Resolution"`, `Description => "Compile and install a background service that forces the OS's minimum timer resolution"`, `Order => 110`, `Category => TweakCategory.Gaming`.

    Const `CscPath = @"C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe"` (matches `30 ...:219-239`'s hardcoded path exactly — do not attempt to dynamically resolve a different `csc.exe`, per D-07's local-only/network-free scoping). `SetState(true)`: FIRST probe `File.Exists(CscPath)` (inject this check behind a small internal virtual/testable seam, e.g. a `Func<string,bool> fileExists` constructor parameter defaulting to `File.Exists`, so Task's unit tests can fake it without touching the real filesystem) — if false, call `log.Log("[TIMER-RES] csc.exe not found at expected path — Timer Resolution toggle cannot be applied on this machine.")` and RETURN without attempting compilation (this is the Pitfall 3 fix: the source script's own `-ErrorAction SilentlyContinue` swallows this same failure silently; this handler must not). If the compiler is present, write the ~200-line `SetTimerResolutionService.cs` C# source (a FIXED compile-time string literal ported verbatim from `30 ...:1-218` — never built from user/download input, per this phase's threat model) to `C:\Windows\SetTimerResolutionService.cs`, run `scriptRunner.RunProcessAsync(CscPath, "... /out:C:\\Windows\\SetTimerResolutionService.exe C:\\Windows\\SetTimerResolutionService.cs")` (exact csc arguments per `30 ...:219-230`), delete the `.cs` source file, then `New-Service`-equivalent via `IWindowsServiceController`/`System.ServiceProcess.ServiceController` (`AkariToolbox.App.csproj:44`'s already-referenced package, first real consumer in this app) to register `"Set Timer Resolution Service"` pointing at the compiled `.exe`, set `StartupType Auto`, start it, then `registry.SetValue(RegistryHive.LocalMachine, @"SYSTEM\CurrentControlSet\Control\Session Manager\kernel", "GlobalTimerResolutionRequests", 1, RegistryValueKind.DWord)`.

    `SetState(false)`: stop and delete the service (`sc.exe delete`-equivalent or `ServiceController`), delete the compiled `.exe` if present, delete the `GlobalTimerResolutionRequests` value.

    `GetState()` reads `GlobalTimerResolutionRequests` (DWORD) and returns `true` only if it equals `1`.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~TimerResolution"</automated>
  </verify>
  <done>TimerResolutionTweakHandler compiles, tests pass, a missing csc.exe produces a visible logged failure instead of a silent no-op.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~TimerResolution"` passes all 3 tests.
    - `grep -n "File.Exists" src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs` matches at least once inside `TimerResolutionTweakHandler`.
    - `grep -n "ErrorAction SilentlyContinue" src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs` returns no matches (the silent-failure pattern is not carried into the C# port).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elevated process -> powercfg.exe / csc.exe / sc.exe subprocess spawns | New boundary type for this phase: shelling to system utilities that themselves mutate broad system state (all existing power schemes; a compiled, installed Windows Service) — higher blast radius per call than a single registry SetValue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Denial of Service | PowerPlanTweakHandler's powercfg -restoredefaultschemes deleting all existing power schemes (Pitfall 2) | high | mitigate | Hardened per RESEARCH.md Assumption A4: every pre-existing scheme is exported via `powercfg -export` to a per-session temp folder before any delete, and `powercfg -import` restores them on revert — the naive destructive fallback only runs (and is logged as such) if no session-scoped export exists |
| T-02-10 | Tampering | TimerResolutionTweakHandler's runtime C# compilation + service install | high | mitigate | The compiled C# source is a fixed compile-time string literal, never built from user/download input; pre-flight `File.Exists(CscPath)` probe with a clear logged failure (not `-ErrorAction SilentlyContinue`-style silence) before any compilation attempt |
| T-02-11 | Denial of Service | NetworkIpv4OnlyTweakHandler disabling IPv6/other adapter bindings system-wide | medium | mitigate | Direct 1:1 port of the source script's own binding list (D-07); the revert path re-enables the exact same 8 component IDs plus `ms_tcpip`, matching the script's own off-branch — no broader blast radius than the source script itself |
</threat_model>

<verification>
- `dotnet build AkariToolbox.slnx -c Debug` succeeds.
- `dotnet test` passes all NetworkIpv4Only/PowerPlan/TimerResolution unit tests.
- Elevated manual launch: Gaming Tweaks page shows all 11 stateful toggles.
</verification>

<success_criteria>
All 11 Gaming stateful `ITweakHandler`s exist and are live on the Gaming Tweaks page; Power Plan's revert is a real prior-state restore (not a destructive fallback) whenever a session export exists; Timer Resolution's compiler-missing failure mode is visible, not silent.
</success_criteria>

<output>
Create `.planning/phases/02-gaming-tweaks/02-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `NetworkIpv4OnlyTweakHandler` (Order 107), `PowerPlanTweakHandler` (Order 109, hardened export/import revert), `TimerResolutionTweakHandler` (Order 110, pre-flight `csc.exe` probe)

## Flagged Assumptions

- **PowerPlan hardening (RESEARCH.md Assumption A4) — resolved by this plan, not deferred:** the source script's naive `-restoredefaultschemes` revert is deliberately NOT ported as-authored. This is a discretionary engineering decision (not a locked CONTEXT.md decision) made to satisfy GAMING-01's literal text ("same real-state and revert behavior as Tweaks") rather than ship a lesser standard; it is `reversible` (the hardening can be removed later to match the source script exactly, if a future decision prefers strict as-authored parity over safety) and is not gated behind a `checkpoint:decision` because it strengthens rather than reduces what GAMING-01 promises.
- **NetworkIpv4Only GetState() single-adapter/single-binding read**: like AmdSettings and the service-backed handlers, `GetState()` checks one representative binding (`ms_tcpip6` on the first adapter) rather than all 9 bindings across all adapters, matching established codebase convention for multi-value handlers.
</output>
