---
phase: 02-gaming-tweaks
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs
  - src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs
  - src/AkariToolbox.App/Views/GamingTweaksPage.xaml
  - src/AkariToolbox.Tests/GamingGraphicsTweaksTests.cs
autonomous: true
requirements: [GAMING-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "AmdSettingsTweakHandler.SetState(true) writes every one of the 20 AMD registry values/subkeys documented in RESEARCH.md's AMD Settings full value list (fixed HKCU\\Software\\AMD\\{CN,AIM,DVR} keys plus per-adapter class-GUID UMD/power_v1/IsAutoDefault/IsComponentControl values); SetState(false) reverses each per its own documented On/Off pair (some deleted, some written to a specific Off value, per-value as tabulated — not a uniform delete-everything approach)."
    - "IntelSettingsTweakHandler.SetState(true) creates a 3DKeys subkey with Global_AsyncFlipMode=2 and Global_LowLatency=0 under every 4-digit GPU adapter subkey found via the shared GetGpuAdapterSubKeys helper; SetState(false) deletes the entire 3DKeys subkey per adapter (not a partial value clear) — matching 6 Intel Settings.ps1's own asymmetric create-vs-delete-subkey shape."
    - "The 5 Graphics folder's 5 stateful toggles (Hdcp, P0State, MsiMode from Plan 02-01, plus AmdSettings and IntelSettings from this plan) all render on the Gaming Tweaks page automatically via the catalog filter — no additional GamingTweaksViewModel wiring was needed beyond registering the two new handler classes."
    - "The two D-05 one-shot shortcuts (Resolution/Refresh Rate, HAGS Windowed) are plain RelayCommand-backed buttons on the Gaming Tweaks page, not ITweakHandler toggles — clicking one launches the corresponding ms-settings: URI via Process.Start(UseShellExecute:true) and does not appear in ITweakCatalog.Handlers at all."
  artifacts:
    - path: "src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs"
      provides: "AmdSettingsTweakHandler (Order 103), IntelSettingsTweakHandler (Order 104) — completes the 5 Graphics folder's 5 stateful toggles"
  key_links:
    - from: "src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs"
      to: "System.Diagnostics.Process"
      via: "OpenDisplaySettingsCommand/OpenAdvancedGraphicsSettingsCommand RelayCommands call Process.Start(new ProcessStartInfo(uri) { UseShellExecute = true })"
      pattern: "UseShellExecute = true"
  prohibitions:
    - "Never write to ControlSet001 in AmdSettingsTweakHandler or IntelSettingsTweakHandler, even though 5 Amd Settings.ps1 and 6 Intel Settings.ps1 both hardcode it — target CurrentControlSet for every class-GUID subkey path, per Plan 02-01's established convention."
---

<objective>
Complete the `5 Graphics` folder's toggle set (D-04) by adding the two remaining, most complex handlers — `AmdSettingsTweakHandler` (20 registry values across fixed HKCU keys and per-adapter subkeys) and `IntelSettingsTweakHandler` (subkey create/delete) — and add the two D-05 one-shot display-settings shortcuts, which are plain `RelayCommand`s, not `ITweakHandler`s. After this plan, `5 Graphics`'s 5 stateful toggles + 2 shortcuts are fully ported (the 6 D-06 network-download scripts remain for Plan 02-06).

Purpose: finish one coherent source folder (5 Graphics) before starting the next (6 Windows, Plan 02-03), and prove the HexStringToBytes REG_BINARY conversion helper the AMD handler needs.
Output: 5/5 Graphics toggles live on the Gaming Tweaks page; both display-settings shortcut buttons work.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-gaming-tweaks/02-RESEARCH.md
@.planning/phases/02-gaming-tweaks/02-PATTERNS.md
@src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs
@src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs

# Canonical source scripts
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/5 Amd Settings.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/6 Intel Settings.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/12 Resolution Refresh Rate.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/13 Hags Windowed.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AmdSettingsTweakHandler — 20-value fixed + per-adapter registry handler</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs, src/AkariToolbox.Tests/GamingGraphicsTweaksTests.cs</files>
  <read_first>src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs, C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/5 Amd Settings.ps1 lines 11-256, .planning/phases/02-gaming-tweaks/02-RESEARCH.md "AMD Settings full value list" table</read_first>
  <behavior>
    - Test: `HexStringToBytes("3000")` returns `byte[] { 0x30, 0x00 }` (pairs of hex chars to bytes, matching how reg.exe /t REG_BINARY /d "3000" itself parses the string).
    - Test: `AmdSettingsTweakHandler.SetState(true)` writes all 10 fixed `HKCU\Software\AMD\{CN,AIM,DVR}` values from the table (e.g. `CN\AutoUpdate`=0 DWORD, `AIM\LaunchBugTool`=0 DWORD, `DVR\HotkeysDisabled`=1 DWORD, `CN\SystemTray`="false" SZ, etc.) using a fake `IRegistryService` and asserting each exact value/kind pair.
    - Test: `AmdSettingsTweakHandler.SetState(false)` reverses each of the 10 fixed values per its documented Off behavior (some deleted via `DeleteValue`, none re-written to a different value per the table) — assert `DeleteValue` is called for the "value deleted" rows and no `SetValue` call is made for those keys on the Off path.
    - Test: `AmdSettingsTweakHandler.GetState()` returns true only when the fixed `CN\AutoUpdate` value (representative single-value read, matching this codebase's existing multi-value-handler GetState convention) equals `0`.
  </behavior>
  <action>
    Add a `private static byte[] HexStringToBytes(string hex)` helper to `GamingGraphicsTweaks.cs` (file-scoped, shared): iterate pairs of characters, `Convert.ToByte(hex.Substring(i, 2), 16)` per pair, matching how `reg.exe /t REG_BINARY /d "..."` parses its hex string — used for every REG_BINARY value in the table below (do not scatter manual byte-array literals per RESEARCH.md's Don't-Hand-Roll table).

    Add `AmdSettingsTweakHandler(IRegistryService registry) : ITweakHandler`: `Key => "gpuamdsettings"`, `Title => "AMD Software Settings"`, `Description => "Apply recommended AMD Radeon Software tweaks (disables telemetry/overlays, sets performance registry keys)"`, `Order => 103`, `Category => TweakCategory.Gaming`.

    Implement `SetState(bool enabled)` writing/reversing exactly the 20 rows from RESEARCH.md's "AMD Settings full value list" table (`5 Amd Settings.ps1:29-137` on-branch, `:148-255` off-branch), all under `HKCU` unless noted, using `CurrentControlSet` (not `ControlSet001`) for the class-GUID per-adapter paths:
    - `Software\AMD\CN\AutoUpdate` (DWORD): On=0, Off=deleted.
    - `Software\AMD\AIM\LaunchBugTool` (DWORD): On=0, Off=1.
    - `Software\AMD\DVR\HotkeysDisabled` (DWORD): On=1, Off=deleted.
    - `Software\AMD\CN\SystemTray` (REG_SZ): On="false", Off=deleted.
    - `Software\AMD\DVR\ShowRSOverlay` (REG_SZ): On="false", Off=deleted.
    - `Software\AMD\CN\RSXBrowserUnavailable` (REG_SZ): On="true", Off=deleted.
    - `Software\AMD\CN\AllowWebContent` (REG_SZ): On="false", Off=deleted.
    - `Software\AMD\CN\CN_Hide_Toast_Notification` (REG_SZ): On="true", Off=deleted.
    - `Software\AMD\CN\AnimationEffect` (REG_SZ): On="false", Off=deleted.
    - `Software\AMD\CN\WizardProfile` (REG_SZ): On="PROFILE_CUSTOM", Off=deleted.
    - Per-adapter (each 4-digit subkey from `GetGpuAdapterSubKeys`, under `<classGuid>\<adapter>\UMD\`): `VSyncControl` (BINARY via `HexStringToBytes`): On="3000", Off="31000000"; `TFQ` (BINARY): On="3200", Off=deleted; `Tessellation` (BINARY): On="3100", Off="360034000000"; `Tessellation_OPTION` (BINARY): On="3200", Off="30000000".
    - `Software\AMD\CN\CustomResolutions\EulaAccepted` (REG_SZ): On="true", Off=subkey deleted.
    - `Software\AMD\CN\DisplayOverride\EulaAccepted` (REG_SZ): On="true", Off=subkey deleted.
    - Per-adapter `<classGuid>\<adapter>\power_v1\abmlevel` (BINARY): On="00000000", Off=deleted.
    - Per-adapter `<classGuid>\<adapter>\IsAutoDefault`: On=BINARY "00000000", Off=DWORD 1 (type changes between On/Off, as authored — preserve exactly).
    - Per-adapter `<classGuid>\<adapter>\IsComponentControl` (BINARY): On="0f000000", Off="00000000".
    - `Software\AMD\CN\Notification`: On=deleted then recreated empty (use `IRegistryService`'s existing delete-then-open/create-subkey pattern), Off=subkey deleted.
    - `Software\AMD\CN\{FreeSync,OverlayNotification,VirtualSuperResolution}\AlreadyNotified` (DWORD): On=1 each, Off=subkeys deleted.

    Best-effort, non-blocking side effect from `5 Amd Settings.ps1:22-24`: on the On branch only, attempt to restart `RadeonSoftware.exe` (`Process.GetProcessesByName("RadeonSoftware")` — if found, kill and note in a log; do not fail `SetState` if AMD software isn't installed) — wrap in try/catch, never throw.

    `GetState()` reads the representative `Software\AMD\CN\AutoUpdate` value (DWORD) and returns `true` only if it equals `0` (matches this codebase's existing multi-value-handler `GetState` convention of reading one representative value, e.g. `ClipboardTweakHandler`/`VbsTweakHandler`).
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~AmdSettings"</automated>
  </verify>
  <done>AmdSettingsTweakHandler compiles, all unit tests pass, HexStringToBytes correctly round-trips every BINARY value in the table.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~AmdSettings"` passes all tests.
    - `HexStringToBytes("3000")` returns exactly `[0x30, 0x00]`.
    - `grep -n "ControlSet001" src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs` returns no matches after this task.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IntelSettingsTweakHandler — 3DKeys subkey create/delete</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs, src/AkariToolbox.Tests/GamingGraphicsTweaksTests.cs</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/6 Intel Settings.ps1 lines 21-65</read_first>
  <behavior>
    - Test: `IntelSettingsTweakHandler.SetState(true)`, given a fake registry returning 2 fake 4-digit adapter subkeys, writes `Global_AsyncFlipMode=2` and `Global_LowLatency=0` (both DWORD) under a `3DKeys` child subkey of each adapter.
    - Test: `IntelSettingsTweakHandler.SetState(false)` deletes the entire `3DKeys` subkey (not individual values) under each adapter.
    - Test: `IntelSettingsTweakHandler.GetState()` returns true only when every adapter's `3DKeys\Global_AsyncFlipMode` equals `2`; returns false if the `3DKeys` subkey is absent for any adapter.
  </behavior>
  <action>
    Add `IntelSettingsTweakHandler(IRegistryService registry) : ITweakHandler`: `Key => "gpuintelsettings"`, `Title => "Intel Graphics Settings"`, `Description => "Enable async flip mode and low-latency mode on Intel GPUs"`, `Order => 104`, `Category => TweakCategory.Gaming`. Reuse `GetGpuAdapterSubKeys`. `SetState(true)` writes `Global_AsyncFlipMode=2` and `Global_LowLatency=0` (both DWORD) under `<classGuid>\<adapter>\3DKeys` for each adapter (`CurrentControlSet`, not `ControlSet001`, per `6 Intel Settings.ps1:21-49`'s own hardcoded `ControlSet001`). `SetState(false)` deletes the entire `3DKeys` subkey per adapter — this requires a subkey-delete capability; if `IRegistryService` has no `DeleteSubKeyTree`-equivalent, add one (`void DeleteSubKeyTree(RegistryHive hive, string subKeyPath)`, registry-squatting-safe: open parent writable, `DeleteSubKeyTree(name, throwOnMissingSubKey: false)`) mirroring the existing `DeleteValue`'s never-throws-on-missing convention. `GetState()` reads `3DKeys\Global_AsyncFlipMode` per adapter, returns true only if every adapter's value equals `2` (absent `3DKeys` counts as not-set for that adapter).
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~IntelSettings"</automated>
  </verify>
  <done>IntelSettingsTweakHandler compiles, tests pass, 3DKeys subkey create/delete behavior matches the source script's asymmetric shape.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~IntelSettings"` passes all tests.
    - `IntelSettingsTweakHandler.Order == 104`, `Category == TweakCategory.Gaming`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: D-05 one-shot display-settings shortcuts</name>
  <files>src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs, src/AkariToolbox.App/Views/GamingTweaksPage.xaml</files>
  <read_first>src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs, src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs (search for UseShellExecute = true — the only existing precedent for this exact ProcessStartInfo shape), src/AkariToolbox.App/Views/AkariOSTweaksPage.xaml</read_first>
  <action>
    Add two `[RelayCommand]`-attributed methods to `GamingTweaksViewModel` (CommunityToolkit.Mvvm source generator, same convention as `HomeViewModel.Open`): `OpenDisplaySettings()` calling `Process.Start(new ProcessStartInfo("ms-settings:display") { UseShellExecute = true })`, and `OpenAdvancedGraphicsSettings()` calling `Process.Start(new ProcessStartInfo("ms-settings:display-advancedgraphics") { UseShellExecute = true })` — per `12 Resolution Refresh Rate.ps1:1` and `13 Hags Windowed.ps1:1`, both single-line scripts with no menu, no elevation check, no state (D-05). These are plain URI-scheme launches, not `ITweakHandler`s — do not add them to `ITweakCatalog`.

    Add a small `StackPanel` (Orientation=Horizontal, Spacing=8) with two `Button`s below the `ItemsRepeater` in `GamingTweaksPage.xaml`: "Display Settings" bound to `Command="{x:Bind ViewModel.OpenDisplaySettingsCommand}"`, "Advanced Graphics Settings (HAGS)" bound to `Command="{x:Bind ViewModel.OpenAdvancedGraphicsSettingsCommand}"`.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <human-check>Launch elevated, navigate to Gaming Tweaks. Confirm the two new buttons appear below the toggle list. Click "Display Settings" — confirm the Windows Settings app opens directly to the Display page. Click "Advanced Graphics Settings (HAGS)" — confirm it opens directly to the advanced graphics settings sub-page.</human-check>
  </verify>
  <done>Both shortcut buttons launch the correct ms-settings: URI; neither appears in the toggle list or ITweakCatalog.Handlers.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - `grep -n "ms-settings:display\"" src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs` and `grep -n "ms-settings:display-advancedgraphics" src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs` each match exactly once.
    - `grep -c "UseShellExecute = true" src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs` returns `2`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elevated process -> AMD/Intel per-adapter registry subkeys | Same elevated-process-to-registry boundary as Plan 02-01, now covering HKCU AMD keys and per-adapter Intel 3DKeys subkeys |
| GamingTweaksViewModel -> shell (ms-settings: URI launch) | New boundary type for this phase: launching a first-party OS Settings URI, not a registry write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | AmdSettingsTweakHandler's 20-value multi-key/nested-subkey writes | high | mitigate | Every write goes through `IRegistryService`'s existing null-checked open-then-create convention; `HexStringToBytes` only parses fixed compile-time hex literals from this plan's own source (never user input) — no injection surface; `RadeonSoftware.exe` restart is best-effort/non-blocking, wrapped in try/catch |
| T-02-06 | Information Disclosure | D-05 `ms-settings:` URI launches | low | accept | `Process.Start(UseShellExecute:true, "ms-settings:...")` opens a first-party OS settings page with no data passed and no elevation-prompt bypass — same trust level as a user manually opening Settings |
</threat_model>

<verification>
- `dotnet build AkariToolbox.slnx -c Debug` succeeds.
- `dotnet test` passes all AmdSettings/IntelSettings unit tests.
- Elevated manual launch: Gaming Tweaks page shows 5 toggles (Hdcp, P0State, MsiMode, AmdSettings, IntelSettings) plus 2 working shortcut buttons.
</verification>

<success_criteria>
The `5 Graphics` folder's full D-04/D-05 scope (5 stateful toggles + 2 shortcuts) is live on the Gaming Tweaks page.
</success_criteria>

<output>
Create `.planning/phases/02-gaming-tweaks/02-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `AmdSettingsTweakHandler` (Order 103), `IntelSettingsTweakHandler` (Order 104)
- `HexStringToBytes` shared helper (GamingGraphicsTweaks.cs)
- `IRegistryService.DeleteSubKeyTree` (new member, if not already covered — added here for IntelSettings' 3DKeys deletion)
- `GamingTweaksViewModel.OpenDisplaySettingsCommand`, `OpenAdvancedGraphicsSettingsCommand`

## Flagged Assumptions

- **AmdSettingsTweakHandler.GetState() single-representative-value read**: like every existing multi-value Phase 1 handler (e.g. `VbsTweakHandler`), `GetState()` reads one representative value (`CN\AutoUpdate`) rather than all 20 — this matches established codebase convention, not a shortcut specific to this plan.
</output>
