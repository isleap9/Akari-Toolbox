---
phase: 02-gaming-tweaks
plan: 05
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/AkariToolbox.App/Services/IGamingDropdownService.cs
  - src/AkariToolbox.App/Services/GamingDropdownService.cs
  - src/AkariToolbox.App/Services/TweakHandlerRegistration.cs
  - src/AkariToolbox.App/App.xaml.cs
  - src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs
  - src/AkariToolbox.App/Views/GamingTweaksPage.xaml
  - src/AkariToolbox.Tests/GamingDropdownServiceTests.cs
autonomous: false
requirements: [GAMING-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "IGamingDropdownService validates every selection index against [0, presetArray.Length) before performing any registry write for both SvcHost split threshold and Win32PrioritySeparation — an out-of-range index (negative, or == Length) is rejected with no write performed, mirroring the predecessor's own if (value < 0 || value >= Values.Length) return; guard (RESEARCH.md ASVS V5)."
    - "On GamingTweaksPage load, if the live SvcHostSplitThresholdInKB registry value does not exactly match any preset in the approved list, the dropdown selects the preset with the smallest absolute numeric distance to the live value; if two presets are exactly equidistant, the lower-valued preset is selected (explicit, deterministic tie-break contract set by this plan, since neither the source script nor RESEARCH.md defines one)."
    - "The 'Default' SvcHost preset deletes the SvcHostSplitThresholdInKB value entirely (letting Windows compute its own default) rather than writing either the predecessor's buggy decimal 380000 or a guessed hex-equivalent literal — per RESEARCH.md's Predecessor Discrepancy finding (Assumption A3)."
  artifacts:
    - path: "src/AkariToolbox.App/Services/IGamingDropdownService.cs"
      provides: "Live-read/live-write abstraction for the 2 non-boolean registry dropdowns (SvcHost split threshold, Win32PrioritySeparation) — not an ITweakHandler, no revert/prior-capture semantics needed"
  key_links:
    - from: "src/AkariToolbox.App/Views/GamingTweaksPage.xaml"
      to: "src/AkariToolbox.App/Services/GamingDropdownService.cs"
      via: "GamingTweaksViewModel exposes SelectedSvcHostIndex/SelectedWin32PriorityIndex bound to two ComboBoxes; property setters call IGamingDropdownService's write methods after bounds validation"
      pattern: "IGamingDropdownService"
  prohibitions: []
---

<objective>
Add the two registry dropdowns GAMING-01 requires (SvcHost split threshold, Win32 Priority Separation) as a new, small `IGamingDropdownService` — deliberately NOT an `ITweakHandler` (no boolean state, no revert semantics, per RESEARCH.md's Architectural Responsibility Map). Per D-09, the expanded preset value lists are community-sourced (LOW confidence, not Microsoft-authoritative) and explicitly require user approval before being locked into code — this plan opens with a `checkpoint:decision` presenting RESEARCH.md's proposed lists before any implementation proceeds.

Purpose: close out GAMING-01's dropdown requirement with validated, bounds-checked, tie-break-defined behavior — resolving the boundary and precision edges the spec-less probe fallback flagged as unresolved for this requirement.
Output: 2 working dropdowns on the Gaming Tweaks page, wired to whichever preset lists the user approves.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-gaming-tweaks/02-RESEARCH.md
@.planning/phases/02-gaming-tweaks/02-PATTERNS.md
@src/AkariToolbox.Framework/Services/IRegistryService.cs
@src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs
@src/AkariToolbox.App/Views/GamingTweaksPage.xaml
@src/AkariToolbox.App/Services/TweakHandlerRegistration.cs

# Predecessor reference (superseded by D-09, wider lists)
@C:/Users/isleap/Documents/GitHub/AkariOS-Companion/ViewModels/GamingTweaksViewModel.cs
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve the expanded SvcHost split threshold and Win32 Priority Separation dropdown preset lists (D-09)</decision>
  <context>
    02-CONTEXT.md D-09 keeps both dropdowns but wants more preset values than the predecessor's fixed 6-value SvcHost list and 5-value Win32Priority list, with the exact expanded list flagged as "Claude's/research's call — propose during planning for user approval, not specified now." 02-RESEARCH.md proposes expanded lists below, sourced from community/forum content (LOW confidence, not a Microsoft Learn page) and cross-checked across 2 independent web searches for internal consistency. RESEARCH.md also found the predecessor's own "Default" SvcHost preset (literal decimal `380000`) looks like a decimal/hex transcription bug — Windows' real default `SvcHostSplitThresholdInKB` is `0x380000` hex (decimal 3,670,016), not decimal `380000`. This checkpoint blocks implementation until the preset lists are approved, since RESEARCH.md explicitly flags them as unverified and requiring sign-off before being locked into code.
  </context>
  <options>
    <option id="research-proposed">
      <name>Use RESEARCH.md's proposed expanded lists as-is</name>
      <pros>SvcHost: 10 presets (Default=delete-value, 4/8/12/16/24/32/48/64/128 GB). Win32Priority: 13 presets covering all combinations of {Short,Long} quantum x {Fixed,Variable} length x {No,Medium,High} boost, plus the predecessor's unverified "Legacy/Advanced" 0x06 value carried forward for parity. Fixes the predecessor's Default-value decimal/hex bug by deleting the value instead of writing either number. Broadest selection for users who want fine-grained control.</pros>
      <cons>Community-sourced, not independently confirmed against a Microsoft Learn page; the 48GB/128GB SvcHost values and several Win32Priority combinations are `[ASSUMED]`/extrapolated rather than directly found in search results; 0x06's exact scheduling semantics remain unconfirmed.</cons>
    </option>
    <option id="predecessor-parity">
      <name>Keep only the predecessor's original narrower lists, with the Default-value bug fixed</name>
      <pros>SvcHost: 6 presets (Default=delete-value instead of the buggy 380000, 4/8/16/32/64 GB — all previously shipped and implicitly field-tested by the predecessor's users). Win32Priority: the predecessor's exact original 5 values (0x2A, 0x28, 0x26, 0x16, 0x06). Minimizes risk of shipping an unverified community value; smaller, more conservative change.</pros>
      <cons>Does not fulfill D-09's explicit ask for "more preset values than the predecessor's fixed lists" — under-delivers relative to the locked decision's stated intent.</cons>
    </option>
  </options>
  <resume-signal>Select "research-proposed", "predecessor-parity", or provide your own custom preset list (values in KB for SvcHost, hex for Win32PrioritySeparation) to use instead of either option</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: IGamingDropdownService + validated read/write for both presets</name>
  <files>src/AkariToolbox.App/Services/IGamingDropdownService.cs, src/AkariToolbox.App/Services/GamingDropdownService.cs, src/AkariToolbox.App/Services/TweakHandlerRegistration.cs, src/AkariToolbox.Tests/GamingDropdownServiceTests.cs</files>
  <read_first>src/AkariToolbox.Framework/Services/IRegistryService.cs, src/AkariToolbox.App/Services/TweakHandlerRegistration.cs, C:/Users/isleap/Documents/GitHub/AkariOS-Companion/ViewModels/GamingTweaksViewModel.cs lines 21-30, 56-57 (predecessor's index-bounds guard, reference only)</read_first>
  <action>
    Create `src/AkariToolbox.App/Services/IGamingDropdownService.cs`: expose `IReadOnlyList<(string Label, int? ValueKb)> SvcHostPresets { get; }` (a `null` `ValueKb` represents "Default — delete the value"), `IReadOnlyList<(string Label, int ValueHex)> Win32PriorityPresets { get; }`, `int GetSvcHostPresetIndex()`, `void SetSvcHostPreset(int index)`, `int GetWin32PriorityPresetIndex()`, `void SetWin32PriorityPreset(int index)`. Populate both preset lists as constructor-injected constants matching EXACTLY the option approved in the preceding checkpoint task (read the checkpoint's resume-signal/user response before writing this file — do not default to "research-proposed" without confirming that was actually selected).

    Implement `GamingDropdownService(IRegistryService registry) : IGamingDropdownService`. `GetSvcHostPresetIndex()`: read `registry.GetValue(RegistryHive.LocalMachine, @"SYSTEM\CurrentControlSet\Control", "SvcHostSplitThresholdInKB")`; if absent, return the index of the "Default" preset; otherwise find the preset with the smallest `Math.Abs(liveValue - preset.ValueKb)`, and on an exact tie between two presets, select the one with the LOWER `ValueKb` (explicit, deterministic tie-break per this plan's must_haves — implement via `OrderBy(p => Math.Abs(liveValue - p.ValueKb)).ThenBy(p => p.ValueKb).First()`). `SetSvcHostPreset(int index)`: FIRST validate `index >= 0 && index < SvcHostPresets.Count`, return with no write if out of range (log via a no-op/ignore — this is a programming-contract violation, not a user-facing error, since the UI itself only offers valid indices); if the preset's `ValueKb` is `null` ("Default"), call `registry.DeleteValue(...)`; otherwise `registry.SetValue(..., "SvcHostSplitThresholdInKB", presetValueKb, RegistryValueKind.DWord)`. Implement `GetWin32PriorityPresetIndex()`/`SetWin32PriorityPreset(int index)` identically against `HKLM\SYSTEM\CurrentControlSet\Control\PriorityControl`, value `Win32PrioritySeparation` (DWORD) — no "Default = delete" special case here (every Win32Priority preset writes a real hex value; there is no delete-equivalent in RESEARCH.md's proposal), same bounds-validation-before-write guard.

    Register `services.AddSingleton<IGamingDropdownService, GamingDropdownService>();` in `TweakHandlerServiceCollectionExtensions.AddTweakHandlers()` in `TweakHandlerRegistration.cs`, alongside the existing `IPostInstallService` registration (same App-project registration site).
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~GamingDropdownService"</automated>
  </verify>
  <done>IGamingDropdownService reads/writes both presets correctly, rejects out-of-range indices with no write, and implements the deterministic nearest-preset tie-break for SvcHost.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~GamingDropdownService"` passes, including: (1) an out-of-range index (`-1` and `SvcHostPresets.Count`) performs zero registry writes for both `SetSvcHostPreset`/`SetWin32PriorityPreset`; (2) a live value exactly between two presets selects the lower one; (3) the "Default" SvcHost preset calls `DeleteValue`, never `SetValue` with a literal `380000` or any other guessed number.
    - `grep -n "index >= 0" src/AkariToolbox.App/Services/GamingDropdownService.cs` (or equivalent bounds-check pattern) matches at least twice (once per preset type).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire dropdowns into GamingTweaksViewModel + GamingTweaksPage</name>
  <files>src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs, src/AkariToolbox.App/Views/GamingTweaksPage.xaml</files>
  <read_first>src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs, src/AkariToolbox.App/Views/GamingTweaksPage.xaml</read_first>
  <action>
    Add `IGamingDropdownService` as a constructor-injected dependency on `GamingTweaksViewModel`. Expose `IReadOnlyList&lt;string&gt; SvcHostPresetLabels`/`IReadOnlyList&lt;string&gt; Win32PriorityPresetLabels` (from `IGamingDropdownService.SvcHostPresets`/`Win32PriorityPresets`, `.Select(p => p.Label)`) and two `[ObservableProperty]`-backed int fields `_selectedSvcHostIndex`/`_selectedWin32PriorityIndex`, initialized from `dropdownService.GetSvcHostPresetIndex()`/`GetWin32PriorityPresetIndex()` in the constructor (read live state on load, same principle as every toggle). Add partial `OnSelectedSvcHostIndexChanged(int value)`/`OnSelectedWin32PriorityIndexChanged(int value)` methods (CommunityToolkit.Mvvm's generated property-changed hooks) that call `dropdownService.SetSvcHostPreset(value)`/`SetWin32PriorityPreset(value)` — skip the call during the constructor's initial assignment (guard with a private `_initialized` bool set true only after both indices are set, so loading the page doesn't immediately re-write the value it just read).

    Add two `ComboBox` controls to `GamingTweaksPage.xaml` below the D-05 shortcut buttons (Plan 02-02): one bound `ItemsSource="{x:Bind ViewModel.SvcHostPresetLabels}"` `SelectedIndex="{x:Bind ViewModel.SelectedSvcHostIndex, Mode=TwoWay}"` with a `Header="SvcHost Split Threshold"`, one identically shaped for `Win32PriorityPresetLabels`/`SelectedWin32PriorityIndex` with `Header="Win32 Priority Separation"`.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <human-check>Launch elevated, navigate to Gaming Tweaks. Confirm both dropdowns render with the approved preset labels and the currently-selected index matches your machine's real live registry value (or "Default" if unset). Change the SvcHost dropdown, then run `reg query "HKLM\SYSTEM\CurrentControlSet\Control" /v SvcHostSplitThresholdInKB` in a separate elevated terminal to confirm the write. Select "Default" and confirm the value is deleted (reg query reports ERROR: value not found), not set to a literal number.</human-check>
  </verify>
  <done>Both dropdowns render approved labels, reflect live state on load, and write validated selections through to the registry.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - `grep -n "SelectedSvcHostIndex" src/AkariToolbox.App/Views/GamingTweaksPage.xaml` and `grep -n "SelectedWin32PriorityIndex" src/AkariToolbox.App/Views/GamingTweaksPage.xaml` each match.
    - Manual reg query confirms the "Default" SvcHost selection deletes rather than writes a literal value.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GamingTweaksPage ComboBox selection -> registry write | New input-validation boundary for this phase: unlike the boolean toggles, a dropdown's SelectedIndex is a small integer that must be bounds-checked before use as an array index and before being written to a system-wide registry value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering | Unvalidated dropdown index write to SvcHostSplitThresholdInKB/Win32PrioritySeparation | high | mitigate | Every index is validated against `[0, presetArray.Length)` before any registry write (Task 1, ASVS V5), mirroring the predecessor's own `if (value < 0 || value >= Values.Length) return;` guard |
| T-02-13 | Tampering | SvcHost "Default" preset historically miswritten as decimal-for-hex (predecessor bug, RESEARCH.md Assumption A3) | low | mitigate | The new "Default" preset deletes the value entirely (letting Windows compute its own default) instead of writing either the buggy decimal or a guessed hex literal |
</threat_model>

<verification>
- `dotnet build AkariToolbox.slnx -c Debug` succeeds.
- `dotnet test` passes all GamingDropdownService unit tests.
- Elevated manual launch: both dropdowns render, reflect live state, and write validated selections.
</verification>

<success_criteria>
GAMING-01's dropdown requirement is fully satisfied with a user-approved preset list, bounds-validated writes, and a deterministic nearest-preset tie-break contract.
</success_criteria>

<output>
Create `.planning/phases/02-gaming-tweaks/02-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `IGamingDropdownService` / `GamingDropdownService` (`SvcHostPresets`, `Win32PriorityPresets`, `Get*PresetIndex`, `Set*Preset`)
- `GamingTweaksViewModel.SvcHostPresetLabels`/`Win32PriorityPresetLabels`/`SelectedSvcHostIndex`/`SelectedWin32PriorityIndex`
- 2 `ComboBox` controls on `GamingTweaksPage.xaml`

## Flagged Assumptions (spec-less-probe-fallback edges resolved)

- **Boundary (resolved, explicit):** dropdown selection index is validated against `[0, presetArray.Length)` before any registry write; out-of-range selections are rejected with no write performed (Task 1's must_haves and acceptance criteria).
- **Precision (resolved, explicit):** nearest-preset matching on dropdown load ties break toward the lower value — an explicit, deterministic contract this plan sets since neither the source script nor RESEARCH.md defined one (this is a routine, low-stakes engineering default, not a product decision requiring its own checkpoint).
</output>
