---
phase: 02-gaming-tweaks
plan: 07
type: execute
wave: 5
depends_on: ["02-02", "02-03", "02-04", "02-05", "02-06"]
files_modified:
  - src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs
autonomous: true
requirements: [GAMING-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Exactly 11 Gaming-category ITweakHandler implementations are registered (gpuhdcp, gpup0state, gpumsimode, gpuamdsettings, gpuintelsettings, devpowersavings, netpowersavings, netipv4only, writecacheflush, powerplan, timerresolution), with unique Order values spanning exactly {100,101,...,110} — verified by a new automated unit test, scoped to Category==Gaming, added by this plan (the Gaming-count equivalent of Phase 1's closing 32-handler invariant test)."
    - "The AkariOS-scoped ordering tests (Plan 02-01) still assert exactly 32 handlers with Order [0..31] and the exact predecessor key sequence — re-verified at full-phase completion, confirming the 11 new Gaming handlers introduced zero regression on the existing Akari OS Tweaks page."
    - "Zero matches for ControlSet001 across every file under src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs and GamingWindowsTweaks.cs — a repo-wide grep backstop confirming Plan 02-01 through 02-04's CurrentControlSet convention held across all 11 handlers, not just per-plan in isolation."
    - "Launching the app elevated shows the Gaming Tweaks page with all 11 toggles, 2 dropdowns (SvcHost, Win32Priority), 2 one-shot display-settings shortcuts, and the full D-06 driver-tools button section — spot-checked against real system state via reg query for at least 3 toggles."
  artifacts:
    - path: "src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs"
      provides: "Gaming-scoped regression test (exactly 11 handlers, Order {100..110}) — the sole file this plan modifies, avoiding any cross-plan file-ownership conflict during the earlier waves"
  key_links: []
  prohibitions: []
---

<objective>
Close out Phase 2: verify the full 11-handler Gaming catalog (built across Plans 02-01 through 02-04 with zero shared-file coordination beyond `GamingGraphicsTweaks.cs`/`GamingWindowsTweaks.cs` themselves) holds together as one coherent, correctly-ordered page, alongside the dropdowns (Plan 02-05) and D-06 action buttons (Plan 02-06). Because `GamingTweaksViewModel`/`GamingTweaksPage` were designed in Plan 02-01 to enumerate `catalog.Handlers` filtered by category, no new toggle-list wiring code is needed for the later 8 handlers to appear — this plan is verification + the final regression-test lock, not feature-building, mirroring Phase 1's own `01-07-PLAN.md` closing pattern.

Purpose: prove GAMING-01 holds across the assembled whole — real-state read/revert for all 11 toggles, validated dropdowns, and a fully surfaced accepted-risk D-06 action set — not just per-plan in isolation.
Output: an automated Gaming-scoped ordering-invariant test, and a verified, fully-populated Gaming Tweaks page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-gaming-tweaks/02-RESEARCH.md
@src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs
@src/AkariToolbox.App/Services/ITweakHandler.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gaming-scoped 11-handler ordering regression test</name>
  <files>src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs</files>
  <read_first>src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs, src/AkariToolbox.App/Services/ITweakHandler.cs</read_first>
  <behavior>
    - Test: resolving `IEnumerable<ITweakHandler>` filtered to `Category == TweakCategory.Gaming` from a DI container built via `AddAkariSystemPrimitives()`+`AddTweakHandlers()` yields exactly 11 handlers.
    - Test: those 11 handlers' `Order` values, sorted ascending, are exactly `[100,101,102,103,104,105,106,107,108,109,110]` — no duplicates, no gaps, disjoint from the AkariOS range `[0..31]`.
    - Test: sorting the 11 Gaming handlers by `Order` and taking their `Key` values produces the exact sequence `gpuhdcp, gpup0state, gpumsimode, gpuamdsettings, gpuintelsettings, devpowersavings, netpowersavings, netipv4only, writecacheflush, powerplan, timerresolution`.
    - Test (re-run, unchanged from Plan 02-01): the AkariOS-scoped facts still assert exactly 32 handlers, `Order` `[0..31]`, and the exact predecessor key sequence.
  </behavior>
  <action>
    Add 3 new `[Fact]` methods to `TweakHandlerOrderingTests.cs` mirroring the existing AkariOS-scoped facts' structure exactly, but filtering `provider.GetServices<ITweakHandler>().Where(h => h.Category == TweakCategory.Gaming)` and asserting the count/order-range/key-sequence documented in this task's `<behavior>`. Do not modify the existing AkariOS-scoped facts beyond what Plan 02-01 already did (they should already be correctly scoped and passing — this task only ADDS the Gaming-scoped equivalents, completing the pattern Plan 02-01 deliberately deferred).
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~TweakHandlerOrdering"</automated>
  </verify>
  <done>7 total ordering facts pass (3 pre-existing AkariOS-scoped + 3 new Gaming-scoped + the pre-existing error-resilience test), closing the phase's regression-test coverage.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~TweakHandlerOrdering"` passes all facts, including the 3 new Gaming-scoped ones.
    - Gaming-scoped handler count is exactly `11`.
    - Gaming-scoped `Order` values equal `[100..110]` with no gaps/duplicates and no overlap with the AkariOS `[0..31]` range.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full-phase verification pass + elevated launch check</name>
  <files>src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs</files>
  <read_first>src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs, src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs</read_first>
  <action>
    Run the repo-wide `ControlSet001` grep across `GamingGraphicsTweaks.cs` and `GamingWindowsTweaks.cs` (not just per-plan as Plans 02-01 through 02-04 did) — confirm zero matches. If any match is found, fix it before proceeding (should not happen if prior plans were followed correctly — this is a regression backstop, not expected work).

    Build and launch the app elevated for a final full-Gaming-catalog smoke test.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <automated>grep -rn "ControlSet001" src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs || echo "clean"</automated>
    <human-check>Launch the app elevated. Confirm the Gaming Tweaks page shows exactly 11 toggle rows, 2 dropdowns, 2 display-settings shortcut buttons, and the D-06 driver-tools button section. Spot-check 3 toggles against real system state via a separate elevated terminal: (1) "GPU HDCP Override" toggle state matches `reg query` on a real adapter's `RMHdcpKeyglobZero`; (2) "GPU MSI Mode" toggle state matches `MSISupported` for a real GPU's PnP instance path; (3) "High-Precision Timer Resolution" toggle state matches `GlobalTimerResolutionRequests`. Flip one non-destructive toggle (e.g. "GPU P0 State") on then off, confirming it round-trips to the real prior value both times. Confirm the Akari OS Tweaks page still shows exactly 32 toggles, unaffected by this phase. Confirm Home page and nav sidebar both show "Gaming Tweaks" as enabled alongside "Akari OS Tweaks."</human-check>
  </verify>
  <done>Zero ControlSet001 matches across the full Gaming handler surface; Gaming Tweaks page fully renders and spot-checked toggles match real registry/service state; Akari OS Tweaks page unaffected.</done>
  <acceptance_criteria>
    - `grep -rn "ControlSet001" src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs` returns no matches.
    - Elevated manual launch: Gaming Tweaks page shows exactly 11 toggle rows; 3 spot-checked toggles match real system state; Akari OS Tweaks (32 rows) and Home/nav (both pages enabled) unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full assembled 11-handler Gaming catalog + 2 dropdowns + D-06 action set | Same elevated-process-to-registry/service/subprocess boundaries established across Plans 02-01 through 02-06 — this plan only verifies the assembly, it introduces no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Repudiation | Gaming-scoped ordering regression test not asserting exact handler count/disjoint Order range until this final plan | medium | mitigate | `TweakHandlerOrderingTests` now has a Gaming-scoped fact asserting exactly 11 handlers with `Order` values `{100..110}`, alongside the still-passing AkariOS-scoped facts asserting exactly 32/`[0..31]` — a regression in either category is caught by CI going forward, matching Phase 1's own closing-invariant pattern (`01-07-PLAN.md`) |
</threat_model>

<verification>
- `dotnet test` passes the full ordering-regression suite (7 facts: 3 AkariOS + 3 Gaming + 1 error-resilience).
- `dotnet build` succeeds.
- The ControlSet001 grep returns clean.
- Elevated manual launch confirms all 11 Gaming toggles + 2 dropdowns + 2 shortcuts + D-06 buttons render with correct live state (human-check).
</verification>

<success_criteria>
The full Gaming Tweaks page works end-to-end: 11 correctly-ordered, category-scoped, CurrentControlSet-consistent toggles; 2 validated dropdowns; 2 one-shot shortcuts; the full D-06 action set with its accepted risk surfaced — closing out Phase 2's Success Criteria #1 and #2 (GAMING-01) without any regression to Phase 1's 32-tweak Akari OS Tweaks page.
</success_criteria>

<output>
Create `.planning/phases/02-gaming-tweaks/02-07-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- 3 new `[Fact]` methods in `TweakHandlerOrderingTests` (Gaming-scoped count/order-range/key-sequence)

## Flagged Assumptions (edge-probe items resolved as inapplicable, not force-fit)

- **Full-Gaming-catalog adjacency** ("when two things are exactly equal or just touch, do they merge, collide, or separate?"): resolved by the disjoint-`Order`-range assertion itself (`{100..110}` vs `[0..31]`, no overlap) — this test IS the adjacency probe for this domain, not a separate predicate to add, matching Phase 1's `01-07-PLAN.md` precedent for the same probe.
- **Concurrency (backstop, closed by this plan):** Plan 02-04's must_haves already carries the backstop-verification truth for PowerPlan/TimerResolution's multi-step interruption behavior; this plan's Task 2 elevated manual launch (spot-checking real toggle state after normal operation) is the practical confirmation pass for that backstop, not a re-statement of it.
- **`prohibitions: []` rationale**: this plan's actual prohibition (no `ControlSet001` drift across the full assembled Gaming handler surface) is enforced as an automated `<verify>` grep in Task 2 rather than restated as prose — the stronger, machine-checked form of the same constraint, matching Phase 1's `01-07-PLAN.md` precedent.
</output>
