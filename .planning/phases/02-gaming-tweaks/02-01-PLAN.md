---
phase: 02-gaming-tweaks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/AkariToolbox.App/Services/ITweakHandler.cs
  - src/AkariToolbox.App/Services/TweakCategory.cs
  - src/AkariToolbox.App/Services/TweakHandlers/WifiTweakHandler.cs
  - src/AkariToolbox.App/Services/TweakHandlers/RegistryTweaksBatchA.cs
  - src/AkariToolbox.App/Services/TweakHandlers/RegistryTweaksBatchB.cs
  - src/AkariToolbox.App/Services/TweakHandlers/BcdeditDismTweaks.cs
  - src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs
  - src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs
  - src/AkariToolbox.Tests/TweakCatalogTests.cs
  - src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs
  - src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs
  - src/AkariToolbox.Framework/Services/IRegistryService.cs
  - src/AkariToolbox.Framework/Services/RegistryService.cs
  - src/AkariToolbox.Framework/Services/IScriptRunner.cs
  - src/AkariToolbox.Framework/Services/ScriptRunner.cs
  - src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs
  - src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs
  - src/AkariToolbox.App/Views/GamingTweaksPage.xaml
  - src/AkariToolbox.App/Views/GamingTweaksPage.xaml.cs
  - src/AkariToolbox.App/ViewModels/HomeViewModel.cs
  - src/AkariToolbox.App/MainWindow.xaml.cs
  - src/AkariToolbox.App/App.xaml.cs
autonomous: true
requirements: [GAMING-01]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Adding TweakCategory to ITweakHandler and retrofitting all 32 existing Phase 1 handlers with Category => TweakCategory.AkariOS does not change any existing handler's GetState()/SetState() behavior — the AkariOS-scoped ordering test still asserts exactly 32 handlers with Order values [0..31] in the predecessor's exact key sequence, unchanged from Phase 1."
    - "AkariOSTweaksViewModel filters _catalog.Handlers to Category == TweakCategory.AkariOS and GamingTweaksViewModel filters to Category == TweakCategory.Gaming — the two pages never show each other's toggles, even though ITweakCatalog.Handlers itself stays one flat unfiltered list (RESEARCH.md Pattern 1)."
    - "The Gaming Tweaks card on Home and the Gaming Tweaks nav-sidebar entry are IsEnabled=true and navigate to GamingTweaksPage, matching the AkariOSTweaksPage precedent from Phase 1 (both now behave the same way — nothing is left disabled for GAMING-01's own scope)."
    - "The Hdcp toggle on the new Gaming Tweaks page reflects the real live registry state of RMHdcpKeyglobZero under every non-Configuration-filtered (now: 4-digit-named) GPU adapter subkey on load, and flipping it off restores the value the app itself last observed live rather than a hardcoded default — the same real-state-read/real-prior-state-revert guarantee as TWEAKS-01/TWEAKS-03."
    - "Calling ITweakCatalog.SetStateAsync for the Hdcp key twice with the same requested value is a no-op the second call — this is unchanged, inherited Phase 1 ITweakCatalog behavior (SetStateAsync compares live GetState() before writing) and now demonstrably applies to a Gaming-category handler too, not just AkariOS ones."
    - "IScriptRunner.RunEmbeddedScriptAsync extracts a named embedded resource to a GUID-suffixed temp file, runs it via powershell.exe -NoProfile -ExecutionPolicy Bypass -File, and deletes the temp file in a finally block regardless of success/failure — proven independently of any Gaming handler via its own unit test before any D-06 script depends on it."
  artifacts:
    - path: "src/AkariToolbox.App/Services/TweakCategory.cs"
      provides: "TweakCategory enum (AkariOS, Gaming) — the discriminator RESEARCH.md flags as the mandatory architectural prerequisite for this phase"
    - path: "src/AkariToolbox.Framework/Services/IRegistryService.cs"
      provides: "GetSubKeyNames(RegistryHive, string) — new capability needed by 4 of the 11 Gaming toggle handlers"
    - path: "src/AkariToolbox.Framework/Services/IScriptRunner.cs"
      provides: "RunEmbeddedScriptAsync(string resourceSuffix, string? arguments, TimeSpan? timeout) — generalizes DefenderTweakHandler.ExtractEmbeddedAsync, needed by the 6 D-06 network scripts (Plan 02-06)"
    - path: "src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs"
      provides: "HdcpTweakHandler (Order 100) — first Gaming-category ITweakHandler, proves the vertical slice"
    - path: "src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs"
      provides: "Gaming Tweaks page ViewModel — mirrors AkariOSTweaksViewModel, filters catalog.Handlers to Category==Gaming, auto-renders every later Gaming handler with zero further ViewModel changes"
    - path: "src/AkariToolbox.App/Views/GamingTweaksPage.xaml"
      provides: "Gaming Tweaks page — catalog-driven toggle list, extended by later plans with dropdowns/shortcuts/action buttons"
  key_links:
    - from: "src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs"
      to: "src/AkariToolbox.App/Services/TweakCatalog.cs"
      via: "catalog.Handlers.Where(h => h.Category == TweakCategory.Gaming), same TryGetStateAsync/write-through pattern as AkariOSTweaksViewModel"
      pattern: "TweakCategory.Gaming"
    - from: "src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs"
      to: "src/AkariToolbox.Framework/Services/IRegistryService.cs"
      via: "HdcpTweakHandler constructor-injects IRegistryService, calls GetSubKeyNames then GetValue/SetValue per adapter subkey"
      pattern: "GetSubKeyNames"
  prohibitions:
    - "Never write to ControlSet001 in any new Gaming registry handler, even where the source .ps1 script hardcodes it (7 Hdcp.ps1 itself uses the self-resolving CurrentControlSet alias, but P0 State/Amd/Intel/MsiMode/DevicePower/NetAdapterPower source scripts hardcode ControlSet001) — always target CurrentControlSet, matching every existing Phase 1 handler's convention (WifiTweakHandler etc.), since ControlSet001 is not guaranteed to be the active control set after a Last-Known-Good recovery boot."
---

<objective>
Land the mandatory architectural prerequisite this phase depends on — a `TweakCategory` discriminator on `ITweakHandler` that lets the existing flat `ITweakCatalog.Handlers` list be filtered per page without any catalog interface change — then prove the whole Gaming Tweaks vertical slice end-to-end on one real toggle (Hdcp) before the remaining 10 stateful handlers, 2 shortcuts, 2 dropdowns, and 6 network actions are built on top of it in later plans/waves. Also lands `IScriptRunner.RunEmbeddedScriptAsync`, an independent primitive Plan 02-06 needs, generalized from `DefenderTweakHandler`'s existing private `ExtractEmbeddedAsync` (that method itself is NOT touched — TWEAKS-02 keeps Defender's code path untouched).

Per D-02, this page is rebuilt from scratch on the Phase 1 `ITweakHandler` live-state pattern, not ported 1:1 from the predecessor's `GamingTweaksViewModel.cs`/`GamingTweaksPage.xaml.cs` (whose private `HKCU\Software\AkariTool` index dropdown mechanism and `HasState`/`SaveState` toggle pattern are reference-only, never carried over). Per D-01, nothing in this plan (or this phase) reads from, writes to, or launches files under `C:\PostInstall\` — `IPostInstallService`/`PostInstallService` stay registered-but-unused (reserved for Phase 4) and are never touched by any Gaming Tweaks handler. Per D-03, the predecessor's `TweakService.SetHdcp` (a `C:\PostInstall\...`-relative bat-script call) is not ported at all — `HdcpTweakHandler` below is a full native replacement built directly against `7 Hdcp.ps1`'s own registry logic, with no bat-script/PostInstall dependency of any kind.

**As a** user who wants to tune gaming/latency settings, **I want to** open a Gaming Tweaks page from Home/nav (currently disabled placeholders) and see a real toggle reflecting my machine's actual HDCP-override state, **so that** I can trust this page the same way I already trust the 32-toggle Akari OS Tweaks page from Phase 1.

<assumption_delta_decision>
**Pluralization detected:** `ITweakCatalog.Handlers` is going from one undifferentiated flat list (Phase 1, implicitly all "AkariOS" tweaks) to a category-partitioned one via the new `TweakCategory` discriminator (this plan introduces the second category, `Gaming`).
**Primary noun going forward:** `TweakCategory`-scoped handler — `AkariOS` becomes one explicit category value among (eventually) several, not the unstated default.
**Decision: add-alongside**, not promote. `ITweakCatalog.Handlers` itself keeps its existing flat-list shape and interface (RESEARCH.md Pattern 1, item 5); only the two consuming ViewModels add a `.Where(h => h.Category == ...)` filter. This matches RESEARCH's own explicit minimal-additive recommendation and keeps this plan's diff small and low-risk.
**What would force a later promote:** if a 3rd/4th category (Debloat, Misc) needs something beyond simple LINQ filtering — e.g. per-category ordering/pagination, or a catalog-level operation scoped to one category — `ITweakCatalog` would then warrant a structured/keyed `Handlers` shape (e.g. `IReadOnlyDictionary<TweakCategory, IReadOnlyList<ITweakHandler>>`) instead of a flat list with a filter convention repeated at every call site. RESEARCH.md Open Question 3 flags this explicitly as a "not now" — noted here so Phase 3/4 planning can revisit it deliberately rather than by accretion.
</assumption_delta_decision>

Purpose: prove the category-partitioned catalog architecture works end-to-end on real code before 10 more handlers depend on the same pattern, and unblock Plan 02-06's embedded-script capability independently.
Output: `TweakCategory` enum wired through all 33 handlers that will exist after this plan (32 retrofitted + 1 new), a working Gaming Tweaks page with one real toggle, and `IScriptRunner.RunEmbeddedScriptAsync` proven in isolation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-gaming-tweaks/02-CONTEXT.md
@.planning/phases/02-gaming-tweaks/02-RESEARCH.md
@.planning/phases/02-gaming-tweaks/02-PATTERNS.md
@src/AkariToolbox.App/Services/ITweakHandler.cs
@src/AkariToolbox.App/Services/TweakHandlers/WifiTweakHandler.cs
@src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs
@src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs
@src/AkariToolbox.Framework/Services/IRegistryService.cs
@src/AkariToolbox.Framework/Services/RegistryService.cs

# Canonical source script for the one real toggle this tracer wires
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/7 Hdcp.ps1
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TweakCategory discriminator + Hdcp toggle end-to-end</name>
  <files>src/AkariToolbox.App/Services/ITweakHandler.cs, src/AkariToolbox.App/Services/TweakCategory.cs, src/AkariToolbox.App/Services/TweakHandlers/WifiTweakHandler.cs, src/AkariToolbox.App/Services/TweakHandlers/RegistryTweaksBatchA.cs, src/AkariToolbox.App/Services/TweakHandlers/RegistryTweaksBatchB.cs, src/AkariToolbox.App/Services/TweakHandlers/BcdeditDismTweaks.cs, src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs, src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs, src/AkariToolbox.Tests/TweakCatalogTests.cs, src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs, src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs, src/AkariToolbox.Framework/Services/IRegistryService.cs, src/AkariToolbox.Framework/Services/RegistryService.cs, src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs, src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs, src/AkariToolbox.App/Views/GamingTweaksPage.xaml, src/AkariToolbox.App/Views/GamingTweaksPage.xaml.cs, src/AkariToolbox.App/ViewModels/HomeViewModel.cs, src/AkariToolbox.App/MainWindow.xaml.cs, src/AkariToolbox.App/App.xaml.cs</files>
  <read_first>src/AkariToolbox.App/Services/ITweakHandler.cs, src/AkariToolbox.App/Services/TweakHandlers/WifiTweakHandler.cs, src/AkariToolbox.App/Services/TweakHandlers/RegistryTweaksBatchB.cs, src/AkariToolbox.Tests/TweakCatalogTests.cs, src/AkariToolbox.Tests/TweakHandlerOrderingTests.cs, src/AkariToolbox.App/ViewModels/AkariOSTweaksViewModel.cs, src/AkariToolbox.App/Views/AkariOSTweaksPage.xaml, src/AkariToolbox.App/Views/AkariOSTweaksPage.xaml.cs, src/AkariToolbox.Framework/Services/IRegistryService.cs, src/AkariToolbox.Framework/Services/RegistryService.cs, src/AkariToolbox.App/ViewModels/HomeViewModel.cs, src/AkariToolbox.App/MainWindow.xaml.cs, src/AkariToolbox.App/App.xaml.cs lines 155-194, C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/7 Hdcp.ps1</read_first>
  <action>
    Create `src/AkariToolbox.App/Services/TweakCategory.cs`: `public enum TweakCategory { AkariOS, Gaming }` in namespace `AkariToolbox.App.Services`.

    Add `TweakCategory Category { get; }` to `ITweakHandler` (after `Order`, before `GetState()`), with a doc comment noting it discriminates which page (`AkariOSTweaksViewModel` vs `GamingTweaksViewModel`) a handler renders on, per RESEARCH.md Pattern 1.

    Add `public TweakCategory Category => TweakCategory.AkariOS;` to every one of the existing 32 handler classes, one line per class, placed directly after each class's existing `Order =>` property: `WifiTweakHandler` (`WifiTweakHandler.cs`), every handler class in `RegistryTweaksBatchA.cs` and `RegistryTweaksBatchB.cs`, every handler class in `BcdeditDismTweaks.cs`, `ClipboardTweakHandler`/`BluetoothTweakHandler`/`CdromTweakHandler`/the 4th handler in `ServiceBackedTweaks.cs`, and `DefenderTweakHandler` in `DefenderTweakHandler.cs`. This is mechanical and must not change any existing `GetState()`/`SetState()` logic. Also add `public TweakCategory Category => TweakCategory.AkariOS;` to the test double `FakeTweakHandler` in `src/AkariToolbox.Tests/TweakCatalogTests.cs` (it implements `ITweakHandler` directly and will fail to compile once `Category` is added to the interface).

    In `AkariOSTweaksViewModel.cs` line 30, change `foreach (var handler in _catalog.Handlers)` to `foreach (var handler in _catalog.Handlers.Where(h => h.Category == TweakCategory.AkariOS))`.

    In `TweakHandlerOrderingTests.cs`: change `Resolving_ITweakHandler_yields_exactly_32_handlers` and `Handler_order_values_span_0_to_31_with_no_gaps_or_duplicates` and `Handlers_sorted_by_order_match_predecessors_exact_key_sequence` to first filter `provider.GetServices<ITweakHandler>().Where(h => h.Category == TweakCategory.AkariOS)` before applying their existing assertions (count still 32, orders still `[0..31]`, key sequence unchanged) — this is the AkariOS-scoped re-assertion of Phase 1's closing invariant, now correctly scoped now that a second category exists. Do NOT add a Gaming-scoped exact-count assertion yet (the Gaming handler count changes across this phase's later plans) — that is Plan 02-07's job (final integration). It is fine and expected for this task's `dotnet test` run to show only 1 Gaming-category handler registered.

    In `IRegistryService.cs`, add `IReadOnlyList<string> GetSubKeyNames(RegistryHive hive, string subKeyPath);` with a doc comment: "Returns direct child subkey names under subKeyPath, or an empty list if absent." In `RegistryService.cs`, implement it following the file's existing convention exactly: `RegistryKey.OpenBaseKey(hive, RegistryView.Registry64)` (match the file's existing `Registry64` view, not `RegistryView.Default`), `.OpenSubKey(subKeyPath)`, null-check, return `subKey?.GetSubKeyNames() ?? []`.

    Create `src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs` with a shared `private static class`-level (or file-scoped) helper `GetGpuAdapterSubKeys(IRegistryService registry)` that calls `registry.GetSubKeyNames(RegistryHive.LocalMachine, GpuDisplayClassGuid)` where `GpuDisplayClassGuid = @"SYSTEM\CurrentControlSet\Control\Class\{4d36e968-e325-11ce-bfc1-08002be10318}"` (note: `CurrentControlSet`, not the source script's own `CurrentControlSet` — Hdcp already uses the safe alias, verified) and filters to names matching `^\d{4}$` via `System.Text.RegularExpressions.Regex.IsMatch` — this is the RESEARCH-recommended standardized filter (the safer 4-digit-adapter-name regex used by Amd/Intel Settings in source), used in place of `7 Hdcp.ps1`'s own `-notlike '*Configuration'` heuristic, so all 4 GPU-adapter-enumerating handlers (Hdcp here; P0State/AmdSettings/IntelSettings in Plan 02-02) share one filter, per RESEARCH's Don't-Hand-Roll table. Add `HdcpTweakHandler(IRegistryService registry) : ITweakHandler`: `Key => "gpuhdcp"`, `Title => "GPU HDCP Override"`, `Description => "Force-disable HDCP on every detected GPU adapter"`, `Order => 100`, `Category => TweakCategory.Gaming`. `GetState()` returns true only if every adapter subkey found by `GetGpuAdapterSubKeys` has `RMHdcpKeyglobZero == 1` (DWORD) — treat zero adapters found as `false` (nothing to report as "on"). `SetState(bool enabled)` writes `RMHdcpKeyglobZero` = `enabled ? 1 : 0` (DWORD) to every adapter subkey returned by `GetGpuAdapterSubKeys` — per `7 Hdcp.ps1:24-30,49-55`, the source writes an explicit 0 on the Default branch, not a delete, so `SetState(false)` writes 0, it does not call `DeleteValue`.

    Create `src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs`: copy `AkariOSTweaksViewModel.cs`'s full constructor/`TryGetStateAsync`/`OnTweakItemPropertyChanged` shape verbatim (same `ITweakCatalog`/`ILogConsoleService`/`DispatcherQueue` constructor signature), changing only `Title = "Gaming Tweaks";` and the handler filter to `_catalog.Handlers.Where(h => h.Category == TweakCategory.Gaming)`. This ViewModel needs NO further changes as later plans add more Gaming handlers — it is catalog-driven.

    Create `src/AkariToolbox.App/Views/GamingTweaksPage.xaml` + `.xaml.cs`: copy `AkariOSTweaksPage.xaml`'s `ItemsRepeater`/`ToggleSwitch` structure verbatim (same `TweakItem` binding), changing only the page's `x:Class`, the `TextBlock` title text to "Gaming Tweaks", and the `.xaml.cs` class name/constructor to `GamingTweaksPage(GamingTweaksViewModel viewModel)`.

    In `App.xaml.cs`, add `builder.Services.AddTransient<GamingTweaksViewModel>();` alongside the existing `AddTransient<AkariOSTweaksViewModel>();` line — `GamingTweaksPage` itself needs no explicit registration (resolved via `ActivatorUtilities.CreateInstance`, same as every other page).

    In `HomeViewModel.cs`, change the `"Gaming Tweaks"` card's `IsEnabled = false` to `IsEnabled = true` and its `Target = typeof(HomePage)` to `Target = typeof(GamingTweaksPage)` (add the `AkariToolbox.App.Views` using if not already present — `AkariOSTweaksPage` is already referenced there).

    In `MainWindow.xaml.cs`, change the `"Gaming Tweaks"` nav entry (currently `new("Gaming Tweaks", "\uE7FC", typeof(HomePage), IsEnabled: false)`) to `new("Gaming Tweaks", "\uE7FC", typeof(GamingTweaksPage))` (drop the `IsEnabled: false`, matching the enabled-entry style already used for `"Akari OS Tweaks"`).

    Build (`dotnet build AkariToolbox.slnx -c Debug`) and launch elevated to smoke-test the Gaming Tweaks page renders with exactly one toggle before continuing to Task 2/3.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~TweakHandlerOrdering"</automated>
    <human-check>Launch the app elevated. Confirm the Home page's Gaming Tweaks card and the nav sidebar's Gaming Tweaks entry are both clickable and navigate to a new page showing exactly one toggle ("GPU HDCP Override"). Toggle it on, then run `reg query "HKLM\SYSTEM\CurrentControlSet\Control\Class\{4d36e968-e325-11ce-bfc1-08002be10318}\<a 4-digit adapter subkey on your machine>" /v RMHdcpKeyglobZero` in a separate elevated terminal to confirm it reads `1`. Toggle it off, confirm the value reads `0`. Confirm the Akari OS Tweaks page still shows exactly 32 toggles (unaffected by this plan).</human-check>
  </verify>
  <done>TweakCategory exists and is applied to all 33 handlers; AkariOS-scoped ordering tests still pass at 32/[0..31]; Gaming Tweaks page is reachable from Home and nav, showing one real, revertible HDCP toggle.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - `dotnet test ... --filter "FullyQualifiedName~TweakHandlerOrdering"` passes 3/3 (AkariOS-scoped: still exactly 32, still `[0..31]`, still the exact predecessor sequence).
    - `grep -rc "TweakCategory.AkariOS" src/AkariToolbox.App/Services/TweakHandlers/` sums to 32 across all 6 files (one per existing handler).
    - `grep -n "TweakCategory.Gaming" src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs` matches at least once (HdcpTweakHandler).
    - `HomeViewModel.Cards.First(c => c.Title == "Gaming Tweaks").IsEnabled` is `true` and `.Target == typeof(GamingTweaksPage)`.
    - `MainWindow.NavItems.First(i => i.Label == "Gaming Tweaks").IsEnabled` is `true`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IScriptRunner.RunEmbeddedScriptAsync — generalized embedded-script primitive</name>
  <files>src/AkariToolbox.Framework/Services/IScriptRunner.cs, src/AkariToolbox.Framework/Services/ScriptRunner.cs, src/AkariToolbox.Tests/ScriptRunnerEmbeddedTests.cs</files>
  <read_first>src/AkariToolbox.Framework/Services/IScriptRunner.cs, src/AkariToolbox.Framework/Services/ScriptRunner.cs, src/AkariToolbox.App/Services/TweakHandlers/DefenderTweakHandler.cs (lines 317-333, ExtractEmbeddedAsync — read the pattern, do NOT modify this file or this method: TWEAKS-02 keeps Defender's code path untouched)</read_first>
  <behavior>
    - Test: `RunEmbeddedScriptAsync` given a resource suffix that matches an embedded test `.ps1` resource extracts it to a temp file, runs it via `powershell.exe`, and returns the script's real exit code (use a trivial embedded test script that runs `exit 7` to assert the exact code propagates).
    - Test: after `RunEmbeddedScriptAsync` completes (success or failure), the extracted temp file no longer exists (cleanup happens in a `finally` block).
    - Test: `RunEmbeddedScriptAsync` given a resource suffix with no matching embedded resource throws `FileNotFoundException` with a message naming the missing suffix (matches `DefenderTweakHandler.ExtractEmbeddedAsync`'s existing exception contract).
  </behavior>
  <action>
    Add `Task<int> RunEmbeddedScriptAsync(string resourceSuffix, string? arguments = null, TimeSpan? timeout = null);` to `IScriptRunner`, doc comment explaining it generalizes the extract-to-temp-then-run pattern first proven privately in `DefenderTweakHandler.ExtractEmbeddedAsync`, now promoted to a shared primitive per `ScriptRunner.cs`'s own doc comment ("A later phase that needs to run an embedded script should add that capability then") — this is that phase.

    Implement in `ScriptRunner.cs`: resolve `typeof(ScriptRunner).Assembly.GetManifestResourceNames()`, find the first name ending with `resourceSuffix` (case-insensitive `OrdinalIgnoreCase`), throw `FileNotFoundException($"Embedded resource not found: {resourceSuffix}")` if none found. Extract to `Path.Combine(Path.GetTempPath(), $"AkariToolbox-{Guid.NewGuid():N}-{resourceSuffix}")` (GUID-suffixed, not a fixed/predictable filename — T-02-SC hardening). In a `try`/`finally`: copy the resource stream to the temp file, then call `await RunProcessAsync("powershell.exe", $"-NoProfile -ExecutionPolicy Bypass -File \"{temp}\" {arguments}".TrimEnd(), timeout)` and return its result; in `finally`, `if (File.Exists(temp)) File.Delete(temp)` wrapped in its own try/catch (best-effort cleanup, never let a delete failure mask the real result).

    For the test, add one throwaway embedded `.ps1` test fixture (e.g. `src/AkariToolbox.Tests/Fixtures/exit7.ps1` containing only `exit 7`) declared as an `<EmbeddedResource Include="Fixtures/exit7.ps1" />` in `src/AkariToolbox.Tests/AkariToolbox.Tests.csproj` — resolve it via `typeof(ScriptRunnerEmbeddedTests).Assembly` in the test, not `ScriptRunner`'s own assembly (this proves the "search this type's own assembly" resolution mechanism generically, matching how a future `AkariToolbox.App`-assembly caller like Plan 02-06 will resolve its own resources). Note `RunEmbeddedScriptAsync` must search `typeof(ScriptRunner).Assembly` per its production implementation — for this test to exercise the real production code path against a resource the test project owns, embed the fixture in `AkariToolbox.Framework`'s own test-adjacent assembly instead (`AkariToolbox.Tests` referencing `AkariToolbox.Framework`) or add the fixture directly under `src/AkariToolbox.Framework/` as an `EmbeddedResource` and reference it from the test — choose whichever keeps `RunEmbeddedScriptAsync`'s assembly-resolution contract (`typeof(ScriptRunner).Assembly`) exercised for real, not mocked.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~ScriptRunnerEmbedded"</automated>
  </verify>
  <done>RunEmbeddedScriptAsync extracts, runs, and cleans up an embedded script; missing-resource and exit-code propagation are both covered by passing tests.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~ScriptRunnerEmbedded"` passes 3/3.
    - A run against the `exit7` fixture returns `7` from `RunEmbeddedScriptAsync`.
    - The extracted temp file is confirmed absent (`File.Exists` false) immediately after the call returns, in both the success and not-found-resource paths.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: P0State + MsiMode toggle handlers</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs, src/AkariToolbox.Tests/GamingGraphicsTweaksTests.cs</files>
  <read_first>src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs (Task 1's HdcpTweakHandler + GetGpuAdapterSubKeys), src/AkariToolbox.Framework/Services/IScriptRunner.cs, C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/8 P0 State.ps1, C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/9 Msi Mode.ps1</read_first>
  <behavior>
    - Test: `P0StateTweakHandler.GetState()` returns true only when every GPU adapter subkey (from a fake `IRegistryService` returning 2 fake 4-digit adapter names) has `DisableDynamicPstate == 1`; returns false if any adapter has `0` or the value is absent.
    - Test: `P0StateTweakHandler.SetState(true)` writes `DisableDynamicPstate = 1` (DWORD) to every adapter subkey returned by the fake registry; `SetState(false)` writes `0` (not a delete).
    - Test: `MsiModeTweakHandler.SetState(true)`, given a fake script-runner that returns two newline-separated PnP `InstanceId` strings, calls `IRegistryService.SetValue` with `MSISupported = 1` for a path built from each `InstanceId` under `SYSTEM\CurrentControlSet\Enum\{instanceId}\Device Parameters\Interrupt Management\MessageSignaledInterruptProperties` (assert `CurrentControlSet` appears in the path, not `ControlSet001`, per this plan's prohibitions).
  </behavior>
  <action>
    Add `P0StateTweakHandler(IRegistryService registry) : ITweakHandler` to `GamingGraphicsTweaks.cs`: `Key => "gpup0state"`, `Title => "GPU P0 State"`, `Description => "Force GPUs to stay at maximum performance state"`, `Order => 101`, `Category => TweakCategory.Gaming`. Reuse the shared `GetGpuAdapterSubKeys` helper from Task 1. `GetState()`/`SetState()` follow the exact same shape as `HdcpTweakHandler` but target `DisableDynamicPstate` (DWORD): On=1, Off=0 (explicit write, not delete — `8 P0 State.ps1:26-33,56-63`).

    Add `MsiModeTweakHandler(IRegistryService registry, IScriptRunner scriptRunner) : ITweakHandler`: `Key => "gpumsimode"`, `Title => "GPU MSI Mode"`, `Description => "Enable Message-Signaled Interrupts on every detected GPU"`, `Order => 102`, `Category => TweakCategory.Gaming`. Add a private async helper `GetGpuInstanceIdsAsync()` that calls `scriptRunner.RunProcessCaptureOutputAsync("powershell.exe", "-NoProfile -Command \"(Get-PnpDevice -Class Display).InstanceId\"")` and splits the result on `'\n'` with `StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries` (per RESEARCH.md Pattern 4/Code Examples). Because `ITweakHandler.GetState()`/`SetState(bool)` are synchronous, call `.GetAwaiter().GetResult()` on the PnP enumeration inside both methods (matching this codebase's existing synchronous `ITweakHandler` contract — do not change the interface to async, RESEARCH.md explicitly scopes this as a process-spawn behind a sync call, same as every other handler). For each `InstanceId`, build the registry path `$@"SYSTEM\CurrentControlSet\Enum\{instanceId}\Device Parameters\Interrupt Management\MessageSignaledInterruptProperties"` (CurrentControlSet, deviating from `9 Msi Mode.ps1:22-28`'s own hardcoded `ControlSet001` per this plan's prohibition) and read/write `MSISupported` (DWORD): On=1, Off=0. `GetState()` returns true only if every enumerated adapter has `MSISupported == 1`; zero adapters found returns `false`.

    Add both handlers' Order values (100 already used by Hdcp — confirm 101/102 are the values used, no collision) and both `Category => TweakCategory.Gaming`.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~GamingGraphicsTweaks"</automated>
  </verify>
  <done>P0State and MsiMode toggles compile, pass their unit tests, and render on the Gaming Tweaks page automatically (catalog-driven, no ViewModel change needed) alongside Hdcp.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~GamingGraphicsTweaks"` passes all new tests.
    - `grep -n "ControlSet001" src/AkariToolbox.App/Services/TweakHandlers/GamingGraphicsTweaks.cs` returns no matches.
    - `P0StateTweakHandler.Order == 101` and `MsiModeTweakHandler.Order == 102`, both `Category == TweakCategory.Gaming`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elevated process -> Windows registry (HKLM Class GUID subkeys, PnP Enum subkeys) | Same elevated-process-to-registry boundary as every Phase 1 handler — no new boundary introduced, only new registry paths under the existing `requireAdministrator` gate |
| GamingTweaksViewModel/Page -> ITweakCatalog | Mirrors the exact Phase 1 AkariOSTweaksViewModel trust boundary — no new UI-to-backend boundary shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | TweakCategory filter in AkariOSTweaksViewModel/GamingTweaksViewModel | medium | mitigate | The AkariOS-scoped ordering regression test (Task 1) still asserts exactly 32 AkariOS-category handlers with orders `[0..31]` — a mis-tagged handler (e.g. a new Gaming handler accidentally left at `Category => AkariOS`) fails this test immediately rather than silently leaking onto the wrong page |
| T-02-02 | Tampering | GetSubKeyNames / Hdcp,P0State,MsiMode registry+PnP writes | high | mitigate | Requires the process's existing `requireAdministrator` elevation (APP-01, unchanged); every write targets `CurrentControlSet`, never `ControlSet001`; PnP `InstanceId` values come from `Get-PnpDevice` output (not free user text) but are still only interpolated into a fixed, well-known registry-path template, never into a shell command string |
| T-02-03 | Elevation of Privilege | GamingTweaksPage/nav newly exposed | low | accept | The entire app already runs `requireAdministrator` (APP-01) before any page renders — adding a page introduces no new elevation surface |
| T-02-04 | Denial of Service | RunEmbeddedScriptAsync temp-file extraction | low | mitigate | Extraction path is `Path.GetTempPath()` + a GUID-suffixed filename (not attacker-predictable), deleted in a `finally` block regardless of outcome (Task 2's own test asserts this) |
</threat_model>

<verification>
- `dotnet build AkariToolbox.slnx -c Debug` succeeds.
- `dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj` passes, including the re-scoped AkariOS ordering tests (still 32/[0..31]) and all new Gaming/ScriptRunner tests.
- Elevated manual launch: Gaming Tweaks reachable from Home + nav, 3 toggles render (Hdcp, P0State, MsiMode) and reflect real system state.
</verification>

<success_criteria>
The category-partitioned catalog architecture is proven end-to-end: 32 existing AkariOS handlers unaffected, 3 new Gaming handlers register and render correctly on a new page, and the embedded-script primitive Plan 02-06 needs exists and is independently tested.
</success_criteria>

<output>
Create `.planning/phases/02-gaming-tweaks/02-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `TweakCategory` enum (`AkariOS`, `Gaming`)
- `ITweakHandler.Category` (new interface member)
- `IRegistryService.GetSubKeyNames(RegistryHive, string)` / `RegistryService` implementation
- `IScriptRunner.RunEmbeddedScriptAsync(string, string?, TimeSpan?)` / `ScriptRunner` implementation
- `HdcpTweakHandler`, `P0StateTweakHandler`, `MsiModeTweakHandler` (Order 100/101/102, Category=Gaming)
- `GetGpuAdapterSubKeys` shared helper (GamingGraphicsTweaks.cs)
- `GamingTweaksViewModel`, `GamingTweaksPage` (catalog-driven, auto-renders future Gaming handlers)
- Home/nav Gaming Tweaks entries flipped to enabled

## Flagged Assumptions (spec-less-probe-fallback edges resolved)

- **Idempotency (resolved, explicit):** `SetState(enabled)` when `GetState()` already equals `enabled` is a no-op for the new Gaming handlers — this is unchanged, inherited `ITweakCatalog.SetStateAsync` behavior from Phase 1 (compares live `GetState()` before writing), now demonstrated end-to-end on `HdcpTweakHandler` in this plan's must_haves truths and acceptance criteria, not re-implemented per-handler.
- **GAMING-01 wording vs D-10 (RESEARCH.md Open Question 1):** REQUIREMENTS.md's GAMING-01 text mentions "service configuration dropdowns," written before 02-CONTEXT.md's D-10 explicitly dropped the AkariOS/Windows-Default Services-preset dropdown with no replacement. This plan set (and RESEARCH.md) treats GAMING-01 as satisfied by the 11 toggle handlers plus the SvcHost/Win32PrioritySeparation dropdowns (Plan 02-05) — D-09/D-10 are the more current, more specific decisions and supersede the general pre-discussion requirement wording. No REQUIREMENTS.md text edit is made by this plan (only GAMING-02's retirement was an explicit, separately-flagged edit in 02-CONTEXT.md D-12).
</output>
