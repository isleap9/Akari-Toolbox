---
phase: 02-gaming-tweaks
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs
  - src/AkariToolbox.Tests/GamingWindowsTweaksTests.cs
autonomous: true
requirements: [GAMING-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "DevicePowerSavingsTweakHandler.SetState(true) writes the documented power-management values under every Device Parameters/WDF subkey found by recursing HKLM:\\SYSTEM\\CurrentControlSet\\Enum\\{ACPI,HID,PCI,USB}, and preserves the ACPI branch's source-authored SeleactiveSuspendEnabled misspelling exactly as-is (not silently corrected) — the HID/PCI/USB branches use the correctly-spelled SelectiveSuspendEnabled, matching the source script's own inconsistency verbatim."
    - "NetAdapterPowerSavingsTweakHandler.SetState(true) writes the documented 12 REG_SZ '0' values plus PnPCapabilities=24 (DWORD) to every 4-digit subkey found under the network-adapter class GUID; SetState(false) deletes all of them."
    - "WriteCacheFlushTweakHandler implements its On and Off paths as two independently-targeted enumeration calls (On matches subkeys named exactly 'Device Parameters' and creates/writes a child 'Disk' subkey there; Off matches subkeys named exactly 'Disk' directly and deletes them) — never a single shared enumeration helper reused for both directions, since the source script's own On/Off match targets are asymmetric (Pitfall 4) and a shared helper would silently no-op one direction."
  artifacts:
    - path: "src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs"
      provides: "DevicePowerSavingsTweakHandler (Order 105), NetAdapterPowerSavingsTweakHandler (Order 106), WriteCacheFlushTweakHandler (Order 108) — 3 of the 6 Windows folder toggles"
  key_links: []
  prohibitions:
    - "Never write to ControlSet001 in any handler in this file, even though 25 Device Manager Power Savings & Wake.ps1 and 26 Network Adapter Power Savings & Wake.ps1 both hardcode it — target CurrentControlSet."
    - "Never silently 'fix' the SeleactiveSuspendEnabled typo in the ACPI branch of DevicePowerSavingsTweakHandler into the correctly-spelled SelectiveSuspendEnabled — porting exactly as authored (per D-04/D-07's philosophy) means the ACPI branch's write is a genuine no-op against real Windows behavior, same as the source script; silently correcting it would newly change real-world behavior beyond what was ported."
---

<objective>
Start the `6 Windows` folder's 6-toggle scope (D-07) with the 3 handlers that are pure registry-enumeration (no external process spawn): `DevicePowerSavingsTweakHandler`, `NetAdapterPowerSavingsTweakHandler`, and `WriteCacheFlushTweakHandler`. This plan touches only `GamingWindowsTweaks.cs` (a new file, no overlap with Plan 02-02's `GamingGraphicsTweaks.cs`/ViewModel/Page changes), so it runs in the same wave as Plan 02-02. `NetworkIpv4OnlyTweakHandler`, `PowerPlanTweakHandler`, and `TimerResolutionTweakHandler` (the 3 remaining, process-spawning `6 Windows` handlers) are Plan 02-04.

Purpose: prove the multi-class-recursion registry-enumeration pattern needed for device/network power-savings tweaks, and preserve two source-script gotchas (the ACPI typo, the asymmetric Disk/Device-Parameters match) exactly as documented in RESEARCH.md rather than silently "fixing" them.
Output: 3 more Gaming toggles live on the Gaming Tweaks page (catalog-driven, no ViewModel change needed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-gaming-tweaks/02-RESEARCH.md
@.planning/phases/02-gaming-tweaks/02-PATTERNS.md
@src/AkariToolbox.Framework/Services/IRegistryService.cs
@src/AkariToolbox.App/Services/TweakHandlers/ServiceBackedTweaks.cs

# Canonical source scripts
@C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/25 Device Manager Power Savings & Wake.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/26 Network Adapter Power Savings & Wake.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/28 Write Cache Buffer Flushing.ps1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DevicePowerSavingsTweakHandler</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs, src/AkariToolbox.Tests/GamingWindowsTweaksTests.cs</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/25 Device Manager Power Savings & Wake.ps1 lines 23-118, 128-224, .planning/phases/02-gaming-tweaks/02-RESEARCH.md Pitfall 5</read_first>
  <behavior>
    - Test: `SetState(true)`, given a fake registry enumerating one `Device Parameters` subkey per class (`ACPI`, `HID`, `PCI`, `USB`) and one `WDF` subkey, writes `EnhancedPowerManagementEnabled=0`, `SelectiveSuspendOn=0`, `WaitWakeEnabled=0` (all DWORD) to every `Device Parameters` match, and `IdleInWorkingState=0` (DWORD) to every `WDF` match.
    - Test: for the `ACPI`-class `Device Parameters` match specifically, `SetState(true)` writes the value name `SeleactiveSuspendEnabled` (the source-authored misspelling, verbatim) with BINARY `00`; for `HID`/`PCI`/`USB`-class matches, it writes the correctly-spelled `SelectiveSuspendEnabled` with the same BINARY value — assert both spellings appear, scoped to the correct class.
    - Test: `SetState(false)` deletes every value written by `SetState(true)` (all of `EnhancedPowerManagementEnabled`, `SelectiveSuspendOn`/`SeleactiveSuspendEnabled`, `WaitWakeEnabled`, `IdleInWorkingState`) rather than restoring specific prior values, matching the source script's own delete-based revert.
  </behavior>
  <action>
    Add `DevicePowerSavingsTweakHandler(IRegistryService registry) : ITweakHandler` to `GamingWindowsTweaks.cs`: `Key => "devpowersavings"`, `Title => "Device Manager Power Savings"`, `Description => "Disable power management/selective-suspend on ACPI, HID, PCI, and USB devices"`, `Order => 105`, `Category => TweakCategory.Gaming`. Recurse `HKLM:\SYSTEM\CurrentControlSet\Enum\{ACPI,HID,PCI,USB}` (four separate top-level classes, `CurrentControlSet` not `ControlSet001`) via `GetSubKeyNames` (recursively — this needs multi-level subkey walking since device instance paths are nested several levels deep under each class; implement a small private recursive helper that walks down via repeated `GetSubKeyNames` calls looking for child subkeys literally named `"Device Parameters"` or `"WDF"`). For each `Device Parameters` match found under the `ACPI` class specifically, write `SeleactiveSuspendEnabled` (BINARY `00`, preserving the source's exact misspelling — see this plan's prohibitions); for matches under `HID`/`PCI`/`USB`, write `SelectiveSuspendEnabled` (BINARY `00`, correct spelling) — this class-conditional spelling difference is intentional and must be preserved. For every `Device Parameters` match regardless of class, also write `EnhancedPowerManagementEnabled=0`, `SelectiveSuspendOn=0`, `WaitWakeEnabled=0` (all DWORD). For every `WDF` match, write `IdleInWorkingState=0` (DWORD). `SetState(false)` calls `DeleteValue` for every one of these value names at every matched subkey (delete-based revert, matches `25 ...:128-224`). `GetState()` checks a representative subkey/value (e.g. the first `Device Parameters` match's `EnhancedPowerManagementEnabled == 0`) — return `false` if no matching subkeys are found at all.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~DevicePowerSavings"</automated>
  </verify>
  <done>DevicePowerSavingsTweakHandler compiles, passes tests, preserves the ACPI SeleactiveSuspendEnabled typo exactly.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~DevicePowerSavings"` passes all tests.
    - `grep -n "SeleactiveSuspendEnabled" src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs` matches at least once (typo preserved, with an adjacent code comment explaining why per RESEARCH.md Pitfall 5).
    - `grep -n "ControlSet001" src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs` returns no matches.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NetAdapterPowerSavingsTweakHandler</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs, src/AkariToolbox.Tests/GamingWindowsTweaksTests.cs</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/26 Network Adapter Power Savings & Wake.ps1 lines 24-62, 74-113</read_first>
  <behavior>
    - Test: `SetState(true)`, given a fake registry returning 2 fake 4-digit subkeys under the network-adapter class GUID, writes `PnPCapabilities=24` (DWORD) plus 12 REG_SZ "0" values (`AdvancedEEE`, `EEE`, `EEELinkAdvertisement`, `SipsEnabled`, `ULPMode`, `GigaLite`, `EnableGreenEthernet`, `PowerSavingMode`, `S5WakeOnLan`, `WakeOnMagicPacket`, `ModernStandbyWoLMagicPacket`, `WakeOnPattern`, `WakeOnLink`) to each subkey.
    - Test: `SetState(false)` deletes all 13 values from each subkey.
  </behavior>
  <action>
    Add `NetAdapterPowerSavingsTweakHandler(IRegistryService registry) : ITweakHandler`: `Key => "netpowersavings"`, `Title => "Network Adapter Power Savings"`, `Description => "Disable energy-efficient Ethernet and wake-on-LAN features for lower latency"`, `Order => 106`, `Category => TweakCategory.Gaming`. Enumerate 4-digit subkeys (`^\d{4}$` regex, matching this plan's shared convention) under `SYSTEM\CurrentControlSet\Control\Class\{4d36e972-e325-11ce-bfc1-08002be10318}` (`CurrentControlSet`, not `ControlSet001` per `26 ...:24-62`'s own hardcoding). `SetState(true)` writes `PnPCapabilities=24` (DWORD) and, as REG_SZ `"0"` each: `AdvancedEEE`, `EEE`, `EEELinkAdvertisement`, `SipsEnabled`, `ULPMode`, `GigaLite`, `EnableGreenEthernet`, `PowerSavingMode`, `S5WakeOnLan`, `WakeOnMagicPacket`, `ModernStandbyWoLMagicPacket`, `WakeOnPattern`, `WakeOnLink` (per `26 ...:26-45` — verify each exact value name against the source file during implementation, since some carry vendor-specific prefixes in the actual script; read the file directly rather than relying solely on this summary). `SetState(false)` deletes all 13 values. `GetState()` checks `PnPCapabilities == 24` on the first matched adapter subkey; no adapters found returns `false`.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~NetAdapterPowerSavings"</automated>
  </verify>
  <done>NetAdapterPowerSavingsTweakHandler compiles, passes tests, all 13 values round-trip correctly.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~NetAdapterPowerSavings"` passes all tests.
    - `NetAdapterPowerSavingsTweakHandler.Order == 106`, `Category == TweakCategory.Gaming`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WriteCacheFlushTweakHandler — asymmetric On/Off match targets</name>
  <files>src/AkariToolbox.App/Services/TweakHandlers/GamingWindowsTweaks.cs, src/AkariToolbox.Tests/GamingWindowsTweaksTests.cs</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/6 Windows/28 Write Cache Buffer Flushing.ps1 lines 26-37, 49-60, .planning/phases/02-gaming-tweaks/02-RESEARCH.md Pitfall 4</read_first>
  <behavior>
    - Test: `SetState(true)`, given a fake registry enumerating subkeys named exactly `"Device Parameters"` under `SCSI`/`NVME` device paths, creates a child `Disk` subkey under each match and writes `CacheIsPowerProtected=1` (DWORD) there.
    - Test: `SetState(false)`, given a fake registry enumerating subkeys named exactly `"Disk"` directly (a different match target than the On path), deletes each entire `Disk` subkey.
    - Test: a single fake registry tree containing both a `Device Parameters\Disk` structure (created by a prior On call) and confirms `SetState(false)` against that same tree successfully deletes it — i.e. the Off path's own `"Disk"`-named match correctly finds what the On path created, proving the two independently-implemented enumeration targets are compatible in practice despite matching on different literal names.
  </behavior>
  <action>
    Add `WriteCacheFlushTweakHandler(IRegistryService registry) : ITweakHandler`: `Key => "writecacheflush"`, `Title => "Write Cache Buffer Flushing"`, `Description => "Mark disk write caches as power-protected to reduce flush overhead"`, `Order => 108`, `Category => TweakCategory.Gaming`. Implement `SetState(true)` and `SetState(false)` as two SEPARATE, independently-targeted enumeration methods per RESEARCH.md Pitfall 4 and this plan's must_haves — do NOT share one enumeration helper between them: `SetState(true)` recurses `SCSI`/`NVME` device paths under `CurrentControlSet\Enum` looking for child subkeys literally named `"Device Parameters"`, and for each match creates (or opens) a child `Disk` subkey and writes `CacheIsPowerProtected=1` (DWORD) there. `SetState(false)` recurses the same `SCSI`/`NVME` device paths but looks for child subkeys literally named `"Disk"` directly (not `"Device Parameters"`) and deletes each entire matched subkey via `DeleteSubKeyTree` (added in Plan 02-02 Task 2 — reuse it, do not reimplement). `GetState()` checks whether any `Disk` subkey with `CacheIsPowerProtected == 1` exists under the enumerated paths.
  </action>
  <verify>
    <automated>dotnet test src/AkariToolbox.Tests/AkariToolbox.Tests.csproj --filter "FullyQualifiedName~WriteCacheFlush"</automated>
  </verify>
  <done>WriteCacheFlushTweakHandler's On and Off paths use genuinely different, independently-tested enumeration targets and correctly round-trip against each other.</done>
  <acceptance_criteria>
    - `dotnet test ... --filter "FullyQualifiedName~WriteCacheFlush"` passes all 3 tests, including the round-trip test.
    - Source review: `SetState(true)` and `SetState(false)` are NOT implemented by calling one shared private enumeration method with a parameter switching the match string — they are two distinct code paths (verifies Pitfall 4 is genuinely addressed, not just superficially).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elevated process -> device-class/adapter-class registry subkeys (Enum tree, network-adapter class GUID) | Same elevated-process-to-registry boundary as Plans 02-01/02-02, now covering the `Enum` device tree and a second class GUID |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | Multi-class-recursion registry enumeration (Device/WDF/network-adapter subkeys) | high | mitigate | `CurrentControlSet` only (never `ControlSet001`); the ACPI-branch `SeleactiveSuspendEnabled` typo is preserved as-authored per D-04's port-exactly-as-authored philosophy and documented in-code, not silently "fixed" into new unintended behavior |
| T-02-08 | Repudiation | WriteCacheFlushTweakHandler's asymmetric On/Off match targets (Pitfall 4) | medium | mitigate | Implemented as two explicit, independently-unit-tested enumeration paths (including a round-trip test) so a silent no-op on either direction is caught by CI, not just manual QA |
</threat_model>

<verification>
- `dotnet build AkariToolbox.slnx -c Debug` succeeds.
- `dotnet test` passes all DevicePowerSavings/NetAdapterPowerSavings/WriteCacheFlush unit tests.
- Elevated manual launch: Gaming Tweaks page shows 8 toggles total (5 from Graphics + these 3).
</verification>

<success_criteria>
3 of the 6 Windows-folder toggles are live: device/network power-savings and write-cache-flush, all preserving their source scripts' exact (including buggy/asymmetric) behavior per D-07's port-as-authored philosophy.
</success_criteria>

<output>
Create `.planning/phases/02-gaming-tweaks/02-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- `DevicePowerSavingsTweakHandler` (Order 105), `NetAdapterPowerSavingsTweakHandler` (Order 106), `WriteCacheFlushTweakHandler` (Order 108)

## Flagged Assumptions

- **NetAdapterPowerSavings exact value-name list**: RESEARCH.md's table lists 12-13 REG_SZ value names at a summary level; Task 2's `<action>` explicitly instructs reading the source script directly during implementation rather than trusting only this plan's restated list, since exact vendor-prefixed value names matter for correctness and were not individually re-verified character-by-character in this planning pass.
</output>
