---
phase: 02-gaming-tweaks
plan: 06
type: execute
wave: 4
depends_on: ["02-01", "02-05"]
files_modified:
  - src/AkariToolbox.App/AkariToolbox.App.csproj
  - src/AkariToolbox.App/Resources/GamingScripts/driverclean-auto.ps1
  - src/AkariToolbox.App/Resources/GamingScripts/driverclean-manual.ps1
  - src/AkariToolbox.App/Resources/GamingScripts/directx.ps1
  - src/AkariToolbox.App/Resources/GamingScripts/cpp.ps1
  - src/AkariToolbox.App/Resources/GamingScripts/driverinstalllatest-nvidia.ps1
  - src/AkariToolbox.App/Resources/GamingScripts/driverinstalllatest-amd.ps1
  - src/AkariToolbox.App/Resources/GamingScripts/driverinstalllatest-intel.ps1
  - src/AkariToolbox.App/Resources/GamingScripts/nvidiasettings-recommended.ps1
  - src/AkariToolbox.App/Resources/GamingScripts/nvidiasettings-default.ps1
  - src/AkariToolbox.App/Resources/GamingScripts/driverinstalldebloat.ps1
  - src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs
  - src/AkariToolbox.App/Views/GamingTweaksPage.xaml
autonomous: true
requirements: [GAMING-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the 6 D-06 scripts' underlying install logic is embedded byte-for-byte as authored per branch (Read-Host menu wrapper stripped, everything else unchanged) — no attempted decomposition into toggle/revert state, since D-06 explicitly scopes these as one-shot actions with no added verification."
    - "Every D-06 action button click is logged via ILogConsoleService with a visible statement that no SHA256/signature verification is performed on the downloaded binary before execution, surfacing the accepted risk (D-06) to the user at the moment of action rather than only in a code comment or SUMMARY.md nobody reads at runtime."
  artifacts:
    - path: "src/AkariToolbox.App/Resources/GamingScripts/"
      provides: "Embedded .ps1 resources for all 6 D-06 network-dependent one-shot actions, split per source menu branch"
  key_links:
    - from: "src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs"
      to: "src/AkariToolbox.Framework/Services/IScriptRunner.cs"
      via: "One RelayCommand per D-06 branch calls scriptRunner.RunEmbeddedScriptAsync(resourceSuffix) — the primitive built in Plan 02-01 Task 2"
      pattern: "RunEmbeddedScriptAsync"
  prohibitions: []
---

<objective>
Port the 6 D-06 network-dependent one-shot scripts (`1 Driver Clean.ps1`, `2 Driver Install Latest.ps1`, `3 Driver Install Debloat & Settings.ps1`, `4 Nvidia Settings.ps1`, `10 DirectX.ps1`, `11 C++.ps1`) as embedded PowerShell resources, one per source menu branch, run via `IScriptRunner.RunEmbeddedScriptAsync` (Plan 02-01). Per D-06, these scripts keep their live download-and-install behavior exactly as authored, with NO added SHA256/signature verification for v1 — this is an explicit, accepted user decision (not silently re-added, but surfaced plainly at both the code and runtime-UI level).

Purpose: complete GAMING-01's full toggle/action surface — the 6 D-06 buttons are the last pieces of the `5 Graphics` folder's D-04/D-05/D-06 scope.
Output: 6-10 action buttons on the Gaming Tweaks page (one per source menu branch), each launching the exact ported install logic.

**Scope boundary (D-11):** these D-06 buttons are one-shot installer/driver actions the source scripts themselves already perform — they are explicitly NOT a revival of the predecessor's ~29-button NVIDIA/AMD/Useful-Tools quick-launch grid, which D-11 drops entirely with no replacement. Do not add a generic "launch third-party tool X" button beyond the 6 D-06 scripts' own ported branches.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-gaming-tweaks/02-RESEARCH.md
@.planning/phases/02-gaming-tweaks/02-PATTERNS.md
@src/AkariToolbox.App/AkariToolbox.App.csproj
@src/AkariToolbox.Framework/Services/IScriptRunner.cs
@src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs
@src/AkariToolbox.App/Views/GamingTweaksPage.xaml

# Canonical source scripts (D-06, network-dependent, ported exactly as authored per branch)
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/1 Driver Clean.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/2 Driver Install Latest.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/3 Driver Install Debloat & Settings.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/4 Nvidia Settings.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/10 DirectX.ps1
@C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/11 C++.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver Clean (2 branches), DirectX, C++ — no-menu and simple-menu scripts</name>
  <files>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Resources/GamingScripts/driverclean-auto.ps1, src/AkariToolbox.App/Resources/GamingScripts/driverclean-manual.ps1, src/AkariToolbox.App/Resources/GamingScripts/directx.ps1, src/AkariToolbox.App/Resources/GamingScripts/cpp.ps1, src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs, src/AkariToolbox.App/Views/GamingTweaksPage.xaml</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/1 Driver Clean.ps1, C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/10 DirectX.ps1, C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/11 C++.ps1, src/AkariToolbox.App/AkariToolbox.App.csproj (Defender's EmbeddedResource Include pattern)</read_first>
  <action>
    Read `1 Driver Clean.ps1` in full. It has a 2-branch `Read-Host` menu (DDU Auto / DDU Manual per RESEARCH.md); both branches share the initial 7-Zip/DDU download+extract+`Settings.xml` setup (per `1 ...:35,52,105-127,166`) then diverge on how DDU itself is invoked (headless Auto vs interactive Manual, via a Safe-Mode `bcdedit`+`RunOnce` reboot per `1 ...:213-241`). Create `Resources/GamingScripts/driverclean-auto.ps1` and `driverclean-manual.ps1`: each contains the FULL shared setup logic plus its own branch's DDU-invocation tail, with the `Read-Host`/`Write-Host` menu wrapper stripped — byte-identical to the source branch's underlying logic otherwise (D-06 "exactly as authored").

    Read `10 DirectX.ps1` and `11 C++.ps1` in full — both are confirmed no-menu, single linear scripts (no `Read-Host` to strip). Create `Resources/GamingScripts/directx.ps1` and `cpp.ps1` as direct copies of the source scripts' full content, unchanged.

    Add `<EmbeddedResource Include="Resources\GamingScripts\driverclean-auto.ps1" />`, `driverclean-manual.ps1`, `directx.ps1`, `cpp.ps1` to `AkariToolbox.App.csproj`, in the same `<ItemGroup>` style as the existing Defender `NoDefender.cab`/`DisableDefender.ps1` entries (a new `<ItemGroup>` with a comment noting these are D-06 network-dependent scripts, no SHA256/signature verification per explicit user decision — CONTEXT.md D-06).

    Add 4 `[RelayCommand]` methods to `GamingTweaksViewModel`: `RunDriverCleanAutoAsync`, `RunDriverCleanManualAsync`, `RunDirectXAsync`, `RunCppAsync`, each calling `log.Log("[GAMING] Launching {name} — downloaded binary is NOT SHA256/signature-verified before execution (accepted risk, D-06).")` FIRST, then `await scriptRunner.RunEmbeddedScriptAsync("driverclean-auto.ps1")` (etc., matching each resource's exact filename suffix). Add 4 corresponding `Button`s to `GamingTweaksPage.xaml` grouped under a `TextBlock` section header "Driver Tools (downloads third-party binaries — not integrity-verified)" — this heading text itself is the runtime UI surfacing of the accepted D-06 risk, not just a log line.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <human-check>Launch elevated, navigate to Gaming Tweaks. Confirm the 4 new buttons appear under a visible "not integrity-verified" section header. Click "DirectX" (the simplest, shortest-running script) and confirm the log console shows the pre-launch risk-disclosure line before any download activity.</human-check>
  </verify>
  <done>4 of 6 D-06 scripts (Driver Clean x2 branches, DirectX, C++) are embedded, wired, and visibly disclose the no-verification risk before running.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - `grep -c "EmbeddedResource Include=\"Resources\\\\GamingScripts" src/AkariToolbox.App/AkariToolbox.App.csproj` returns at least `4` after this task.
    - `grep -n "not.*SHA256\|not.*verified\|accepted risk" src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs` matches (risk disclosure present in code).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Driver Install Latest (3 branches) + Nvidia Settings (2 branches)</name>
  <files>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Resources/GamingScripts/driverinstalllatest-nvidia.ps1, src/AkariToolbox.App/Resources/GamingScripts/driverinstalllatest-amd.ps1, src/AkariToolbox.App/Resources/GamingScripts/driverinstalllatest-intel.ps1, src/AkariToolbox.App/Resources/GamingScripts/nvidiasettings-recommended.ps1, src/AkariToolbox.App/Resources/GamingScripts/nvidiasettings-default.ps1, src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs, src/AkariToolbox.App/Views/GamingTweaksPage.xaml</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/2 Driver Install Latest.ps1, C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/4 Nvidia Settings.ps1</read_first>
  <action>
    Read `2 Driver Install Latest.ps1` in full. It has a 3-branch `Read-Host` menu (NVIDIA/AMD/Intel, per RESEARCH.md — NVIDIA/AMD download from official vendor domains, Intel opens a search page with no direct download, per `2 ...:41,47,51,55,64-78,87-88`). Create `driverinstalllatest-nvidia.ps1`, `-amd.ps1`, `-intel.ps1`, each containing its own branch's logic verbatim with the menu wrapper stripped.

    Read `4 Nvidia Settings.ps1` in full. It has an unconditional `winget install "9NF8H0H7WMLT"` step followed by a 2-branch menu (per RESEARCH.md, `4 ...:22,35,38-39,85,328`). Create `nvidiasettings-recommended.ps1` and `nvidiasettings-default.ps1`, each containing the unconditional winget step PLUS its own branch's logic, menu wrapper stripped. Preserve the winget call's existing `try`/`catch` tolerance (per RESEARCH.md's Environment Availability note — `winget.exe` may be absent, the source script already tolerates this, do not add new error handling beyond what's authored).

    Add the 5 `<EmbeddedResource Include=...>` entries to `AkariToolbox.App.csproj`. Add 5 `[RelayCommand]` methods to `GamingTweaksViewModel` (`RunDriverInstallLatestNvidiaAsync`, `-AmdAsync`, `-IntelAsync`, `RunNvidiaSettingsRecommendedAsync`, `RunNvidiaSettingsDefaultAsync`), each with the same pre-launch risk-disclosure log line as Task 1, then `RunEmbeddedScriptAsync` with the matching resource suffix. Add 5 corresponding `Button`s to `GamingTweaksPage.xaml` under the same "not integrity-verified" section from Task 1.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <human-check>Confirm all 5 new buttons appear on the Gaming Tweaks page under the driver-tools section, each with a distinct label matching its branch (NVIDIA/AMD/Intel driver install; Nvidia Settings Recommended/Default).</human-check>
  </verify>
  <done>Driver Install Latest's 3 branches and Nvidia Settings' 2 branches are embedded, wired, and visible on the page.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - `grep -c "EmbeddedResource Include=\"Resources\\\\GamingScripts" src/AkariToolbox.App/AkariToolbox.App.csproj` returns at least `9` after this task (4 from Task 1 + 5 here).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Driver Install Debloat & Settings (771 lines, largest script in scope)</name>
  <files>src/AkariToolbox.App/AkariToolbox.App.csproj, src/AkariToolbox.App/Resources/GamingScripts/driverinstalldebloat.ps1, src/AkariToolbox.App/ViewModels/GamingTweaksViewModel.cs, src/AkariToolbox.App/Views/GamingTweaksPage.xaml</files>
  <read_first>C:/Users/isleap/Desktop/AkariOS Tweaks/5 Graphics/3 Driver Install Debloat & Settings.ps1 (full 771 lines — this is the one script this phase's RESEARCH pass did not itemize branch-by-branch; determine its actual menu structure by reading it directly, do not assume RESEARCH.md's summary is exhaustive)</read_first>
  <action>
    Read the full 771-line source script to determine its actual `Read-Host` menu structure (RESEARCH.md explicitly did not itemize this script's branches, noting only that it downloads 7-Zip/Inspector from `github.com/FR33THYFR33THY/Ultimate-Files` and opens official nvidia.com/amd.com/intel.com pages, per `3 ...:22,59,174,392,603`). If the script has N distinct top-level menu branches, create one embedded resource per branch (`driverinstalldebloat-{branch}.ps1`, following Task 1/2's naming convention) with each branch's `Read-Host` wrapper stripped and its underlying logic otherwise byte-identical to the source (D-06 "exactly as authored" — this is the largest script in scope, so resist any temptation to simplify or decompose its logic beyond stripping the interactive menu). If the script turns out to have no top-level menu (a single linear flow, like DirectX/C++), embed it as a single `driverinstalldebloat.ps1` resource instead — update this task's `<files>` accordingly if the actual branch count differs from the single filename listed above.

    Add the resulting `<EmbeddedResource Include=...>` entries to `AkariToolbox.App.csproj`. Add one `[RelayCommand]` per embedded branch to `GamingTweaksViewModel` (same risk-disclosure-then-`RunEmbeddedScriptAsync` shape as Tasks 1/2), and one `Button` per branch to `GamingTweaksPage.xaml` under the driver-tools section.
  </action>
  <verify>
    <automated>dotnet build AkariToolbox.slnx -c Debug</automated>
    <human-check>Confirm the new button(s) for Driver Install Debloat & Settings appear on the Gaming Tweaks page. Do not click-test this one (771 lines, longest-running, most invasive of the 6 D-06 scripts) — visual/build confirmation only is sufficient for this task.</human-check>
  </verify>
  <done>Driver Install Debloat & Settings is embedded (per its actual branch structure, determined by reading the source) and wired identically to the other 5 D-06 scripts.</done>
  <acceptance_criteria>
    - `dotnet build AkariToolbox.slnx -c Debug` exits 0.
    - At least one `<EmbeddedResource Include="Resources\GamingScripts\driverinstalldebloat...">` entry exists in `AkariToolbox.App.csproj`.
    - All 6 D-06 scripts now have at least one embedded resource + RelayCommand + Button (running total across this plan's 3 tasks).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elevated process -> internet (github.com/FR33THYFR33THY/Ultimate-Files, official vendor domains) -> downloaded third-party binary execution | The highest-severity trust boundary in this phase and, per PROJECT.md, in the whole app: an elevated process downloads and runs third-party binaries with no integrity verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Tampering | All 6 D-06 embedded scripts download and execute third-party binaries with admin rights, no SHA256/signature verification | critical | accept | **Explicit user decision D-06 (02-CONTEXT.md)** — no added verification for v1, despite this being the app's top-severity risk class per PROJECT.md's threat model; each script's download source is unchanged from the source script (`github.com/FR33THYFR33THY/Ultimate-Files` for 5 of 6; official vendor domains for `2 Driver Install Latest.ps1`'s driver downloads); the risk is surfaced to the user both in-code (a visible pre-launch `ILogConsoleService` line, Task 1) and in the page's own UI section header — not silently hidden |
| T-02-SC | Tampering | Embedded `.ps1` resource extraction/execution pipeline (`RunEmbeddedScriptAsync`, Plan 02-01) | high | mitigate | Extraction path is `Path.GetTempPath()` + a GUID-suffixed filename (not attacker-predictable/fixed); the file is deleted in a `finally` block after execution; the resource itself is a build-time embedded asset (not runtime-downloaded), so the extraction step introduces no additional tampering surface beyond the already-accepted T-02-14 risk |
| T-02-15 | Denial of Service | 771-line `Driver Install Debloat & Settings.ps1` runs unattended after Read-Host stripping | medium | mitigate | Each stripped branch is extracted from the exact source script content (D-06 "exactly as authored"), preserving behavior parity with the manually-tested source script; `IScriptRunner.RunProcessAsync`'s existing timeout/never-throw contract (inherited via `RunEmbeddedScriptAsync`) prevents a hung install from blocking the UI thread |
</threat_model>

<verification>
- `dotnet build AkariToolbox.slnx -c Debug` succeeds.
- All 6 D-06 scripts have at least one embedded resource, `RelayCommand`, and `Button`.
- Elevated manual launch: driver-tools section renders with all buttons, risk-disclosure log line fires before each launch.
</verification>

<success_criteria>
All 6 D-06 network-dependent scripts are ported as embedded resources split per source menu branch, wired to the Gaming Tweaks page, with the accepted no-verification risk surfaced plainly at runtime.
</success_criteria>

<output>
Create `.planning/phases/02-gaming-tweaks/02-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- Embedded `.ps1` resources under `src/AkariToolbox.App/Resources/GamingScripts/` (10-13 files depending on Task 3's actual branch count)
- `GamingTweaksViewModel` RelayCommands: `RunDriverCleanAutoAsync`, `RunDriverCleanManualAsync`, `RunDirectXAsync`, `RunCppAsync`, `RunDriverInstallLatestNvidiaAsync`/`-AmdAsync`/`-IntelAsync`, `RunNvidiaSettingsRecommendedAsync`/`-DefaultAsync`, and one or more `RunDriverInstallDebloat*Async` (Task 3's actual count TBD by source-read)
- Driver-tools UI section on `GamingTweaksPage.xaml`

## Flagged Assumptions

- **Driver Install Debloat & Settings branch count**: RESEARCH.md explicitly did not itemize this 771-line script's internal menu structure, calling it "out of scope per D-06." Task 3 requires the executor to read the actual source and determine the real branch count at execution time rather than trusting a number invented at planning time — this is a deliberate "read the source, don't guess" instruction, not a gap.
</output>
